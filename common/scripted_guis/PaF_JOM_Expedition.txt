scripted_gui = {

	JOM_container_gui = {
		window_name = "PaF_JOM_container_gui"
		context_type = decision_category
		parent_window_token = decision_tab

		properties = {
			EXPEDITION_BAR = { frame = JOM_expedition_var }
		}

		dynamic_lists = {
		
		}

		triggers = {
			
		}

		effects = {
			JOM_expedition_button_click = {
				if = {
					limit = {
						NOT = { 
							has_country_flag = JOM_expedition_map_open
						}
					}
					set_country_flag = JOM_expedition_map_open
				}
				else_if = {
					limit = {
						has_country_flag = JOM_expedition_map_open
					}
					clr_country_flag = JOM_expedition_map_open
				}	
			}
		}
	}
	JOM_map_container_gui = {
		window_name = "PaF_JOM_map_container_gui"
		context_type = player_context
		parent_window_token = PaF_JOM_map_container_gui
		visible = {
			has_country_flag = JOM_expedition_map_open
		}
		properties = {
			
		}

		dynamic_lists = {
		
		}

		triggers = {
			JOM_expedition_map_visible = {
				has_country_flag = JOM_expedition_map_open
			}
		}

		effects = {

		}
	}
}
