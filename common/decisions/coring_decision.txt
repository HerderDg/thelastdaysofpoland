core_category = {
	core_the_state = {

		ai_will_do = {
			base = 100
		}
		target_array = global.countries
		target_non_existing = yes
		target_trigger = {
			FROM = {
				any_core_state = {
					NOT = { is_core_of = ROOT }
					is_controlled_by = ROOT
				}
			}
		}
		available = {
			has_political_power > FROM.total_coring_cost
		}
		days_remove = 30
		custom_cost_trigger = {
			has_political_power > FROM.total_coring_cost
		}
		custom_cost_text = coring_cost_text
		complete_effect = {
			set_temp_variable = { coring_pp_cost = FROM.total_coring_cost }
			multiply_temp_variable = { coring_pp_cost = -1 }
			add_political_power = coring_pp_cost
			log = "[GetDateText]: Decision core_the_state (v: [?FROM.coring_cost])"
		}
		remove_effect = {
			PaF_change_capital_coring_prequel = yes
			every_controlled_state = {
				limit = {
					is_core_of = FROM
				}
				add_core_of = ROOT
			}
			
		}
	}
}