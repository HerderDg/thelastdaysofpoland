
unification_category = {	


lodz_voivodeship_unification = {

	icon = GFX_decision_lodz
	
	available = {
		has_war = no
		custom_trigger_tooltip = {
			tooltip = controls_lodz_voivodeship
			can_unify_lodz = yes
		}
		NOT = { is_subject = yes }
		if = {
			limit = {
				NOT = {
					original_tag = WML
				}
			}
			NOT = { is_in_faction_with = WML }
		}
		
		if = {
			limit = {
					original_tag = WML
			}
			OR = {
				has_country_flag = WML_set_dialog_total
				has_country_flag = WML_set_dialog_partial
			}
		}
		if = {
			limit = {
				is_in_faction = yes
			}
			is_faction_leader = yes
		}	
	}
	
	fire_only_once = yes

	cost = 0
	
	visible = {
		is_lodzkie = yes
		NOT = {
			has_global_flag = lodzkie_unified
		}
	}

	complete_effect = {
		log = "[GetDateText]: Decision lodz_voivodeship_unification"
		set_country_flag = unified_voivodeship
		hidden_effect = {
			add_dynamic_modifier = {
				modifier = developed_economy_modifier
			}
			add_ideas = administrative_strain
			set_technology = {
				public_services = 1
			}
			if = {
				limit = {
					NOT = { has_idea = WML_baluty_radiation }
				}
				add_ideas = WML_baluty_radiation
			}
		}
		set_global_flag = lodzkie_unified
		add_research_slot = 1
		if = {
			limit = {
				NOT = { original_tag = ZHP }
			}
			add_victory_points = {
				province = 7602
				value = -15
			}
		}
		
		if = {
			limit = { original_tag = ZHP }
			set_cosmetic_tag = ZHP_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.4 }
				}
			}
			
		}



		else_if = {
			limit = { original_tag = SAP
					 check_variable = {
						var = sapek_sanity
						value = 50
						compare = greater_than_or_equals
					} 
					}
			set_cosmetic_tag = SAP_sane_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.5 }
				}
			}
			
		}
		

		else_if = {
			limit = { original_tag = SAP 
					 check_variable = {
						var = sapek_sanity
						value = 50
						compare = less_than
					} 
			}
			set_cosmetic_tag = SAP_insane_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.5 }
				}
			}
			
		}



		else_if = {
			limit = { original_tag = WML }
			set_cosmetic_tag = WML_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.1 }
				}
			}
			
		}



		else_if = {
			limit = { original_tag = BBW }
			set_cosmetic_tag = BBW_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.2 }
				}
			}
			
		}



		else_if = {
			limit = { 
					 original_tag = ZYD
					 has_government = squattism
				}
			set_cosmetic_tag = ZYD_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.3 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = ZYD
					 has_government = marxism_cranism }
			set_cosmetic_tag = ZYD_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.37 }
				}
			}
			
		}


		else_if = {
			limit = { original_tag = KRS
					  has_government = corporativism }
			set_cosmetic_tag = KRS_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.6 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = POG }
			set_cosmetic_tag = POG_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.35 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = RUT }
			set_cosmetic_tag = RUT_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.36 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = AMW }
			set_cosmetic_tag = AMW_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.177 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = KAR }
			set_cosmetic_tag = KAR_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.184 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = LKS }
			set_cosmetic_tag = LKS_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.188 }
				}
			}
			
		}	
		
		else_if = {
			limit = { original_tag = RDM }
			set_cosmetic_tag = RDM_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.193 }
				}
			}
			
		}	
		
		else_if = {
			limit = { original_tag = TIG }
			set_cosmetic_tag = TIG_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.172 }
				}
			}
			
		}			

	}
	ai_will_do = { factor = 1 }
}

lubelskie_voivodeship_unification = {

	icon = GFX_decision_lubelskie
	
	available = {
		has_war = no
		custom_trigger_tooltip = {
			tooltip = controls_lublin_voivodeship
			can_unify_lubelskie = yes
		}
		NOT = { is_subject = yes }
	}
	
	fire_only_once = yes

	cost = 0

	visible = { 
		is_lubelskie = yes
		NOT = {
			has_global_flag = lubelskie_unified
		}
	}

	complete_effect = {
		log = "[GetDateText]: Decision lubelskie_voivodeship_unification"
		set_country_flag = unified_voivodeship
		set_global_flag = lubelskie_unified
		add_research_slot = 1
		hidden_effect = {
			add_dynamic_modifier = {
				modifier = developed_economy_modifier
			}
			add_ideas = administrative_strain
			set_technology = {
				public_services = 1
			}
		}
		if = {
			limit = { original_tag = CRN }
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.7 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = PUL }
			set_cosmetic_tag = PUL_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.8 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = KSN }
			set_cosmetic_tag = KSN_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.9 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = SWD }
			set_cosmetic_tag = SWD_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.10 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = ZUK }
			hidden_effect = {
				country_event = {
					id = zuk.2
    				days = 30
				}
				set_capital = {
					state = 363
					remember_old_capital = no
				}
			}
			
			set_cosmetic_tag = ZUK_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.11 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = BIL }
			set_cosmetic_tag = BIL_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.12 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = BPD }
			set_cosmetic_tag = BPD_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.13 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = LEN }
			set_cosmetic_tag = LEN_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.154 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = TEN }
			set_cosmetic_tag = TEN_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.155 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = ORD }
			set_cosmetic_tag = ORD_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification_generic.1 }
				}
			}
			
		}
	}
	ai_will_do = { factor = 1 }
}

zaglebie_voivodeship_unification = {

	icon = GFX_decision_zaglebie
	
	available = {
		has_war = no
		custom_trigger_tooltip = {
			tooltip = controls_zaglebie
			can_unify_zaglebie = yes
		}
		NOT = { is_subject = yes }
		if = {
			limit = {
				is_in_faction = yes
			}
			is_faction_leader = yes
		}
	}
	
	fire_only_once = yes

	cost = 0
	
	visible = {
		OR = {
			is_zaglebie_and_jura = yes
			AND = {
				original_tag = MYS
				has_country_flag = MYS_eurocrusade
			}
			AND = {
				original_tag = DGR
				has_global_flag = ZAG_congress_of_bedzin_failure
			}
			AND = {
				original_tag = JAW
				has_global_flag = ZAG_congress_of_bedzin_failure
				NOT = { has_cosmetic_tag = JAW_SOS }
			}			
		}
		NOT = {
			has_global_flag = zaglebie_unified
		}
	}

	complete_effect = {
		log = "[GetDateText]: Decision zaglebie_voivodeship_unification"
		set_country_flag = unified_voivodeship
		set_global_flag = zaglebie_unified
		add_research_slot = 1
		hidden_effect = {
			add_dynamic_modifier = {
				modifier = developed_economy_modifier
			}
			add_ideas = administrative_strain
			set_technology = {
				public_services = 1
			}
		}

		if = {
			limit = {
				NOT = { original_tag = JWK }
			}
			add_victory_points = {
				province = 8405
				value = -30
			}
		}
		if = {
			limit = {
				NOT = { original_tag = BRZ }
			}
			add_victory_points = {
				province = 8473
				value = -29
			}
		}
		

		if = {
			limit = { original_tag = DGR }
			set_cosmetic_tag = DGR_voivodeship
			set_variable = {
				reindustrialisation_var = 0
			}
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.14 }
				}
			}
			
		}



		else_if = {
			limit = { original_tag = SOS }
			set_cosmetic_tag = SOS_voivodeship
			hidden_effect = {
				if = {
					limit = {
						has_global_flag = ZAG_congress_of_bedzin_failure
					}
					every_country = {
						limit = { NOT = { is_ai = yes } }
						news_event = { id = unification.15 }
					}
				}
				if = {
					limit = {
						has_global_flag = ZAG_congress_of_bedzin_success
					}
					every_country = {
						limit = { NOT = { is_ai = yes } }
						news_event = { id = unification_alt.15 }
					}
					SOS = {
						country_event = {
							id = bedzincon.14
							days = 30
						}
					}					
				}
			}
			
		}



		else_if = {
			limit = { original_tag = JAW }
			set_cosmetic_tag = JAW_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.16 }
				}
			}
			
		}


		else_if = {
			limit = { original_tag = JUR }
			set_cosmetic_tag = JUR_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.17 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = ZAW }
			set_cosmetic_tag = ZAW_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.203 }
				}
			}
			
		}



		else_if = {
			limit = { original_tag = CCH }
			set_cosmetic_tag = CCH_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.18 }
				}
			}
			
		}



		else_if = {
			limit = { original_tag = BRZ }
			set_cosmetic_tag = BRZ_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.19 }
				}
			}
			
		}



		else_if = {
			limit = { original_tag = JWK }
			set_cosmetic_tag = JWK_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.20 }
				}
			}
			
		}


	}
	ai_will_do = { factor = 1 }
}

upper_silesia_voivodeship_unification = {

	icon = GFX_decision_slask
	
	available = {
		has_war = no
		custom_trigger_tooltip = {
			tooltip = controls_upper_silesia
			can_unify_upper_silesia = yes
		}
		NOT = { is_subject = yes }
		if = {
			limit = {
				ROOT = { is_in_faction = yes }
			}
			is_faction_leader = yes			
		}
		OR = {
			MYS = { exists = no }
			MYS = { is_subject_of = ROOT }
			MYS = { has_country_flag = MYS_eurocrusade }
		}
	}
	
	fire_only_once = yes

	cost = 0
	
	visible = {
		is_upper_silesia = yes
		NOT = {
			has_global_flag = slaskie_unified
		}
	}

	complete_effect = {
		log = "[GetDateText]: Decision upper_silesia_voivodeship_unification"
		set_country_flag = unified_voivodeship
		set_global_flag = slaskie_unified
		add_research_slot = 1
		hidden_effect = {
			add_dynamic_modifier = {
				modifier = developed_economy_modifier
			}
			add_ideas = administrative_strain
			set_technology = {
				public_services = 1
			}
		}
		if = {
			limit = { original_tag = ASG }
			set_cosmetic_tag = ASG_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.21 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = WOD
			has_government = neonationalism
			}
			set_cosmetic_tag = WOD_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.22 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = WOD
			has_government = postpolonism
			}
			set_cosmetic_tag = WOD_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.211 }
				}
			}
			
		}		

		else_if = {
			limit = { original_tag = DUR }
			set_cosmetic_tag = DUR_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.181 }
				}
			}
			
		}		



		else_if = {
			limit = { original_tag = JSW }
			set_cosmetic_tag = JSW_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.23 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = SOL }
			set_cosmetic_tag = SOL_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification_generic.1 }
				}
			}
			
		}	

		else_if = {
			limit = { original_tag = SBW }
			set_cosmetic_tag = SBW_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification_generic.1 }
				}
			}
			
		}		

		else_if = {
			limit = { original_tag = GLI }
			set_cosmetic_tag = GLI_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.24 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = CHO }
			set_cosmetic_tag = CHO_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.25 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = RAS }
			set_cosmetic_tag = RAS_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.26 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = FER }
			set_cosmetic_tag = FER_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.27 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = RUD }
			set_cosmetic_tag = RUD_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.28 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = KAT }
			set_cosmetic_tag = KAT_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.29 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = TRC }
			set_cosmetic_tag = TRC_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.30 }
				}
			}
		}

		else_if = {
			limit = { original_tag = TYS }
			set_cosmetic_tag = TYS_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.200 }
				}
			}
			
		}		
	}

	ai_will_do = { factor = 1 }
}

south_greater_poland_voivodeship_unification = {

	icon = GFX_decision_kalisz
	
	available = {
		has_war = no
		custom_trigger_tooltip = {
			tooltip = controls_southern_greater_poland
			can_unify_kaliskie = yes
		}
		NOT = { is_subject = yes }
		OR = {
			is_in_faction = no
			is_faction_leader = yes
		}
	}
	
	fire_only_once = yes

	cost = 0
	
	visible = {
		is_southern_greater_poland = yes
		NOT = {
			has_global_flag = kaliskie_unified
		}
	}

	complete_effect = {
		log = "[GetDateText]: Decision south_greater_poland_voivodeship_unification"
		set_country_flag = unified_voivodeship
		set_global_flag = kaliskie_unified
		mark_focus_tree_layout_dirty = yes
		add_research_slot = 1

		hidden_effect = {
			add_dynamic_modifier = {
				modifier = developed_economy_modifier
			}
			add_ideas = administrative_strain
			set_technology = {
				public_services = 1
			}
		}
		if = {
			limit = { original_tag = CAL }
			set_cosmetic_tag = CAL_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.31 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = FEL }
			set_cosmetic_tag = FEL_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.33 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = GNI }
			set_cosmetic_tag = GNI_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.32 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = LIS }
			set_cosmetic_tag = LIS_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.187 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = LSZ }
			set_cosmetic_tag = LSZ_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.157 }
				}
			}
			
		}

	}
        ai_will_do = { factor = 1 }

}

tatry_unification = {

	icon = GFX_decision_gory
	
	available = {
		has_war = no
		custom_trigger_tooltip = {
			tooltip = controls_tatry
			can_unify_tatry = yes
		}
		NOT = { is_subject = yes }
	}
	
	fire_only_once = yes

	cost = 0
	
	visible = {
		is_tatry_region = yes
		NOT = {
			has_global_flag = tatry_unified
		}
	}

	complete_effect = {
		log = "[GetDateText]: Decision tatry_unification"
		set_country_flag = unified_voivodeship
		set_global_flag = tatry_unified
		add_research_slot = 1
		hidden_effect = {
			add_dynamic_modifier = {
				modifier = developed_economy_modifier
			}
			add_ideas = administrative_strain
			set_technology = {
				public_services = 1
			}
		}
		if = {
			limit = { original_tag = CIE }
			set_cosmetic_tag = CIE_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.38 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = BSK
					  has_government = autocracy }
			set_cosmetic_tag = BSK_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.39 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = BBA }
			set_cosmetic_tag = BBA_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.178 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = GUR }
			set_cosmetic_tag = GUR_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.40 }
				}
			}
			if = {
				limit = { ZKP = {
					is_subject_of = GUR
				} }
				annex_country = {
					target = ZKP
				}
			}
		}

		else_if = {
			limit = { original_tag = SCZ }
			set_cosmetic_tag = SCZ_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.42 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = LEM }
			set_cosmetic_tag = LEM_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.43 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = WYM} 
			set_cosmetic_tag = WYM_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification_generic.1 }
					#news_event = { id = unification.44 }
				}
			}
			
		}
	}
	ai_will_do = { factor = 1 }
}

northen_greater_poland_voivodeship_unification = {

	icon = GFX_decision_poznan
	
	available = {
		has_war = no
		custom_trigger_tooltip = {
			tooltip = controls_poznanskie
			can_unify_north_greater_poland = yes
		}
		NOT = { is_subject = yes }
	}
	
	visible = {
		date > 2028.01.01
	}

	fire_only_once = yes

	cost = 0
	
	visible = {
		is_northern_greater_poland = yes
		NOT = {
			has_global_flag = poznanskie_unified
		}
	}

	complete_effect = {
		log = "[GetDateText]: Decision northen_greater_poland_voivodeship_unification"
		set_country_flag = unified_voivodeship
		set_global_flag = poznanskie_unified
		add_research_slot = 1
		hidden_effect = {
			add_dynamic_modifier = {
				modifier = developed_economy_modifier
			}
			add_ideas = administrative_strain
			set_technology = {
				public_services = 1
			}
		}
		if = {
			limit = { original_tag = POZ }
			set_cosmetic_tag = POZ_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.50 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = KAM }
			set_cosmetic_tag = KAM_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.71 }
				}
				swap_ideas = {
				    remove_idea = POZ_surrender
				    add_idea = POZ_surrender2
			    }
			}
			
		}

		else_if = {
			limit = { original_tag = MOT }
			set_cosmetic_tag = MOT_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.72 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = SUS }
			set_cosmetic_tag = SUS_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.34 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = SYJ }
			set_cosmetic_tag = SYJ_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.51 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = PIL
					  has_government = corporativism }
			set_cosmetic_tag = PIL_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.152 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = NAC }
			set_cosmetic_tag = NAC_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.52 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = SLU }
			set_cosmetic_tag = SLU_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.61 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = PIL
					  has_government = postpolonism }
			set_cosmetic_tag = PIL_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.53 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = WRT }
			set_cosmetic_tag = WRT_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.153 }
				}
			}
			
		}	
		
		else_if = {
			limit = { original_tag = ROZ }
			set_cosmetic_tag = ROZ_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.195 }
				}
			}
			
		}			
		
		else_if = {
			limit = { original_tag = ZBK }
			set_cosmetic_tag = ZBK_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.196 }
				}
			}
			
		}
      
 }
  ai_will_do = { factor = 1 }

}

legnica_voivodeship_unification = {

	icon = GFX_decision_legnica
	
	available = {
		has_war = no
		custom_trigger_tooltip = {
			tooltip = controls_legnica
			can_unify_legnica = yes
		}
		NOT = { is_subject = yes }
	}
	
	visible = {
		date > 2028.01.01
	}

	fire_only_once = yes

	cost = 0
	
	visible = {
		is_legnica = yes
		NOT = {
			has_global_flag = legnica_unified
		}
	}

	complete_effect = {
		log = "[GetDateText]: Decision legnica_voivodeship_unification"
		set_country_flag = unified_voivodeship
		set_global_flag = legnica_unified
		add_research_slot = 1
		hidden_effect = {
			add_dynamic_modifier = {
				modifier = developed_economy_modifier
			}
			add_ideas = administrative_strain
			set_technology = {
				public_services = 1
			}
		}
		if = {
			limit = { original_tag = ZAG }
			set_cosmetic_tag = ZAG_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.69 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = WIN }
			set_cosmetic_tag = WIN_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.70 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = PZL }
			set_cosmetic_tag = PZL_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification_generic.1 }
					#news_event = { id = unification.191 }
				}
			}
			
		}		

		else_if = {
			limit = { original_tag = EKS }
			set_cosmetic_tag = EKS_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.45 }
				}
			}
		}

		else_if = {
			limit = { original_tag = ECO }
			set_cosmetic_tag = ECO_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification_generic.1 }
				}
			}
		}
		
		else_if = {
			limit = { original_tag = BOG }
			set_cosmetic_tag = BOG_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification_generic.1 }
				}
			}
		}		

		else_if = {
			limit = { original_tag = KGH }
			set_cosmetic_tag = KGH_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.48 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = TOU }
			set_cosmetic_tag = TOU_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.199 }
				}
			}
			
		}	

		else_if = {
			limit = { original_tag = CYG }
			set_cosmetic_tag = CYG_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.60 }
				}
			}
			
		}
      
	}
  ai_will_do = { factor = 1 }

}

lower_silesia_voivodeship_unification = {

	icon = GFX_decision_wroclaw
	
	available = {
		has_war = no
		custom_trigger_tooltip = {
			tooltip = controls_lower_silesia
			can_unify_lower_silesia = yes
		}
		NOT = { is_subject = yes }
	}
	
	visible = {
		date > 2028.01.01
	}

	fire_only_once = yes

	cost = 0
	
	visible = {
		is_lower_silesia = yes
		NOT = {
			has_global_flag = dolnoslaskie_unified
		}
	}

	complete_effect = {
		log = "[GetDateText]: Decision lower_silesia_voivodeship_unification"
		set_country_flag = unified_voivodeship
		set_global_flag = dolnoslaskie_unified
		add_research_slot = 1
		hidden_effect = {
			add_dynamic_modifier = {
				modifier = developed_economy_modifier
			}
			add_ideas = administrative_strain
			set_technology = {
				public_services = 1
			}
		}
		if = {
			limit = { original_tag = WRO }
			set_cosmetic_tag = WRO_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.46 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = CZE }
			set_cosmetic_tag = CZE_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.47 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = RYB }
			set_cosmetic_tag = RYB_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.49 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = ANA }
			set_cosmetic_tag = ANA_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.57 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = ZUL }
			set_cosmetic_tag = ZUL_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.59 }
				}
			}
			
		}
	
		
		else_if = {
			limit = { original_tag = MNL }
			set_cosmetic_tag = MNL_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.58 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = MPK }
			set_cosmetic_tag = MPK_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.78 }
				}
			}
			
		}
      
	}
  ai_will_do = { factor = 1 }

}

opole_voivodeship_unification = {

	icon = GFX_decision_opole
	
	available = {
		has_war = no
		custom_trigger_tooltip = {
			tooltip = controls_opole_voivodeship
			can_unify_opolskie = yes
		}
		NOT = { is_subject = yes }
	}
	
	visible = {
		date > 2028.01.01
	}

	fire_only_once = yes

	cost = 0
	
	visible = {
		is_opolskie = yes
		NOT = {
			has_global_flag = opolskie_unified
		}
	}

	complete_effect = {
		log = "[GetDateText]: Decision opole_voivodeship_unification"
		set_country_flag = unified_voivodeship
		set_global_flag = opolskie_unified
		add_research_slot = 1
		hidden_effect = {
			add_dynamic_modifier = {
				modifier = developed_economy_modifier
			}
			add_ideas = administrative_strain
			set_technology = {
				public_services = 1
			}
		}
		if = {
			limit = { original_tag = MNI }
			set_cosmetic_tag = MNI_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.63 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = OPO }
			set_cosmetic_tag = OPO_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.64 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = LCH }
			set_cosmetic_tag = LCH_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.65 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = WAN }
			set_cosmetic_tag = WAN_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.66 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = LEH	}
			set_cosmetic_tag = LEH_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.67 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = NYS }
			set_cosmetic_tag = NYS_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.68 }
				}
				NYS = {
					add_ideas = podlasie_tribe_idea
				}
			}
			
		}

	}
	ai_will_do = { factor = 1 }
}

west_pomerania_voivodeship_unification = {

	icon = GFX_decision_zachpom
	
	available = {
		has_war = no
		custom_trigger_tooltip = {
			tooltip = controls_west_pomerania_voivodeship
			can_unify_west_pomerania = yes
		}
		NOT = { is_subject = yes }
	}
	
	visible = {
		date > 2028.01.01
	}

	fire_only_once = yes

	cost = 0
	
	visible = {
		is_west_pomerania = yes
		NOT = {
			has_global_flag = zachodniopomorskie_unified
		}
	}

	complete_effect = {
		log = "[GetDateText]: Decision west_pomerania_voivodeship_unification"
		set_country_flag = unified_voivodeship
		set_global_flag = zachodniopomorskie_unified
		add_research_slot = 1
		set_technology = {
			public_services = 1
		}
		hidden_effect = {
			add_dynamic_modifier = {
				modifier = developed_economy_modifier
			}
			add_ideas = administrative_strain
		}
		if = {
			limit = { original_tag = JAS }
			set_cosmetic_tag = JAS_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.73 }
				}
			}
			
		}

		if = {
			limit = { original_tag = BOR }
			set_cosmetic_tag = BOR_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.197 }
				}
			}
			
		}	

		if = {
			limit = { original_tag = SNF }
			set_cosmetic_tag = SNF_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.198 }
				}
			}
			
		}			

		else_if = {
			limit = { original_tag = EKW }
			set_cosmetic_tag = EKW_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.54 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = STE }
			set_cosmetic_tag = STE_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.74 }
				}
			}
		}

		else_if = {
			limit = { original_tag = SWI
						has_government = marxism_cranism }
			set_cosmetic_tag = SWI_voivodeship_zmurkiewicz
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.75 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = SWI
						has_government = paternal_conservatism }
			set_cosmetic_tag = SWI_voivodeship_brudzinski
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.751 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = JOM }
			set_cosmetic_tag = JOM_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.76 }
				}
			}
			
		}	

		else_if = {
			limit = { original_tag = USP }
			set_cosmetic_tag = USP_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.77 }
				}
			}
			
		}	
		
		else_if = {
			limit = { original_tag = NOR }
			set_cosmetic_tag = NOR_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.166 }
				}
			}
			
		}		

	}
	ai_will_do = { factor = 1 }
}

kuyavia_voivodeship_unification = {

	icon = GFX_decision_kujpom
	
	available = {
		has_war = no
		custom_trigger_tooltip = {
			tooltip = controls_kuyavia_voivodeship
			can_unify_kuyavia = yes
		}
		NOT = { is_subject = yes }
		if = {
			limit = {
				ROOT = { is_in_faction = yes }
			}
			is_faction_leader = yes
		}
	}
	
	fire_only_once = yes

	cost = 0
	
	visible = {
		AND = {
			is_kuyavia = yes
			NOT = {
				has_global_flag = kuyavia_unified
			}
		}
	}

	complete_effect = {
		log = "[GetDateText]: Decision kuyavia_voivodeship_unification"
		set_country_flag = unified_voivodeship
		set_global_flag = kuyavia_unified
		add_research_slot = 1
		set_technology = {
			public_services = 1
		}
		hidden_effect = {
			add_dynamic_modifier = {
				modifier = developed_economy_modifier
			}
			add_ideas = administrative_strain
		}
		if = {
			limit = { original_tag = INO }
			set_cosmetic_tag = INO_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.79 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = TOR }
			set_cosmetic_tag = TOR_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.80 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = GRU }
			set_cosmetic_tag = GRU_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.81 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = KUJ }
			set_cosmetic_tag = KUJ_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.82 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = BYD 
			          has_government = euroliberalism }
			set_cosmetic_tag = BYD_voivodeship_bru

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.83 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = BYD 
			          has_government = neonationalism }
			set_cosmetic_tag = BYD_voivodeship_scb

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.207 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = BYD 
			          has_government = paternal_conservatism }
			set_cosmetic_tag = BYD_voivodeship_pre

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.208 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = AMA}
			set_cosmetic_tag = AMA_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.149 }
				}
			}
			
		}

 #		else_if = {
 #			limit = { original_tag = AMA 
 #			          has_government = squattism }
 #			set_cosmetic_tag = AMA_voivodeship
 #
 #			hidden_effect = {
 #				every_country = {
 #					limit = { NOT = { is_ai = yes } }
 #					#news_event = { id = unification.205 }
 #					news_event = { id = unification_generic.1 }
 #				}
 #			}
 #			
 #		}
 #		
 #		else_if = {
 #			limit = { original_tag = AMA 
 #			          has_government = corporativism }
 #			set_cosmetic_tag = AMA_voivodeship
 #
 #			hidden_effect = {
 #				every_country = {
 #					limit = { NOT = { is_ai = yes } }
 #					#news_event = { id = unification.206 }
 #					news_event = { id = unification_generic.1 }
 #				}
 #			}
 #			
 #		}
		
		else_if = {
			limit = { original_tag = UFO }
			set_cosmetic_tag = UFO_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.174 }
				}
			}
			
		}		
		
 #		else_if = {
 #			limit = { original_tag = TOR
 #					  has_government = freemarketism }
 #			set_cosmetic_tag = TOR_voivodeship
 #
 #			hidden_effect = {
 #				every_country = {
 #					limit = { NOT = { is_ai = yes } }
 #					news_event = { id = unification.150 }
 #				}
 #			}
 #			
 #		}

		else_if = {
			limit = { original_tag = HOG }
			set_cosmetic_tag = HOG_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.151 }
				}
			}
			
		}
		
		

	}
	ai_will_do = { factor = 1 }
}

pomerelia_voivodeship_unification = {

	icon = GFX_decision_pom
	
	available = {
		has_war = no
		custom_trigger_tooltip = {
			tooltip = controls_pomerelia_voivodeship
			can_unify_pomerelia = yes
		}
		NOT = { is_subject = yes }
	}
	
	fire_only_once = yes

	cost = 0
	
	visible = { 
		is_pomerelia = yes
		NOT = {
			has_global_flag = pomorskie_unified
		}
	}

	complete_effect = {
		log = "[GetDateText]: Decision pomerelia_voivodeship_unification"
		set_country_flag = unified_voivodeship
		set_global_flag = pomorskie_unified
		add_research_slot = 1
		set_technology = {
			public_services = 1
		}
		hidden_effect = {
			add_dynamic_modifier = {
				modifier = developed_economy_modifier
			}
			add_ideas = administrative_strain
		}		
		if = {
			limit = { original_tag = OWS }
			set_cosmetic_tag = OWS_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.84 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = BOL }
			set_cosmetic_tag = BOL_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.85 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = GDA }
			set_cosmetic_tag = GDA_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.86 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = GAY }
			set_cosmetic_tag = GAY_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.87 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = KSZ }
			set_cosmetic_tag = KSZ_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.88 }
				}
			}
			
		}
			
		else_if = {
			limit = { original_tag = KSC }
			set_cosmetic_tag = KSC_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.185 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = TVI }
			set_cosmetic_tag = TVI_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.175 }
				}
			}
			
		}		

		else_if = {
			limit = { original_tag = RLE }
			set_cosmetic_tag = RLE_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					#news_event = { id = unification.194 }
					news_event = { id = unification_generic.1 }
				}
			}
			
		}		

		else_if = {
			limit = { original_tag = HEL }
			set_cosmetic_tag = HEL_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.89 }
				}
			}
			
		}	
		
		else_if = {
			limit = { original_tag = CZL }
			set_cosmetic_tag = CZL_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.90 }
				}
			}
			
		}	

		else_if = {
			limit = { original_tag = LOT }
			set_cosmetic_tag = LOT_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.91 }
				}
			}
			
		}	

		else_if = {
			limit = { original_tag = KOR }
			set_cosmetic_tag = KOR_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.92 }
				}
			}
			
		}		

	}
	ai_will_do = { factor = 1 }
}

warmia_and_masuria_voivodeship_unification = {

	icon = GFX_decision_wm
	
	available = {
		has_war = no
		custom_trigger_tooltip = {
			tooltip = controls_warmia_and_masuria_voivodeship
			can_unify_warmia_and_masuria = yes
		}
		NOT = { is_subject = yes }
	}
	
	fire_only_once = yes

	cost = 0
	
	visible = { 
		is_warmia_and_masuria = yes
		NOT = {
			has_global_flag = warminsko_mazurskie_unified
		}
	}

	complete_effect = {
		log = "[GetDateText]: Decision warmia_and_masuria_voivodeship_unification"
		set_country_flag = unified_voivodeship
		set_global_flag = warminsko_mazurskie_unified
		add_research_slot = 1
		set_technology = {
			public_services = 1
		}
		hidden_effect = {
			add_dynamic_modifier = {
				modifier = developed_economy_modifier
			}
			add_ideas = administrative_strain
		}	
		if = {
			limit = { original_tag = MAL }
			set_cosmetic_tag = MAL_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.93 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = ROS }
			set_cosmetic_tag = ROS_voivodeship
			
			# hidden_effect = {
			# 	every_country = {
			# 		limit = { NOT = { is_ai = yes } }
			# 		news_event = { id = unification.56 }
			# 	}
			# }
			
		}

		else_if = {
			limit = { original_tag = TEU }
			set_cosmetic_tag = TEU_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.94 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = PRU }
			set_cosmetic_tag = PRU_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.95 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = GAL }
			set_cosmetic_tag = GAL_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.183 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = PZW }
			set_cosmetic_tag = PZW_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.96 }
				}
			}
			
		}	

		else_if = {
			limit = { original_tag = WRM }
			set_cosmetic_tag = WRM_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.97 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = ELB }
			set_cosmetic_tag = ELB_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.182 }
					
				}
			}
			
		}		
	
		else_if = {
			limit = { original_tag = OLS }
			set_cosmetic_tag = OLS_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.189 }
					
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = RAJ }
			set_cosmetic_tag = RAJ_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.192 }
					
				}
			}
			
		}		
		
	}
	ai_will_do = { factor = 1 }
}

podlasie_voivodeship_unification = {

	icon = GFX_decision_podlasie
	
	available = {
		has_war = no
		custom_trigger_tooltip = {
			tooltip = controls_podlasie_voivodeship
			can_unify_podlasie = yes
		}
		NOT = { is_subject = yes }
	}
	
	fire_only_once = yes

	cost = 0
	
	visible = { 
		is_podlasie = yes
		NOT = {
			has_global_flag = podlaskie_unified
		}
	}

	complete_effect = {
		log = "[GetDateText]: Decision podlasie_voivodeship_unification"
		set_country_flag = unified_voivodeship
		set_global_flag = podlaskie_unified
		set_technology = {
			public_services = 1
		}
		hidden_effect = {
			add_dynamic_modifier = {
				modifier = developed_economy_modifier
			}
			add_ideas = administrative_strain
		}
		add_research_slot = 1	
		if = {
			limit = { original_tag = SAN }
			set_cosmetic_tag = SAN_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.98 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = TAT }
			set_cosmetic_tag = TAT_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.99 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = BOM }
			set_cosmetic_tag = BOM_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.100 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = KON }
			set_cosmetic_tag = KON_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.101 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = BEL }
			set_cosmetic_tag = BEL_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.102 }
				}
			}
			
		}	

		else_if = {
			limit = { original_tag = LMZ }
			set_cosmetic_tag = LMZ_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.103 }
				}
			}
			
		}	
	
		else_if = {
			limit = { original_tag = SDL }
			set_cosmetic_tag = SDL_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.104 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = AGR }
			set_cosmetic_tag = AGR_voivodeship
			
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.105 }
				}
			}
			
		}		

	}
	ai_will_do = { factor = 1 }
}

radom_province_voivodeship_unification = {

	icon = GFX_decision_radom
	
	available = {
		has_war = no
		custom_trigger_tooltip = {
			tooltip = controls_radom_province_voivodeship
			can_unify_radom_province = yes
		}
		NOT = { is_subject = yes }
	}
	
	fire_only_once = yes

	cost = 0
	
	visible = { 
		is_radom_province = yes
		NOT = {
			has_global_flag = radomskie_unified
		}
	}

	complete_effect = {
		log = "[GetDateText]: Decision radom_province_voivodeship_unification"
		set_country_flag = unified_voivodeship
		set_global_flag = radomskie_unified
		add_research_slot = 1
		set_technology = {
			public_services = 1
		}
		hidden_effect = {
			add_dynamic_modifier = {
				modifier = developed_economy_modifier
			}
			add_ideas = administrative_strain
		}	
		if = {
			limit = { original_tag = SAT }
			set_cosmetic_tag = SAT_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.106 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = RAD }
			set_cosmetic_tag = RAD_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.107 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = AIR }
			set_cosmetic_tag = AIR_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.212 }
				}
			}
		}
		
		else_if = {
			limit = { original_tag = SZY }
			set_cosmetic_tag = SZY_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification_generic.1 }
				}
			}
		}		

		else_if = {
			limit = { original_tag = SKA }
			set_cosmetic_tag = SKA_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.109 }
				}
			}
			
		}		

		else_if = {
			limit = { original_tag = PAB }
			set_cosmetic_tag = PAB_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.173 }
				}
			}
			
		}	

	}
	ai_will_do = { factor = 1 }
}

mazovia_voivodeship_unification = {

	icon = GFX_decision_mazowsze
	
	available = {
		has_war = no
		custom_trigger_tooltip = {
			tooltip = controls_mazovia_voivodeship
			can_unify_mazovia = yes
		}
	}
	
	fire_only_once = yes

	cost = 0
	
	visible = { 
		is_mazovia = yes
		NOT = {
			has_global_flag = mazowieckie_unified
		}
	}

	complete_effect = {
		log = "[GetDateText]: Decision mazovia_voivodeship_unification"
		set_country_flag = unified_voivodeship
		set_global_flag = mazowieckie_unified
		add_research_slot = 1
		set_technology = {
			public_services = 1
		}
		hidden_effect = {
			add_dynamic_modifier = {
				modifier = developed_economy_modifier
			}
			add_ideas = administrative_strain
		}		
		if = {
			limit = { original_tag = ORL }
			set_cosmetic_tag = ORL_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.110 }
				}
			}
		}

		else_if = {
			limit = { original_tag = MAR }
			set_cosmetic_tag = MAR_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.170 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = PLO }
			set_cosmetic_tag = PLO_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.171 }
				}
			}
			
		}		

		else_if = {
			limit = { original_tag = SAS }
			set_cosmetic_tag = SAS_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.55 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = POL }
			set_cosmetic_tag = POL_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.111 }
				}
			}
			
		}
		else_if = {
			limit = { original_tag = SCH }
			set_cosmetic_tag = SCH_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.210 }
				}
			}
		}		

	}
	ai_will_do = { factor = 1 }
}

capital_district_voivodeship_unification = {

	icon = GFX_decision_capital
	
	available = {
		has_war = no
		custom_trigger_tooltip = {
			tooltip = controls_capital_district_voivodeship
			can_unify_capital_district = yes
		}
		NOT = { has_global_flag = CAL_barbarians_win_flag }
		NOT = { is_subject = yes }
	}
	
	fire_only_once = yes

	cost = 0
	
	visible = { 
		is_capital_district = yes
		NOT = {
			has_global_flag = capital_district_unified
		}
	}

	complete_effect = {
		log = "[GetDateText]: Decision capital_district_voivodeship_unification"
		set_country_flag = unified_voivodeship
		set_global_flag = capital_district_unified
		add_research_slot = 1
		set_technology = {
			public_services = 1
		}
		hidden_effect = {
			add_dynamic_modifier = {
				modifier = developed_economy_modifier
			}
			add_ideas = administrative_strain
		}		
		if = {
			limit = { original_tag = MAF }
			set_cosmetic_tag = MAF_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.112 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = WOL }
			set_cosmetic_tag = WOL_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.164 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = CPK }
			set_cosmetic_tag = CPK_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					#news_event = { id = unification.164 }
					news_event = { id = unification_generic.1 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = ATO }
			set_cosmetic_tag = ATO_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					#news_event = { id = unification.164 }
					news_event = { id = unification_generic.1 }
				}
			}
			
		}

		else_if = {
			limit = { 
				original_tag = WOW
				has_country_leader = {
					character = WOW_antoni_macierewicz
					ruling_only = yes
				}				
			}
			set_cosmetic_tag = WOW_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.113 }
				}
			}
			
		}
		else_if = {
			limit = { 
				original_tag = WOW
				NOT = { 
				has_country_leader = {
					character = WOW_antoni_macierewicz
					ruling_only = yes
				}
				}				
			}

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification_generic.1 }
				}
			}
			
		}
		

		else_if = {
			limit = { original_tag = ZYR }
			set_cosmetic_tag = ZYR_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.114 }
				}
			}
			
		}	

		else_if = {
			limit = { 
				original_tag = DUP 
				NOT = { has_cosmetic_tag = DUP_warsaw_right }
			}
			set_cosmetic_tag = DUP_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.115 }
				}
			}
			
		}

		else_if = {
			limit = { 
				original_tag = DUP
				has_cosmetic_tag = DUP_warsaw_right			
			}

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification_generic.1 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = BIE }
			set_cosmetic_tag = BIE_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.116 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = PLC }
			set_cosmetic_tag = PLC_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.117 }
				}
			}
		}

		else_if = {
			limit = { original_tag = ZIE }
			set_cosmetic_tag = ZIE_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.118 }
				}
			}
			
		}
	
		else_if = {
			limit = { original_tag = TVP }
			set_cosmetic_tag = TVP_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.119 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = TRZ }
			set_cosmetic_tag = TRZ_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.120 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = GAR }
			set_cosmetic_tag = GAR_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.108 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = BLO }
			set_cosmetic_tag = BLO_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.167 }
				}
			}
		}	

		else_if = {
			limit = { original_tag = FSO }
			set_cosmetic_tag = FSO_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					#news_event = { id = unification.169 }
					news_event = { id = unification_generic.1 }
				}
			}
		}	
		
		else_if = {
			limit = { original_tag = IPN }
			set_cosmetic_tag = IPN_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.158 }
				}
			}
		}		
		
		else_if = {
			limit = { original_tag = ITA }
			set_cosmetic_tag = ITA_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					#news_event = { id = unification.163 }
					news_event = { id = unification_generic.1 }
				}
			}
		}

		else_if = {
			limit = { original_tag = WAT }
			set_cosmetic_tag = WAT_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.213 }
				}
			}
		}		

		else_if = {
			limit = { original_tag = LEG }
			set_cosmetic_tag = LEG_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					#news_event = { id = unification.160 }
					news_event = { id = unification_generic.1 }
				}
			}
		}		
		
		else_if = {
			limit = { original_tag = JEH }
			set_cosmetic_tag = JEH_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					#news_event = { id = unification.165 }
					news_event = { id = unification_generic.1 }
				}
			}
		}
		
		else_if = {
			limit = { original_tag = KCZ }
			set_cosmetic_tag = KCZ_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					#news_event = { id = unification.161 }
					news_event = { id = unification_generic.1 }
				}
			}
		}	

		else_if = {
			limit = { original_tag = KOD }
			set_cosmetic_tag = KOD_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					#news_event = { id = unification.159 }
					news_event = { id = unification_generic.1 }
				}
			}
		}
		
		else_if = {
			limit = { original_tag = URS
			NOT = { has_cosmetic_tag = URS_warsaw_right } 
			}
			set_cosmetic_tag = URS_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.162 }
				}
			}
		}	
		else_if = {
			limit = { 
				original_tag = URS
				has_cosmetic_tag = URS_warsaw_right
				}				
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification_generic.1 }
				}
			}
			
		}
		
		
		else_if = {
			limit = { original_tag = WRS }
			set_cosmetic_tag = WRS_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.168 }
				}
			}
		}
		
		else_if = {
			limit = { original_tag = WRC }
			set_cosmetic_tag = WRC_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					#news_event = { id = unification.202 }
					news_event = { id = unification_generic.1 }
				}
			}
		}
		
		else_if = {
			limit = { original_tag = ZNP }
			set_cosmetic_tag = ZNP_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.204 }
				}
			}
		}		
		
	}

	ai_will_do = { factor = 1 }
}

holy_cross_voivodeship_unification = {

	icon = GFX_decision_santacruz
	
	available = {
		has_war = no
		custom_trigger_tooltip = {
			tooltip = controls_holy_cross_voivodeship
			can_unify_holy_cross = yes
		}
		NOT = { is_subject = yes }
	}
	
	fire_only_once = yes

	cost = 0
	
	visible = { 
		is_holy_cross = yes
		NOT = {
			has_global_flag = swietokrzyskie_unified
		}
		
	}

	complete_effect = {
		log = "[GetDateText]: Decision holy_cross_voivodeship_unification"
		set_country_flag = unified_voivodeship
		set_global_flag = swietokrzyskie_unified
		add_research_slot = 1
		set_technology = {
			public_services = 1
		}
		hidden_effect = {
			add_dynamic_modifier = {
				modifier = developed_economy_modifier
			}
			add_ideas = administrative_strain
		}	
		if = {
			limit = { original_tag = KEL }
			set_cosmetic_tag = KEL_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.121 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = NRA }
			set_cosmetic_tag = NRA_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.122 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = NSZ }
			set_cosmetic_tag = NSZ_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.123 }
				}
			}
			
		}	

		else_if = {
			limit = { original_tag = SND }
			set_cosmetic_tag = SND_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.124 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = KIE }
			set_cosmetic_tag = KIE_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.125 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = STO }
			set_cosmetic_tag = STO_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.126 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = STA }
			set_cosmetic_tag = STA_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					#news_event = { id = unification.127 }
					news_event = { id = unification_generic.1 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = ETS }
			set_cosmetic_tag = ETS_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification_alt.127 }
				}
			}
			
		}		

	}
	

	ai_will_do = { factor = 1 }
}

subcarpathia_voivodeship_unification = {

	icon = GFX_decision_podkarpacie
	
	available = {
		has_war = no
		custom_trigger_tooltip = {
			tooltip = controls_subcarpathia_voivodeship
			can_unify_subcarpathia = yes
		}
		NOT = { is_subject = yes }
	}
	
	fire_only_once = yes

	cost = 0
	
	visible = { 
		is_subcarpathia = yes
		NOT = {
			has_global_flag = podkarpackie_unified
		}
	}

	complete_effect = {
		log = "[GetDateText]: Decision subcarpathia_voivodeship_unification"
		set_country_flag = unified_voivodeship
		set_global_flag = podkarpackie_unified
		add_research_slot = 1
		set_technology = {
			public_services = 1
		}
		hidden_effect = {
			add_dynamic_modifier = {
				modifier = developed_economy_modifier
			}
			add_ideas = administrative_strain
		}	
		if = {
			limit = { original_tag = RZE }
			set_cosmetic_tag = RZE_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.128 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = CHR }
			set_cosmetic_tag = CHR_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.129 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = UPA }
			set_cosmetic_tag = UPA_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.130 }
				}
			}
			
		}	

		else_if = {
			limit = { original_tag = KOZ }
			set_cosmetic_tag = KOZ_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.131 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = BSC }
			set_cosmetic_tag = BSC_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.132 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = KRO }
			set_cosmetic_tag = KRO_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.133 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = TAR }
			set_cosmetic_tag = TAR_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.134 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = LGP }
			set_cosmetic_tag = LGP_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.135 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = LEZ }
			set_cosmetic_tag = LEZ_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.186 }
				}
			}
			
		}	
		
		else_if = {
			limit = { original_tag = UHR }
			set_cosmetic_tag = UHR_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.201 }
				}
			}
			
		}			

	}
	

	ai_will_do = { factor = 1 }
}

lesser_poland_voivodeship_unification = {

	icon = GFX_decision_malopolska
	
	available = {
		has_war = no
		custom_trigger_tooltip = {
			tooltip = controls_lesser_poland_voivodeship
			can_unify_lesser_poland = yes
		}
		NOT = { is_subject = yes }
		if = {
			limit = { 
					original_tag = HOR
			}
			OR = {
				AND = {
					has_country_flag = MPOL_war_in_heaven
					MSL = { exists = no }
				}
				MSL = { is_subject_of = HOR }
			}
		}
		if = {
			limit = { 
					original_tag = MSL
			}
			AND = {
				has_country_flag = MPOL_war_in_heaven
				HOR = { exists = no }
			}
		}		
	}
	
	fire_only_once = yes

	cost = 0
	
	visible = {
		is_lesser_poland = yes
		NOT = {
			has_global_flag = malopolskie_unified
		}
	}

	complete_effect = {
		log = "[GetDateText]: Decision lesser_poland_voivodeship_unification"
		set_country_flag = unified_voivodeship
		set_global_flag = malopolskie_unified
		add_research_slot = 1
		set_technology = {
			public_services = 1
		}
		hidden_effect = {
			add_dynamic_modifier = {
				modifier = developed_economy_modifier
			}
			add_ideas = administrative_strain
		}
		
		
		if = {
			limit = { original_tag = KRK }
			set_cosmetic_tag = KRK_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.136 }
				}
			}
		}

		else_if = {
			limit = { original_tag = PRK }
			set_cosmetic_tag = PRK_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.62 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = NHT }
			set_cosmetic_tag = NHT_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.137 }
				}
			}
			
		}	

		else_if = {
			limit = { original_tag = WDW }
			set_cosmetic_tag = WDW_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.138 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = TRN }
			set_cosmetic_tag = TRN_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.139 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = OSW }
			set_cosmetic_tag = OSW_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.140 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = CHW }
			set_cosmetic_tag = CHW_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.180 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = UMN }
			set_cosmetic_tag = UMN_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.141 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
				AND = {
					original_tag = HOR
					has_government = autocracy
				}
			}			
			set_cosmetic_tag = HOR_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.142 }
				}
				HOR = {
					clr_country_flag = MPOL_war_in_heaven
				}
			}
			
		}

		else_if = {
			limit = { 
				AND = {
					original_tag = HOR
					is_in_faction = no
					has_government = postpolonism
				}		
			}
			set_cosmetic_tag = HOR_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification_generic.1 }
				}
				HOR = {
					clr_country_flag = MPOL_war_in_heaven
				}
			}
			
		}		
		
		else_if = {
			limit = { original_tag = KRA }
			set_cosmetic_tag = KRA_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.143 }
				}
			}
			
		}

		else_if = {
			limit = { 
				original_tag = MSL
				is_in_faction = no
			}
			set_cosmetic_tag = MSL_voivodeship
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.41 }
				}
				MSL = {
					clr_country_flag = MPOL_war_in_heaven
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = BRK }
			set_cosmetic_tag = BRK_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.144 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = WND }
			set_cosmetic_tag = WND_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.145 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = HAB }
			set_cosmetic_tag = HAB_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.146 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = AGH }
			set_cosmetic_tag = AGH_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.147 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = UJG }
			set_cosmetic_tag = UJG_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.148 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = BRS }
			set_cosmetic_tag = BRS_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.179 }
					
				}
			}
			
		}

		else_if = {
			limit = { original_tag = NPL }
			set_cosmetic_tag = NPL_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.176 }
				}
					
			}
		}
				
		else_if = {
			limit = { original_tag = OSP }
			set_cosmetic_tag = OSP_voivodeship

			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = unification.190 }
					
				}
			}
		}
	}
	

	ai_will_do = { factor = 1 }
}


}
