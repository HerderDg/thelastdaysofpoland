SWI_unification_goal_category = {
    visible = {
		OR = {
			has_war_with = STE
			has_war_with = JOM
			has_war_with = USP
			has_war_with = EKW
			has_war_with = JAS	
		}
		NOT = {
			controls_state = 68
		}
    }
    priority = 100
    visible_when_empty = yes
}














