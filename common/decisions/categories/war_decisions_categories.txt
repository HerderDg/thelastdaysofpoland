ai_war_decisions = {
	priority = 100
	icon = GFX_decision_category_usa_intervention_mandate
	
	allowed = {
		is_somsiad = no
		NOT = { original_tag = STE }
		NOT = { original_tag = TOR }
		NOT = { original_tag = DGR }
		NOT = { original_tag = JOM }
		NOT = { original_tag = SND }
		NOT = { original_tag = ATM }
		NOT = { original_tag = BBW }
		NOT = { original_tag = ZYD }
		NOT = { original_tag = KRS }
		NOT = { original_tag = ZHP }
		NOT = { original_tag = SAP }
		NOT = {
			has_global_flag = postsmog_active
		}
	}
	visible = {
		ROOT  =	{	
			NOT = {
				has_country_flag = unified_voivodeship
			}
		}
	}	
}

regional_conquest_category = {
	priority = 50
	icon = GFX_category_icon
	allowed = {
		is_somsiad = no
		NOT = {
			has_global_flag = postsmog_active
		}
	}
	visible = {
		has_country_flag = unified_voivodeship
	}
}

final_conquest_category = {
	priority = 50
	icon = GFX_category_icon
	allowed = {
		is_somsiad = no
		NOT = {
			has_global_flag = postsmog_active
		}
	}
	visible = {
		has_country_flag = unified_supregional
		NOT = { has_country_flag = poland_mountain }
	}
}

