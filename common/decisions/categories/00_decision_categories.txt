 coring_category = {
	 priority = 2
	 icon = GFX_category_icon
	
	 allowed = {
		 always = yes
	 }

	 scripted_gui = coring_ui
 	on_map_area = {
 		target_array = core_states
 		target_trigger = {
 			any_owned_state = {
 				NOT = { is_core_of = ROOT }
 			}
 		}
 		name = current_cores
 		zoom = 1000
	}
}