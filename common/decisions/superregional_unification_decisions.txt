
unification_category = {

	central_poland_unification = {
		icon = GFX_decision_centralpol

		available = {
			custom_trigger_tooltip = {
				tooltip = controls_central_poland
				can_unify_central_poland = yes
			}
		if = {
			limit = {
				ROOT = { is_in_faction = yes }
			}
			is_faction_leader = yes			
		}
		}

		visible = {
			is_central_poland_unifier  = yes
			NOT = {
				has_global_flag = central_poland_unified
			}
			has_country_flag = unified_regional
			has_global_flag = story_unification
			NOT = {
				original_tag = ATM
				AND = {
					original_tag = BBW
					is_ai = no
				}
				has_global_flag = BBW_is_player
			}
		}

		fire_only_once = yes

		cost = 0

		complete_effect = {
			log = "[GetDateText]: Decision central_poland_unification"
			set_country_flag = unified_supregional
			set_global_flag = central_poland_unified
			superregional_unification_effect = yes
		}
		ai_will_do = { factor = 1 }
	}

	galicia_unification = {

		icon = GFX_decision_eastpol
		
		available = {
			custom_trigger_tooltip = {
				tooltip = controls_galicia
				can_unify_galicia = yes
			}
		if = {
			limit = {
				ROOT = { is_in_faction = yes }
			}
			is_faction_leader = yes			
		}
		}
		
		fire_only_once = yes

		cost = 0

		visible = { 
			is_galicia_unifier = yes
			NOT = {
				has_global_flag = galicia_unified
			}
			has_country_flag = unified_regional
			has_global_flag = story_unification
		}

		complete_effect = {
			log = "[GetDateText]: Decision galicia_unification"
			set_country_flag = unified_supregional
			set_global_flag = galicia_unified
			superregional_unification_effect = yes
		}
		ai_will_do = { factor = 1 }
	}

	silesia_unification = {

		icon = GFX_decision_gigaslask
		
		available = {
			custom_trigger_tooltip = {
				tooltip = controls_silesia
				can_unify_silesia = yes
			}
		if = {
			limit = {
				ROOT = { is_in_faction = yes }
			}
			is_faction_leader = yes			
		}
		}
		
		fire_only_once = yes

		cost = 0
		
		visible = { 
			is_silesia_unifier = yes
			NOT = {
				has_global_flag = silesia_unified
			}			
			has_country_flag = unified_regional
			has_global_flag = story_unification
			OR = {
				has_global_flag = BBW_is_player
				BBW = {
					is_ai = no
				}
			}
		}
			

		complete_effect = {
			log = "[GetDateText]: Decision silesia_supregional_unification"
			set_country_flag = unified_supregional
			set_global_flag = silesia_unified
			superregional_unification_effect = yes
		}
		ai_will_do = { factor = 1 }
	}

	capital_region_unification = {
		icon = GFX_decision_kujmazdc
		
		available = {
			custom_trigger_tooltip = {
				tooltip = controls_capital_region
				can_unify_capital_region = yes
			}	
		if = {
			limit = {
				ROOT = { is_in_faction = yes }
			}
			is_faction_leader = yes			
		}	
		}
		
		fire_only_once = yes

		cost = 0
		
		visible = { 
			is_capital_region_unifier = yes
			NOT = {
				has_global_flag = capital_region_unified
			}
			OR = {
				AND = {
					has_country_flag = unified_regional
					is_capital_district = no
				}
				AND = {
					has_country_flag = unified_voivodeship
					is_capital_district = yes
				}				
			}
			NOT = {
				OR = {
					has_global_flag = BBW_is_player
					BBW = {
						is_ai = no
					}
				}
			}
			has_global_flag = story_unification
		}
			

		complete_effect = {
			log = "[GetDateText]: Decision capital_region_supregional_unification"
			set_country_flag = unified_supregional
			set_global_flag = capital_region_unified
			superregional_unification_effect = yes
		}
		ai_will_do = { factor = 1 }
	}

	northwest_unification = {
		icon = GFX_decision_westpol
		
		available = {
			custom_trigger_tooltip = {
				tooltip = controls_northwest
				can_unify_northwest = yes
			}	
		if = {
			limit = {
				ROOT = { is_in_faction = yes }
			}
			is_faction_leader = yes			
		}
		}
		
		fire_only_once = yes

		cost = 0
		
		visible = { 
			is_northwest_unifier = yes
			NOT = {
				has_global_flag = northwest_unified
			}
			NOT = {
				OR = {
					has_global_flag = BBW_is_player
					BBW = {
						is_ai = no
					}
				}
			}
			has_country_flag = unified_regional
			has_global_flag = story_unification
		}
			

		complete_effect = {
			log = "[GetDateText]: Decision northwest_unification"
			set_country_flag = unified_supregional
			set_global_flag = northwest_unified
			superregional_unification_effect = yes
		}
		ai_will_do = { factor = 1 }
	}



	northeast_unification = {
		icon = GFX_decision_zadupie
		
		available = {
			custom_trigger_tooltip = {
				tooltip = controls_northeast
				can_unify_northeast = yes
			}
		if = {
			limit = {
				ROOT = { is_in_faction = yes }
			}
			is_faction_leader = yes			
		}
		}
		
		fire_only_once = yes

		cost = 0
		
		visible = { 
			is_northeast_unifier = yes
			NOT = {
				has_global_flag = northeast_unified
			}
			NOT = {
				OR = {
					has_global_flag = BBW_is_player
					BBW = {
						is_ai = no
					}
				}
			}
			has_country_flag = unified_regional
			has_global_flag = story_unification
		}
			

		complete_effect = {
			log = "[GetDateText]: Decision northeast_unification"
			set_country_flag = unified_supregional
			set_global_flag = northeast_unified
			superregional_unification_effect = yes
		}
		ai_will_do = { factor = 1 }
	}

	superregional_unification_decision = {
		icon = GFX_decision_icon_default
		
		available = {
			controls_six_voivodeships = yes
		if = {
			limit = {
				ROOT = { is_in_faction = yes }
			}
			is_faction_leader = yes			
		}
		}
		
		fire_only_once = yes

		cost = 0
		
		visible = { 
			has_country_flag = unified_regional
			OR = {
				has_global_flag = ffa_unification
				original_tag = ATM
			}
		}
			

		complete_effect = {
			log = "[GetDateText]: Decision northeast_unification"
			set_country_flag = unified_supregional
			superregional_unification_effect = yes
		}
		ai_will_do = { factor = 1 }
	}

	realize_miner_dream = {
		icon = GFX_decision_icon_default
		
		available = {
			custom_trigger_tooltip = {
				tooltip = controls_lodz_jura_and_silesia
				#controls_lodz = yes
				#controls_zaglebie = yes
				controls_opolskie = yes
				controls_upper_silesia = yes
			}
		}
		
		fire_only_once = yes

		cost = 0
		
		visible = { 
			has_country_flag = unified_regional
			has_global_flag = story_unification
			original_tag = BBW
		}
			

		complete_effect = {
			log = "[GetDateText]: Decision northeast_unification"
			set_country_flag = unified_supregional
			superregional_unification_effect = yes
		}
		ai_will_do = { factor = 1 }
	}

	north_unification = {
		icon = GFX_decision_westpol
		
		available = {
			custom_trigger_tooltip = {
				tooltip = "Took over North Poland"
				can_unify_west_pomerania = yes
				can_unify_pomerelia = yes
				can_unify_warmia_and_masuria = yes 
				controls_state = 48
			}	
		}
		
		fire_only_once = yes

		cost = 0
		
		visible = { 
			OR = {
				is_warmia_and_masuria_unifier = yes
				is_pomerelia_unifier = yes
			}
			NOT = {
				has_global_flag = north_unified
			}
			OR = {
				has_global_flag = BBW_is_player
				BBW = {
					is_ai = no
				}
			}
			
			has_country_flag = unified_regional
			has_global_flag = story_unification
		}
			

		complete_effect = {
			log = "[GetDateText]: Decision northwest_unification"
			set_country_flag = unified_supregional
			set_global_flag = north_unified
			superregional_unification_effect = yes
		}
		ai_will_do = { factor = 1 }
	}

	kuyavia_podlachia_belt_unification = {
		icon = GFX_decision_westpol
		
		available = {
			custom_trigger_tooltip = {
				tooltip = "Took over Kuyavia-Podlachia belt"
				can_unify_kuyavia = yes
				can_unify_mazovia = yes
				can_unify_podlasie = yes
			}	
		}
		
		fire_only_once = yes

		cost = 0
		
		visible = { 
			OR = {
				is_kuyavia_mazovia_unifier = yes
				is_podlasie_unifier = yes
			}
			NOT = {
				has_global_flag = kuyavia_podlachia_belt_unified
			}
			OR = {
				has_global_flag = BBW_is_player
				BBW = {
					is_ai = no
				}
			}
			if = {
				limit = {
					is_kuyavia_mazovia_unifier = yes
				}
				has_country_flag = unified_regional
			}
			else = {
				has_country_flag = unified_voivodeship
			}
			has_global_flag = story_unification
		}
			

		complete_effect = {
			log = "[GetDateText]: Decision northwest_unification"
			set_country_flag = unified_supregional
			set_global_flag = kuyavia_podlachia_belt_unified
			superregional_unification_effect = yes
		}
		ai_will_do = { factor = 1 }
	}

	warsaw_kielce_unification = {
		icon = GFX_decision_westpol
		
		available = {
			custom_trigger_tooltip = {
				tooltip = "Took over warsaw-kielce region"
				can_unify_capital_district = yes
				can_unify_radom_province = yes
				can_unify_holy_cross = yes
			}	
		}
		
		fire_only_once = yes

		cost = 0
		
		visible = { 
			OR = {
				is_capital_district_unifier = yes
				is_old_polish_basin_unifier = yes
			}
			NOT = {
				has_global_flag = warsaw_kielce_unified
			}
			OR = {
				has_global_flag = BBW_is_player
				BBW = {
					is_ai = no
				}
			}
			if = {
				limit = {
					is_old_polish_basin_unifier = yes
				}
				has_country_flag = unified_regional
			}
			else = {
				has_country_flag = unified_voivodeship
			}
			has_global_flag = story_unification
		}
			

		complete_effect = {
			log = "[GetDateText]: Decision northwest_unification"
			set_country_flag = unified_supregional
			set_global_flag = warsaw_kielce_unified
			superregional_unification_effect = yes
		}
		ai_will_do = { factor = 1 }
	}

	southwest_unification = {
		icon = GFX_decision_westpol
		
		available = {
			custom_trigger_tooltip = {
				tooltip = "Took over Southwest"
				can_unify_kuyavia = yes
				can_unify_mazovia = yes
				can_unify_podlasie = yes
			}	
		}
		
		fire_only_once = yes

		cost = 0
		
		visible = { 
			OR = {
				is_greater_lower_silesia_unifier = yes
				is_greater_poland_unifier = yes
			}
			NOT = {
				has_global_flag = southwest_unified
			}
			OR = {
				has_global_flag = BBW_is_player
				BBW = {
					is_ai = no
				}
			}
			has_country_flag = unified_regional
			has_global_flag = story_unification
		}
			

		complete_effect = {
			log = "[GetDateText]: Decision northwest_unification"
			set_country_flag = unified_supregional
			set_global_flag = southwest_unified
			superregional_unification_effect = yes
		}
		ai_will_do = { factor = 1 }
	}
}
