ai_war_decisions = {

	
#██████╗░███████╗░██████╗██╗░░██╗██╗██████╗░███████╗██╗░░██╗██╗███████╗
#██╔══██╗██╔════╝██╔════╝██║░██╔╝██║██╔══██╗╚════██║██║░██╔╝██║██╔════╝
#██████╦╝█████╗░░╚█████╗░█████═╝░██║██║░░██║░░███╔═╝█████═╝░██║█████╗░░
#██╔══██╗██╔══╝░░░╚═══██╗██╔═██╗░██║██║░░██║██╔══╝░░██╔═██╗░██║██╔══╝░░
#██████╦╝███████╗██████╔╝██║░╚██╗██║██████╔╝███████╗██║░╚██╗██║███████╗
#╚═════╝░╚══════╝╚═════╝░╚═╝░░╚═╝╚═╝╚═════╝░╚══════╝╚═╝░░╚═╝╚═╝╚══════╝
#BESKIDZKIE - FAZA PIERWSZA

CARPAT_01_GORCE = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = SCZ
			original_tag = LEM
			original_tag = GUR
			original_tag = BSK
		}
	}
	target_root_trigger = {
		OR = {
			original_tag = SCZ
			original_tag = LEM
			original_tag = GUR
			original_tag = BSK
		}		
	}
	target_trigger = {
		if = {
			limit = {
				ROOT = { original_tag = GUR }
			}
		ROOT = { 
			has_war = no
			date > 2027.10.11
			ZGR = { exists = yes }
		}			
		}
		if = {
			limit = {
				OR = {
					ROOT = { original_tag = SCZ }
					ROOT = { original_tag = LEM }
					ROOT = { original_tag = BSK }
				}
			}
		ROOT = { 
			has_war = no
			date > 2029.02.11
			ZGR = { exists = yes }	
			ZGR = { is_neighbor_of = ROOT }		
			}	
		}

	}
	targets = {
		ZGR
	}

	war_with_target_on_complete = yes
	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision GAL_01_SALT_HYPERWAR"
		declare_war_on = {
			target = ZGR
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

CARPAT_01_LEMKO = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = SCZ
			original_tag = LEM
			original_tag = GUR
			original_tag = BSK
		}
	}

	targets = {
		LEM
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}
	war_with_target_on_complete = yes
	target_root_trigger = {
		if = {
			limit = {
				original_tag = SCZ
			}
		has_war = no
		date > 2028.02.14
		LEM = { exists = yes }			
		}
		if = {
			limit = {
				original_tag = LEM
			}
		has_war = no
		date > 2028.02.14
		SCZ = { exists = yes }			
		}		
		if = {
			limit = {
				OR = {
					original_tag = GUR
					original_tag = LEM
					original_tag = BSK
				}
			}
		has_war = no
		date > 2029.02.11
		LEM = { exists = yes }	
		LEM = { is_neighbor_of = ROOT }		
		}
	}

	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision GAL_01_SALT_HYPERWAR"
		if = { 
			limit = { NOT = { original_tag = LEM }}
		declare_war_on = {
			target = LEM
			type = voivodeship_war
		}
		}
		if = { 
			limit = { original_tag = LEM
			SCZ = { exists = yes }	
		}
		declare_war_on = {
			target = SCZ
			type = voivodeship_war
		}
		}		
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
} 


#BESKIDZKIE - FAZA DRUGA

CARPAT_02_EAST = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = SCZ
			original_tag = LEM
			original_tag = GUR
		}
	}

	available = {
		if = {
			limit = {
				original_tag = SCZ
			}
		has_war = no
		date > 2028.10.14
		LEM = { exists = no }			
		}
		if = {
			limit = {
				original_tag = LEM
			}
		has_war = no
		date > 2028.10.14
		SCZ = { exists = no }			
		}		
		if = {
			limit = {
				original_tag = GUR
			}
		has_war = no
		date > 2028.10.14
		ZGR = { exists = no }			
		}
	}

	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision CARPAT_02_EAST"
		if = { 
			limit = { NOT = { original_tag = LEM }
			LEM = {exists=yes}
		}
		declare_war_on = {
			target = LEM
			type = voivodeship_war
		}
		}
		if = { 
			limit = { NOT = { original_tag = SCZ }
			SCZ = {exists=yes}
		}
		declare_war_on = {
			target = SCZ
			type = voivodeship_war
		}
		}
		if = { 
			limit = { NOT = { original_tag = GUR }
			GUR = {exists=yes}
		}
		declare_war_on = {
			target = GUR
			type = voivodeship_war
		}
		}						
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
} 

 
CARPAT_02_WEST = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		original_tag = BSK
	}

	available = {
		has_war = no
		date > 2029.11.14
		CIE = { exists = no }
	}

	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision CARPAT_02_WEST"
		if = { 
			limit = { NOT = { original_tag = LEM }
			LEM = {exists=yes}
		}
		declare_war_on = {
			target = LEM
			type = voivodeship_war
		}
		}
		if = { 
			limit = { NOT = { original_tag = TYM }
			TYM = {exists=yes}
		}
		declare_war_on = {
			target = TYM
			type = voivodeship_war
		}
		}
		if = { 
			limit = { NOT = { original_tag = ZGR }
			ZGR = {exists=yes}
		}
		declare_war_on = {
			target = ZGR
			type = voivodeship_war
		}
		}				
		if = { 
			limit = { NOT = { original_tag = SCZ }
			SCZ = {exists=yes}
		}
		declare_war_on = {
			target = SCZ
			type = voivodeship_war
		}
		}
		if = { 
			limit = { NOT = { original_tag = GUR }
			GUR = {exists=yes}
		}
		declare_war_on = {
			target = GUR
			type = voivodeship_war
		}
		}						
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}


CARPAT_02_EAST_V_WEST = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = SCZ
			original_tag = LEM
			original_tag = GUR
		}
	}

	available = {
		if = {
			limit = {
				original_tag = SCZ
			}
		has_war = no
		date > 2029.05.14
		LEM = { exists = no }			
		GUR = { exists = no }			
		}
		if = {
			limit = {
				original_tag = LEM
			}
		has_war = no
		date > 2029.05.14
		SCZ = { exists = no }			
		GUR = { exists = no }			
		}		
		if = {
			limit = {
				original_tag = GUR
			}
		has_war = no
		date > 2029.05.14
		SCZ = { exists = no }			
		LEM = { exists = no }			
		}
	}

	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision CARPAT_02_EAST"
		if = { 
			limit = { NOT = { original_tag = BSK }
			BSK = {exists=yes}
		}
		declare_war_on = {
			target = BSK
			type = voivodeship_war
		}
		}
		if = { 
			limit = { NOT = { original_tag = WYM }
			WYM = {exists=yes}
		}
		declare_war_on = {
			target = WYM
			type = voivodeship_war
		}
		}
		if = { 
			limit = { NOT = { original_tag = BBA }
			BBA = {exists=yes}
		}
		declare_war_on = {
			target = BBA
			type = voivodeship_war
		}
		}
		if = { 
			limit = { NOT = { original_tag = CIE }
			CIE = {exists=yes}
		}
		declare_war_on = {
			target = CIE
			type = voivodeship_war
		}
		}								
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
} 

#BESKIDZKIE - FAZA TRZECIA

CARPAT_03_TYMBARK = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = LEM
			original_tag = GUR
			original_tag = BSK
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2029.02.11
	}
	targets = {
		TYM
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}
	war_with_target_on_complete = yes
	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision GAL_01_SALT_HYPERWAR"
		declare_war_on = {
			target = TYM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

CARPAT_03_FINISH_HIM = {
	allowed = {
		is_tatry_region = yes
		NOT = {
			original_tag = BBA
			original_tag = WYM
		}
	}
	icon = GFX_decision_generic_prepare_civil_war
	target_root_trigger = {
		count_triggers = {
			amount = 7
			LEM = { exists = no }
			SCZ = { exists = no }
			TYM = { exists = no }
			ZGR = { exists = no }
			GUR = { exists = no }
			ZKP = { exists = no }
			CIE = { exists = no }
			BSK = { exists = no }
		}
		OR = {
			BBA = {exists = yes}
			WYM = {exists = yes}
		}		
	}

	targets = {
		BBA
	}
	war_with_target_on_complete = yes
	days_remove = 7
	ai_will_do = {
		base = 1000
	}
	remove_effect = {
		if = {
			limit = {
				country_exists = BBA
				BBA = { is_subject_of = ROOT }
			}
		annex_country = {
			target = BBA
			transfer_troops = yes
		}
		}
		if = {
			limit = {
				country_exists = BBA
				BBA = { NOT = { is_subject_of = ROOT } }
			}
			declare_war_on = {
				target = BBA
				type = voivodeship_war
			}
		}
		if = {
			limit = {
				country_exists = WYM
				WYM = { is_subject_of = ROOT }
			}
		annex_country = {
			target = WYM
			transfer_troops = yes
		}
		}
		if = {
			limit = {
				country_exists = WYM
				WYM = { NOT = { is_subject_of = ROOT } }
			}
			declare_war_on = {
				target = WYM
				type = voivodeship_war
			}
		}				
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}


#███████╗░█████╗░░██████╗░██╗░░░░░███████╗██████╗░██╗███████╗  ██╗ ░░░░░██╗██╗░░░██╗██████╗░░█████╗░
#╚════██║██╔══██╗██╔════╝░██║░░░░░██╔════╝██╔══██╗██║██╔════╝  ██║ ░░░░░██║██║░░░██║██╔══██╗██╔══██╗
#░░███╔═╝███████║██║░░██╗░██║░░░░░█████╗░░██████╦╝██║█████╗░░  ██║ ░░░░░██║██║░░░██║██████╔╝███████║
#██╔══╝░░██╔══██║██║░░╚██╗██║░░░░░██╔══╝░░██╔══██╗██║██╔══╝░░  ██║ ██╗░░██║██║░░░██║██╔══██╗██╔══██║
#███████╗██║░░██║╚██████╔╝███████╗███████╗██████╦╝██║███████╗  ██║ ╚█████╔╝╚██████╔╝██║░░██║██║░░██║
#╚══════╝╚═╝░░╚═╝░╚═════╝░╚══════╝╚══════╝╚═════╝░╚═╝╚══════╝  ╚═╝ ░╚════╝░░╚═════╝░╚═╝░░╚═╝╚═╝░░╚═╝
#ZAGŁĘBIE I JURA - FAZA PIERWSZA


 ZAGJUR_01_JWK = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = JUR
			original_tag = CCH
		}		
	}

	target_root_trigger = {
		OR = {
			AND = { 
			original_tag = JUR
			JUR = { NOT = { has_cosmetic_tag = JUR_intercounty }}
			}
			AND = { 
			original_tag = CCH
			CCH = { NOT = { has_cosmetic_tag = CCH_intercounty }}
			}
		}
		OR = {
			JUR = { exists = no }
			CCH = { exists = no }
		}			
		NOT = { 
			has_idea = cooldown
		}
		ROOT = { any_neighbor_country = { original_tag = JWK } }
		has_war = no
		date > 2028.02.14
	}

	targets = {
		JWK
	}
	war_with_target_on_complete = yes
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}	

	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	war_with_on_remove = JWK
	complete_effect = {
		add_timed_idea = {
			idea = cooldown
			days = 15
		}
	}
	remove_effect  = {
		log = "[GetDateText]: Decision ZAGJUR_01_JWK"
			if = { limit = { country_exists = JWK }
			declare_war_on = {
				target = JWK
				type = voivodeship_war
			}
			}
	}

 	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
 	}
 }

ZAGJUR_01_JUR = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = CCH
			original_tag = JWK
		}
	}

	target_root_trigger = {
		OR = {
			AND = {
				original_tag = JWK
				BRZ = { exists = no }
				JWK = { NOT = { has_cosmetic_tag = JWK_intercounty }}
			}
			AND = { 
			original_tag = CCH
			CCH = { NOT = { has_cosmetic_tag = CCH_intercounty }}
			}
		}
		NOT = { 
			has_idea = cooldown
		}
		ROOT = { any_neighbor_country = { original_tag = JUR } }
		has_war = no
		date > 2028.02.14
	}

	targets = {
		JUR
	}
	war_with_target_on_complete = yes
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}	

	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	war_with_on_remove = JUR
	complete_effect = {
		add_timed_idea = {
			idea = cooldown
			days = 15
		}
	}
	remove_effect  = {
		log = "[GetDateText]: Decision jura_ZAW"
			if = { limit = { country_exists = JUR }
			declare_war_on = {
				target = JUR
				type = voivodeship_war
			}
			}
	}

 	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
 	}
}

 ZAGJUR_01_BRZ = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = JWK
			original_tag = CCH
			original_tag = JUR
		}
	}

	target_root_trigger = {
		OR = {
			AND = { 
			original_tag = JWK
			JWK = { NOT = { has_cosmetic_tag = JWK_intercounty }}
			}
			AND = { 
				original_tag = JUR
				CCH = { exists = no }
				JUR = { NOT = { has_cosmetic_tag = JUR_intercounty }}
			}
			AND = { 
				original_tag = CCH
				JUR = { exists = no }
				CCH = { NOT = { has_cosmetic_tag = CCH_intercounty }}
			}
		}
		NOT = { 
			has_idea = cooldown
		}
		ROOT = { any_neighbor_country = { original_tag = BRZ } }
		has_war = no
		NOT = { has_war_with = BRZ }
		date > 2028.02.14
	}

	targets = {
		BRZ
	}
	war_with_target_on_complete = yes
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}	

	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	war_with_on_remove = BRZ
	complete_effect = {
		add_timed_idea = {
			idea = cooldown
			days = 15
		}
	}
	remove_effect  = {
		log = "[GetDateText]: Decision ZAGJUR_01_BRZ"
			if = { limit = { country_exists = BRZ }
			declare_war_on = {
				target = BRZ
				type = voivodeship_war
			}
			}
	}

 	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
 	}
 }


 ZAGJUR_01_CCH = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = JWK
			original_tag = JUR
		}
	}

	target_root_trigger = {
		OR = {
			AND = { 
				original_tag = JWK
				BRZ = { exists = no }
				JWK = { NOT = { has_cosmetic_tag = JWK_intercounty }}
			}
			AND = { 
				original_tag = JUR
				JUR = { NOT = { has_cosmetic_tag = JUR_intercounty }}
			}
		}
		NOT = { 
			has_idea = cooldown
		}
		ROOT = { any_neighbor_country = { original_tag = CCH } }
		has_war = no
		date > 2028.02.14
	}

	targets = {
		CCH
	}
	war_with_target_on_complete = yes
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}	

	ai_will_do = {
		factor = 100

	}

	days_remove = 7
	war_with_on_remove = CCH
	complete_effect = {
		add_timed_idea = {
			idea = cooldown
			days = 15
		}
	}
	remove_effect  = {
		log = "[GetDateText]: Decision ZAGJUR_01_CCH"
			if = { limit = { country_exists = CCH }
			declare_war_on = {
				target = CCH
				type = voivodeship_war
			}
			}
	}

 	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
 	}
 }
 ZAGJUR_01_SOS = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = JAW
		}		
	}

	target_root_trigger = {
		OR = {
			original_tag = JAW
		}
		NOT = { 
			has_idea = cooldown
		}
		ROOT = { any_neighbor_country = { original_tag = SOS } }
		has_war = no
		date > 2028.02.14
		NOT = { has_global_flag = ZAG_congress_of_bedzin_success }
		has_global_flag = ZAG_congress_of_bedzin_failure
		is_subject = no
	}

	targets = {
		SOS
	}
	war_with_target_on_complete = yes
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}		

	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	war_with_on_remove = SOS
	complete_effect = {
		add_timed_idea = {
			idea = cooldown
			days = 15
		}
	}
	remove_effect  = {
		log = "[GetDateText]: Decision ZAGJUR_01_SOS"
			if = { limit = { country_exists = SOS }
			declare_war_on = {
				target = SOS
				type = voivodeship_war
			}
			}
			if = { limit = { country_exists = BED
			BED = { is_subject_of = SOS }
			}
			declare_war_on = {
				target = BED
				type = voivodeship_war
			}
			}
			if = { limit = { country_exists = SIE
			SIE = { is_subject_of = SOS }
			}
			declare_war_on = {
				target = SIE
				type = voivodeship_war
			}
			}						
	}

 	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
 	}
 }
 ZAGJUR_01_DGR = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = JAW
			original_tag = SOS
		}
	}

	target_root_trigger = {
		OR = {
			original_tag = JAW
			original_tag = SOS
		}
		NOT = { 
			has_idea = cooldown
		}
		ROOT = { any_neighbor_country = { original_tag = DGR } }
		has_war = no
		date > 2028.02.14
		NOT = { has_global_flag = ZAG_congress_of_bedzin_success }
		has_global_flag = ZAG_congress_of_bedzin_failure
		is_subject = no
	}

	targets = {
		DGR
	}
	war_with_target_on_complete = yes
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}	

	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	war_with_on_remove = DGR
	complete_effect = {
		add_timed_idea = {
			idea = cooldown
			days = 15
		}
	}
	remove_effect  = {
		log = "[GetDateText]: Decision ZAGJUR_01_DGR"
			if = { limit = { country_exists = DGR }
			declare_war_on = {
				target = DGR
				type = voivodeship_war
			}
			}
			if = { limit = { country_exists = DGA }
			declare_war_on = {
				target = DGA
				type = voivodeship_war
			}
			}			
	}

 	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
 	}
 }

 ZAGJUR_01_JAW = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = SOS
		}
	}

	target_root_trigger = {
			OR = {
				original_tag = SOS
			}
			NOT = { 
				has_idea = cooldown
				}
			ROOT = { any_neighbor_country = { original_tag = JAW } }
			has_war = no
			date > 2028.02.14
			NOT = { has_global_flag = ZAG_congress_of_bedzin_success }
			has_global_flag = ZAG_congress_of_bedzin_failure
	}

	targets = {
		JAW
	}
	war_with_target_on_complete = yes
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}		

	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	war_with_on_remove = JAW
	complete_effect = {
		add_timed_idea = {
			idea = cooldown
			days = 15
		}
	}
	remove_effect  = {
		log = "[GetDateText]: Decision ZAGJUR_01_JAW"
			if = { limit = { country_exists = JAW }
			declare_war_on = {
				target = JAW
				type = voivodeship_war
			}
			}
	}

 	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
 	}
 }

ZAGJUR_01_ZAW = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = JAW
			original_tag = SOS
		}		
	}

	target_root_trigger = {
			OR = {
				original_tag = JAW
				original_tag = SOS
			}
			NOT = { 
				has_idea = cooldown
			}
			ROOT = { any_neighbor_country = { original_tag = ZAW } }
			has_war = no
			date > 2028.02.14
			ZAW = { NOT = { has_cosmetic_tag = ZAW_intercounty }}
			if = {
				limit = {
					has_global_flag = ZAG_congress_of_bedzin_success
				}
				any_allied_country = {
					is_neighbor_of = ZAW
				}
			}
			OR = {
			has_global_flag = ZAG_congress_of_bedzin_failure
			has_global_flag = ZAG_congress_of_bedzin_success
			}
			is_subject = no
	}

	targets = {
		ZAW
	}
	war_with_target_on_complete = yes
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}	

	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	war_with_on_remove = ZAW
	complete_effect = {
		add_timed_idea = {
			idea = cooldown
			days = 15
		}
	}
	remove_effect  = {
		log = "[GetDateText]: Decision ZAGJUR_01_ZAW"
			if = { limit = { country_exists = ZAW }
			declare_war_on = {
				target = ZAW
				type = voivodeship_war
			}
			}		
	}

 	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
 	}
 }

#ZAGŁĘBIE I JURA - FAZA DRUGA


 ZAGJUR_02_ZAGLEBIE = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = JWK
			original_tag = JUR
			original_tag = CCH
		}
	}

	target_root_trigger = {
		OR = {
			original_tag = JWK
			original_tag = JUR
			original_tag = CCH
		}

		count_triggers = {
		amount = 3
			BRZ = { exists = no }
			JWK = { exists = no }
			JUR = { exists = no }
			CCH = { exists = no }
		}
		ROOT = {
			NOT = {
				is_in_faction_with = SOS
			}
		}
	}

	targets = {
		DGR DGA BED SIE SOS JAW ZAW
	}
	war_with_target_on_complete = yes
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}	

	ai_will_do = {
		factor = 100

	}

	days_remove = 7
	remove_effect  = {	
		log = "[GetDateText]: Decision ZAGJUR_02_ZAGLEBIE"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}

 	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
 	}
 }

 ZAGJUR_02_JURA = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = SOS
			original_tag = JAW
			original_tag = ZAW
		}
	}

	target_root_trigger = {
		OR = {
			AND = {
			count_triggers = {
				amount = 3
					SOS = { exists = no }
					DGR = { exists = no }
					ZAW = { exists = no }
					JAW = { exists = no }
			}
			has_global_flag = ZAG_congress_of_bedzin_failure
			}
			AND = {
				has_global_flag = ZAG_congress_of_bedzin_success
				OR = {
					AND = {
					original_tag = ZAW
					NOT = { ZAW = { has_cosmetic_tag = ZAW_intercounty }}
					count_triggers = {
						amount = 3
							SOS = { exists = no }
							DGR = { exists = no }
							JAW = { exists = no }
					}
					}
					AND = {
						OR = {
							original_tag = SOS
							original_tag = JAW
						}
						OR = {
						ZAW = { exists = no }
						ZAW = { has_cosmetic_tag = ZAW_intercounty }
						}
					}										
				}
			}
		}
		is_subject = no
		
		has_war = no
	}

	targets = {
		JUR CCH JWK BRZ
	}	
	war_with_target_on_complete = yes

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	ai_will_do = {
		factor = 100

	}

	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision ZAGJUR_02_JURA"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}

 	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
 	}
 }
#ZAGŁĘBIE I JURA - FAZA OSTATECZNA

 ZAGJUR_03_PUPPETS = {
	allowed = {
		OR = {
			original_tag = SOS
			original_tag = JAW
			original_tag = ZAW
			original_tag = JUR
			original_tag = JWK
			original_tag = CCH
			original_tag = DGR
		}
	}
	icon = GFX_decision_generic_prepare_civil_war
	target_root_trigger = {
		OR = {
			AND = {
				has_global_flag = ZAG_congress_of_bedzin_failure
				count_triggers = {
					amount = 7
					DGR = { exists = no }
					SOS = { exists = no }
					JAW = { exists = no }
					ZAW = { exists = no }
					JUR = { exists = no }
					JWK = { exists = no }	
					CCH = { exists = no }
					BRZ = { exists = no }
				}
				OR = {
				SIE = { exists = yes }
				BED = { exists = yes }
				}
				OR = {
					SIE = { NOT = { has_cosmetic_tag = SIE_intercounty }}
					BED = { NOT = { has_cosmetic_tag = BED_intercounty }}
				}
			}
			AND = {
				has_global_flag = ZAG_congress_of_bedzin_success
				OR = {
					SIE = { NOT = { has_cosmetic_tag = SIE_intercounty }}
					BED = { NOT = { has_cosmetic_tag = BED_intercounty }}
				}
			}
		}
	}
	targets = {
		SIE BED DGA
	}
	war_with_target_on_complete = yes
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	days_remove = 7
	ai_will_do = {
		base = 1000
	}
	remove_effect = {
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}

 	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
 	}
 }


#░██████╗░░█████╗░██████╗░███╗░░██╗██╗░░░██╗  ░██████╗██╗░░░░░░█████╗░░██████╗██╗░░██╗
#██╔════╝░██╔══██╗██╔══██╗████╗░██║╚██╗░██╔╝  ██╔════╝██║░░░░░██╔══██╗██╔════╝██║░██╔╝
#██║░░██╗░██║░░██║██████╔╝██╔██╗██║░╚████╔╝░  ╚█████╗░██║░░░░░███████║╚█████╗░█████═╝░
#██║░░╚██╗██║░░██║██╔══██╗██║╚████║░░╚██╔╝░░  ░╚═══██╗██║░░░░░██╔══██║░╚═══██╗██╔═██╗░
#╚██████╔╝╚█████╔╝██║░░██║██║░╚███║░░░██║░░░  ██████╔╝███████╗██║░░██║██████╔╝██║░╚██╗
#░╚═════╝░░╚════╝░╚═╝░░╚═╝╚═╝░░╚══╝░░░╚═╝░░░  ╚═════╝░╚══════╝╚═╝░░╚═╝╚═════╝░╚═╝░░╚═╝
#GÓRNY ŚLĄSK - FAZA PIERWSZA
#GÓRNY ŚLĄSK - FAZA DRUGA
#GÓRNY ŚLĄSK - FAZA OSTATECZNA

GSIL_01_HANYS_WAR = { 
	icon = GFX_decision_generic_prepare_civil_war

	allowed = {
		is_upper_silesia = yes
	}
	ai_will_do = {
		factor = 100
	}

	target_root_trigger = {
		date > 2028.04.01
		has_war = no		
		has_global_flag = chosen_silesian_behemoth
		ROOT = { NOT = { has_idea = cooldown }}
		NOT = { is_upper_silesia_minor = yes }
	}

	targets = {
		ASG RAS CHO RUD FER GLI TRC KAT SBW SOL DUR TYS JSW WOD RBN MYS
	}

	target_trigger = {
		FROM = {
			NOT = {
				AND = {
					original_tag = MYS
					MYS = { has_country_flag = MYS_eurocrusade }
				}
			}
		}
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}

	}
	war_with_target_on_complete = yes
	days_remove = 7
	complete_effect = {
		ROOT = { add_timed_idea = {
			idea = cooldown
			days = 9
		}
	 }
	}
	remove_effect = {
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}

 	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
 	}

}


#███╗░░░███╗░█████╗░██╗░░░░░░█████╗░██████╗░░█████╗░██╗░░░░░░██████╗██╗░░██╗██╗███████╗
#████╗░████║██╔══██╗██║░░░░░██╔══██╗██╔══██╗██╔══██╗██║░░░░░██╔════╝██║░██╔╝██║██╔════╝
#██╔████╔██║███████║██║░░░░░██║░░██║██████╔╝██║░░██║██║░░░░░╚█████╗░█████═╝░██║█████╗░░
#██║╚██╔╝██║██╔══██║██║░░░░░██║░░██║██╔═══╝░██║░░██║██║░░░░░░╚═══██╗██╔═██╗░██║██╔══╝░░
#██║░╚═╝░██║██║░░██║███████╗╚█████╔╝██║░░░░░╚█████╔╝███████╗██████╔╝██║░╚██╗██║███████╗
#╚═╝░░░░░╚═╝╚═╝░░╚═╝╚══════╝░╚════╝░╚═╝░░░░░░╚════╝░╚══════╝╚═════╝░╚═╝░░╚═╝╚═╝╚══════╝
#MAŁOPOLSKIE - FAZA PIERWSZA

 MPOL_01_SALT_HYPERWAR = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = BOH
			original_tag = UMN
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2028.04.11
	}
	targets = {
		BOH UMN
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}
	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision GAL_01_SALT_HYPERWAR"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }

 MPOL_01_TOTAL_ABORDAGE = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {
				original_tag = BRS
				original_tag = SKW
			}
		}

		target_root_trigger = {
			has_war = no
			date > 2028.03.03
		}
		targets = {
			BRS SKW
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7
		remove_effect  = {
			log = "[GetDateText]: Decision MPOL_01_TOTAL_ABORDAGE"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}
		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
		}
 }


 MPOL_01_ELEVENTH_CRUSADE = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {
				original_tag = MSL
				original_tag = WDW
			}
		}

		target_root_trigger = {
			has_war = no
			NOT = { country_exists = HOR }
			date > 2028.05.03
		}
		targets = {
			MSL WDW
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7
		remove_effect  = {
			log = "[GetDateText]: Decision MPOL_01_TOTAL_ABORDAGE"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}
		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
		}
 }


 MPOL_01_AUSCHWITZ_KRENAU = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {
				original_tag = CHW
				original_tag = OSW
			}
		}

		target_root_trigger = {
			has_war = no
			date > 2028.05.03
		}

		targets = {
			CHW OSW
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}
		ai_will_do = {
			factor = 100
		}

		days_remove = 7
		remove_effect  = {
			log = "[GetDateText]: Decision MPOL_01_TOTAL_ABORDAGE"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}
		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
		}
 } 


 MPOL_01_FINAL_SOLUTION_OF_HUTA_PROBLEM = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {
				original_tag = BRK
				original_tag = KRK 
				original_tag = KRA
				original_tag = WND
				original_tag = UJG
				original_tag = AGH
				original_tag = HAB
			}
		}
		target_root_trigger = {
			count_triggers = {
				amount = 6
				KRK = { exists = no }
				KRA = { exists = no }
				BRK = { exists = no }
				WND = { exists = no }
				UJG = { exists = no }
				AGH = { exists = no }
				HAB = { exists = no }
			}
			NHT = { num_of_controlled_states > 1 }
			NOT = { has_war_with = NHT }
			has_war = no
			date > 2028.02.01
		}

		targets = {
			NHT
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}
		war_with_target_on_complete = yes
		ai_will_do = {
			factor = 100
		}

		days_remove = 5
		remove_effect  = {
			log = "[GetDateText]: Decision krakow_walka_na_maczety2"
			declare_war_on = {
				target = NHT
				type = voivodeship_war
			}		
		}
		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
		}
 }

 MPOL_01_NAL_HUTTA_STRIKES_BACK = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = NHT
		}

		target_root_trigger = {
			OR = {	
				KRK = { num_of_controlled_states > 1 }
				KRA = { num_of_controlled_states > 1 }
				BRK = { num_of_controlled_states > 1 }
				WND = { num_of_controlled_states > 1 }
				UJG = { num_of_controlled_states > 1 }
				AGH = { num_of_controlled_states > 1 }
				HAB = { num_of_controlled_states > 1 }
			}
			has_war = no
			date > 2028.06.01
		}
		targets = {
			KRK KRA BRK WND UJG AGH HAB
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}
		
		ai_will_do = {
			factor = 100
		}

		days_remove = 10
		remove_effect  = {
			log = "[GetDateText]: Decision krakow_walka_na_maczety"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}		
		}
		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
		}
 }


#MPOL_015_GREAT_NIEPOLOMICE_AUCTION_BID = {    #WORK IN PROGRESS - TO BE REFINED LATER
 #
 #		icon = GFX_decision_generic_prepare_civil_war
 #		
 #		allowed = {
 #			is_lesser_poland = yes
 #			}
 #
 #		available = {
 #			OR = {
 #				original_tag = UMN
 #				original_tag = BOH
 #				original_tag = BRS
 #				original_tag = SKW
 #			}
 #			has_global_flag = NPL_auction_active
 #		}
 #
 #    	available = {
 #    	    political_power > 20
 #    	}
 #
 #		0
 #		days_remove = 7
 #		days_re_enable = 3
 #
 #		ai_will_do = {
 #			factor = 100
 #		}
 #
 #		remove_effect  = {
 #			log = "[GetDateText]: Decision wielka licytacja niepolomicka"
 #		}
 #		cancel_trigger = {
 #			ROOT = {
 #				has_war = yes
 #			}
 #		}
 #	}	


#MAŁOPOLSKIE - FAZA DRUGA
   MPOL_02_SIEGE_OF_KRAKOW = {
	
	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		original_tag = PRK
	}

	target_root_trigger = {
		has_war = no
		date > 2028.10.10
		owns_state = 459
	}

	ai_will_do = {
		factor = 100
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
			is_krakow_tag = yes
		}
	}

	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision oblezenie krakowa"
		ROOT = {
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

 MPOL_02_AGRARIAN_REVOLUTION = {
	
	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = OSP
			original_tag = TRN
			original_tag = NPL
			original_tag = BRS
			original_tag = SKW
			original_tag = UMN
			original_tag = BOH
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2029.05.01
	}
	targets = {
		OSP TRN TRW BRS SKW UMN BOH NPL
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}
	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision wojna na tarnowszczyznie"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}					
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }


 MPOL_02_IN_THE_NAME_OF_POPE = {
	
	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = MSL
			original_tag = HOR
			original_tag = WDW
			original_tag = OSW
			original_tag = CHW
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2029.03.05
	}
	targets = {
		MSL HOR WDW OSW CHW
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision wojna na papajszczyźnie"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}			
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }
#MAŁOPOLSKIE - FAZA OSTATECZNA

 MPOL_03_GALICIAN_CLUSTERFUCK = {
	
	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		is_lesser_poland = yes
	}

	target_root_trigger = {
		has_war = no
		date > 2030.03.01
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
			is_lesser_poland = yes
			NOT = { has_war_with = ROOT }
		}
	}

	ai_will_do = {
		factor = 100
	}
	days_re_enable = 30
	remove_effect  = {
		log = "[GetDateText]: Decision raban galicyjski"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
 }

#██████╗░░█████╗░██████╗░██╗░░██╗░█████╗░██████╗░██████╗░░█████╗░░█████╗░██╗░░██╗██╗███████╗
#██╔══██╗██╔══██╗██╔══██╗██║░██╔╝██╔══██╗██╔══██╗██╔══██╗██╔══██╗██╔══██╗██║░██╔╝██║██╔════╝
#██████╔╝██║░░██║██║░░██║█████═╝░███████║██████╔╝██████╔╝███████║██║░░╚═╝█████═╝░██║█████╗░░
#██╔═══╝░██║░░██║██║░░██║██╔═██╗░██╔══██║██╔══██╗██╔═══╝░██╔══██║██║░░██╗██╔═██╗░██║██╔══╝░░
#██║░░░░░╚█████╔╝██████╔╝██║░╚██╗██║░░██║██║░░██║██║░░░░░██║░░██║╚█████╔╝██║░╚██╗██║███████╗
#╚═╝░░░░░░╚════╝░╚═════╝░╚═╝░░╚═╝╚═╝░░╚═╝╚═╝░░╚═╝╚═╝░░░░░╚═╝░░╚═╝░╚════╝░╚═╝░░╚═╝╚═╝╚══════╝
#PODKARPACKIE - FAZA PIERWSZA

 PDKRP_01_CHR_LEZ = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		original_tag = CHR
	}

	target_root_trigger = {
		has_war = no
		date > 2027.09.01
	}

	targets = { LEZ }
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}
	war_with_target_on_complete = yes
	ai_will_do = {
		factor = 100
	}


	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision lezajsk_wypij_do_dna"

		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}

	}

 	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
 	}
 }

 PDKRP_01_KRO_JSO = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		original_tag = KRO
	}

	target_root_trigger = {
		has_war = no
		date > 2027.07.13
	}

	ai_will_do = {
		factor = 100
	}

	targets = { JSO }
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}
	war_with_target_on_complete = yes
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision wino_wino_wypij_do_dna"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}

	}

 	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
 	}
 }

 PDKRP_01_BSC_ANC = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		original_tag = BSC
	}

	target_root_trigger = {
		has_war = no
		date > 2027.07.01
	}

	targets = { ANC }
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}
	war_with_target_on_complete = yes

	ai_will_do = {
		factor = 100

	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision bieszczady_war"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}

 	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
 	}
 }

 PDKRP_01_TAR_KOL = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		original_tag = TAR
	}

	target_root_trigger = {
		has_war = no
		date > 2027.08.01
	}

	ai_will_do = {
		factor = 100
	}

	targets = { KOL }
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}
	war_with_target_on_complete = yes
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision tarnobrzeg_kolbuszowa"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}

 	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
 	}
 }

#PODKARPACKIE - FAZA DRUGA

 PDKRP_02_MIELEC_DEBICA = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = MIE
			original_tag = DBI
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2028.04.01
	}
	targets = {
		MIE DBI
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	ai_will_do = {
		factor = 100

	}

	days_remove = 1
	remove_effect  = {
		log = "[GetDateText]: Decision mielec_debica"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}

	}

 	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
 	}
 }

 PDKRP_02_BATTLE_ROYALE = { 
	icon = GFX_decision_generic_prepare_civil_war

	allowed = {
		is_subcarpathia_unifier = yes
	}
	ai_will_do = {
		factor = 100
	}

	target_root_trigger = {
		date > 2028.04.20
		has_war = no
		ROOT = { NOT = { has_idea = cooldown }}
	}

	targets = {
		KRO LGP UHR UPA CHR RZE KOZ BSC TAR
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}
	war_with_target_on_complete = yes
	days_remove = 7
	complete_effect = {
		ROOT = { add_timed_idea = {
			idea = cooldown
			days = 9
		}
	 }
	}
	remove_effect = {
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}

 	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
 	}

}

#PODKARPACKIE - FAZA OSTATECZNA

 PDKRP_03_MINORS = {

	icon = GFX_decision_generic_prepare_civil_war
	allowed = {
		is_subcarpathia_unifier = yes
	}	
	target_root_trigger = {
		has_war = no
		ROOT = { NOT = { has_idea = cooldown }}
		count_triggers = {
			amount = 8
			KRO = { exists = no }
			LGP = { exists = no }
			UHR = { exists = no }
			UPA = { exists = no }
			CHR = { exists = no }
			RZE = { exists = no }
			KOZ = { exists = no }
			BSC = { exists = no }
			TAR = { exists = no }				
		}
	}

	targets = {
		LEZ DBI MIE KOL STL LCZ ANC JSO
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
			NOT = { has_war_with = ROOT }
		}
	}
	war_with_target_on_complete = yes
	ai_will_do = {
		factor = 100
	}

	days_remove = 10
	remove_effect  = {
		log = "[GetDateText]: Decision podkarpacie_mielec_debica"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}

 	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
 	}
 }

 PDKRP_03_BEX = {

	icon = GFX_decision_generic_prepare_civil_war
	allowed = {
		is_subcarpathia = yes		
	}	
	target_root_trigger = {
		has_war = no
		count_triggers = {
			amount = 14
			KOL = { exists = no }
			LEZ = { exists = no }
			ANC = { exists = no }
			JSO = { exists = no }
			KRO = { exists = no }
			TAR = { exists = no }	
			UPA = { exists = no }
			KOZ = { exists = no }
			BSC = { exists = no }
			UHR = { exists = no }
			LGP = { exists = no }
			CHR = { exists = no }	
			RZE = { exists = no }	
			DBI = { exists = no }	
			MIE = { exists = no }	
			LCZ = { exists = no }
		}
	}

	targets = { BEX }
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}


	ai_will_do = {
		factor = 100
	}

	days_remove = 10
	remove_effect  = {
		log = "[GetDateText]: Decision heart_of_darkness"
		if = { limit = { country_exists = BEX }
			annex_country = {
				target = BEX
				transfer_troops = no
			}
			hidden_effect = { 
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = {
						id = sanok.1
					}
				}
			}
		}
	}

 	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
 	}
 }


#██╗░░██╗░█████╗░██╗░░░░░██╗░░░██╗  ░█████╗░██████╗░░█████╗░░██████╗░██████╗
#██║░░██║██╔══██╗██║░░░░░╚██╗░██╔╝  ██╔══██╗██╔══██╗██╔══██╗██╔════╝██╔════╝
#███████║██║░░██║██║░░░░░░╚████╔╝░  ██║░░╚═╝██████╔╝██║░░██║╚█████╗░╚█████╗░
#██╔══██║██║░░██║██║░░░░░░░╚██╔╝░░  ██║░░██╗██╔══██╗██║░░██║░╚═══██╗░╚═══██╗
#██║░░██║╚█████╔╝███████╗░░░██║░░░  ╚█████╔╝██║░░██║╚█████╔╝██████╔╝██████╔╝
#╚═╝░░╚═╝░╚════╝░╚══════╝░░░╚═╝░░░  ░╚════╝░╚═╝░░╚═╝░╚════╝░╚═════╝░╚═════╝░
#ŚWIĘTOKRZYSKIE - FAZA PIERWSZA
 HOLY_CROSS_01_ROUND_ONE = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = NSZ
			original_tag = STO
			original_tag = NRA
			original_tag = STA
			original_tag = SND					
			original_tag = KEL				
		}
	}

	target_root_trigger = {
		ROOT = { any_neighbor_country = { is_holy_cross = yes } }
		has_war = no
		date > 2028.06.01
		date < 2029.02.18
		NOT = {
			any_other_country = {
				has_country_flag = holy_cross_round_one
			}
			has_country_flag = holy_cross_round_one_fought
		}
	}

	ai_will_do = {
		factor = 100		
	}
	days_re_enable = 5
	days_remove = 7
	complete_effect = {
		ROOT = {
			set_country_flag = holy_cross_round_one 
			set_country_flag = holy_cross_round_one_fought
		}
	}

	targets = {
		HOL
	}
	target_non_existing = yes

	remove_effect  = {
		log = "[GetDateText]: Decision holy_cross_round_one - fight"
		random_neighbor_country = { 
			limit = { 
				is_holy_cross = yes
				NOT = { original_tag = KIE }
			}
			declare_war_on = {
				target = ROOT
				type = voivodeship_war
			}
		}
		ROOT = {
			clr_country_flag = holy_cross_round_one 
		}
	}

	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
		any_other_country = {
			has_country_flag = holy_cross_round_one
		}
	}
 }

#ŚWIĘTOKRZYSKIE - FAZA DRUGA

 HOLY_CROSS_01_ROUND_TWO = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = NSZ
			original_tag = STO
			original_tag = NRA
			original_tag = STA
			original_tag = SND					
			original_tag = KIE			
		}
	}

	target_root_trigger = {
		NOT = {
			any_other_country = {
				has_country_flag = holy_cross_round_two
			}
		}
		ROOT = { any_neighbor_country = { is_holy_cross = yes } }
		has_war = no
		date > 2029.02.18
	}

	ai_will_do = {
		factor = 100		
	}

	targets = {
		HOL
	}
	target_non_existing = yes

	days_remove = 7
	complete_effect = {
		ROOT = {
			set_country_flag = holy_cross_round_two 
		}
	}
	remove_effect  = {
		log = "[GetDateText]: Decision holy_cross_round_two - fight"
		random_neighbor_country = { 
			limit = { 
				is_holy_cross = yes
			}
			declare_war_on = {
				target = ROOT
				type = voivodeship_war
			}
		}
		ROOT = {
			clr_country_flag = holy_cross_round_two 
		}
	}

	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
		any_other_country = {
			has_country_flag = holy_cross_round_two
		}
	}
 }
#ŚWIĘTOKRZYSKIE - FAZA OSTATECZNA
#██╗░░░░░██╗░░░██╗██████╗░███████╗██╗░░░░░░██████╗██╗░░██╗██╗███████╗
#██║░░░░░██║░░░██║██╔══██╗██╔════╝██║░░░░░██╔════╝██║░██╔╝██║██╔════╝
#██║░░░░░██║░░░██║██████╦╝█████╗░░██║░░░░░╚█████╗░█████═╝░██║█████╗░░
#██║░░░░░██║░░░██║██╔══██╗██╔══╝░░██║░░░░░░╚═══██╗██╔═██╗░██║██╔══╝░░
#███████╗╚██████╔╝██████╦╝███████╗███████╗██████╔╝██║░╚██╗██║███████╗
#╚══════╝░╚═════╝░╚═════╝░╚══════╝╚══════╝╚═════╝░╚═╝░░╚═╝╚═╝╚══════╝
#LUBELSKIE - FAZA PIERWSZA
LBL_01_MAFIA = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		is_lubelskie = yes
		is_lubelskie_tribe = no
	}

	target_root_trigger= {
		if = {
			limit = {
				ROOT = {
					original_tag = TEN
				}
			}
		has_war = no
		date > 2027.05.04
		}
		if = {
			limit = {
				ROOT = {
					NOT = { original_tag = TEN }
				}
			}
		has_war = no
		date > 2030.01.01
		TEN = { exists = no }
		}
	}

	targets = { RMF }
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision LBL_01_RPP"
		declare_war_on = {
			target = RMF
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}
LBL_01_RPP = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		is_lubelskie = yes
		is_lubelskie_tribe = no
	}

	target_root_trigger = {
		if = {
			limit = {
				ROOT = {
					OR = {
					original_tag = TEN
					original_tag = BPD
					}
				}
			}
		has_war = no
		date > 2028.04.01
		}
		if = {
			limit = {
				ROOT = {
					NOT = { 
						OR = {
							original_tag = TEN 
							original_tag = BPD 
						}
					}
				}
			}
		has_war = no
		date > 2030.01.01
		BPD = { exists = no }
		TEN = { exists = no }
		}
	}
	targets = { RPP }
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision LBL_01_RPP"
		declare_war_on = {
			target = RPP
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}
LBL_01_AZOTY = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		is_lubelskie = yes
		is_lubelskie_tribe = no
	}

	target_root_trigger = {
		if = {
			limit = {
				ROOT = {
					original_tag = PUL
				}
			}
		has_war = no
		date > 2027.08.01
		}
		if = {
			limit = {
				ROOT = {
					NOT = { 
						original_tag = PUL
					}
				}
			}
		has_war = no
		date > 2030.01.01
		PUL = { exists = no }
		}
	}

	ai_will_do = {
		factor = 100
	}
	targets = { AZO }
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision LBL_01_AZOTY"
		declare_war_on = {
			target = AZO
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}
LBL_01_KRASNYSTAW = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		is_lubelskie = yes
		is_lubelskie_tribe = no
	}

	target_root_trigger = {
		if = {
			limit = {
				ROOT = {
					original_tag = ZUK
				}
			}
		has_war = no
		date > 2028.05.01
		KST = { exists = yes }
		}
		if = {
			limit = {
				ROOT = {
					NOT = { 
						original_tag = ZUK
					}
				}
			}
		has_war = no
		date > 2030.01.01
		ZUK = { exists = no }
		KST = { exists = yes }
		ROOT = { is_neighbor_of = KST }
		}
	}

	ai_will_do = {
		factor = 100
	}

	targets = { KST }
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision LBL_01_KRASNYSTAW"
		declare_war_on = {
			target = KST
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}
LBL_01_BREJDACTWO = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		is_lubelskie = yes
		is_lubelskie_tribe = no
	}

	target_root_trigger = {
		if = {
			limit = {
				ROOT = {
					original_tag = ZUK
				}
			}
		has_war = no
		date > 2028.04.01
		}
		if = {
			limit = {
				ROOT = {
					NOT = { 
						original_tag = ZUK
					}
				}
			}
		has_war = no
		date > 2030.01.01
		ZUK = { exists = no }
		}
	}

	targets = { LBW }
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision LBL_01_BREJDACTWO"
		declare_war_on = {
			target = LBW
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}
LBL_01_PARTYZANCI = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		is_lubelskie = yes
		is_lubelskie_tribe = no
	}

	target_root_trigger = {
		if = {
			limit = {
				ROOT = {
					OR = {
						original_tag = TEN
						original_tag = BPD
					}
				}
			}
		has_war = no
		date > 2028.10.01
		}
		if = {
			limit = {
				ROOT = {
					NOT = { 
						OR = {
						original_tag = BPD
						original_tag = TEN
						}
					}
				}
			}
		has_war = no
		date > 2030.01.01
		BPD = { exists = no }
		TEN = { exists = no }
		}
	}

	targets = { PLB }
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision LBL_01_PARTYZANCI"
		declare_war_on = {
			target = PLB
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

LBL_01_ORDYNACJA = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = LEN
			original_tag = BIL
			original_tag = ORD
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2028.03.01
	}
	targets = { ORD BIL LEN }
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}
	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision LBL_01_ORDYNACJA"
		declare_war_on = {
			target = FROM 
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}
LBL_01_EAST_LUBLIN_WAR = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = SWD
			original_tag = KSN
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2028.03.13
	}
	targets = { KSN SWD }
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	ai_will_do = {
		factor = 100
	}
	

	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision LBL_01_EAST_LUBLIN"
		declare_war_on = {
			target = FROM 
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}
LBL_015_NORTH_LUBLIN_WAR = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = BPD
			original_tag = PUL
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2028.08.01
	}
	targets = { BPD PUL }
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision LBL_01_EAST_LUBLIN"
		declare_war_on = {
			target = FROM 
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

#LUBELSKIE - FAZA DRUGA
LBL_02_LUBLIN_WAR = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = CRN
			original_tag = KSN
			original_tag = SWD
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2029.07.01
	}
	targets = { CRN KSN SWD }
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision LBL_01_EAST_LUBLIN"
		declare_war_on = {
			target = FROM 
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}
LBL_02_NORTH_LUBLIN_WAR = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = BPD
			original_tag = TEN
			original_tag = PUL
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2029.05.15
	}
	targets = { BPD TEN PUL }
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision LBL_02_NORTH_LUBLIN"
		declare_war_on = {
			target = FROM 
			type = voivodeship_war
		}		
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}
LBL_02_SOUTH_LUBLIN_WAR = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = ZUK
			original_tag = BIL
			original_tag = ORD
			original_tag = LEN
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2029.05.20
	}
	targets = { ZUK BIL ORD LEN }
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision LBL_02_SOUTH_LUBLIN"
		declare_war_on = {
			target = FROM 
			type = voivodeship_war
		}			
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}


#LUBELSKIE - FAZA OSTATECZNA
LBL_03_LUBELSZCZYZNA_HYPERWAR = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		is_lubelskie = yes
	}

	target_root_trigger = {
		has_war = no
		date > 2030.02.01
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
			is_lubelskie = yes
		}
	}

	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision LBL_02_SOUTH_LUBLIN"
		declare_war_on = {
			target = FROM 
			type = voivodeship_war
		}		
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
} 


#██████╗░░█████╗░██████╗░░█████╗░███╗░░░███╗░██████╗██╗░░██╗██╗███████╗
#██╔══██╗██╔══██╗██╔══██╗██╔══██╗████╗░████║██╔════╝██║░██╔╝██║██╔════╝
#██████╔╝███████║██║░░██║██║░░██║██╔████╔██║╚█████╗░█████═╝░██║█████╗░░
#██╔══██╗██╔══██║██║░░██║██║░░██║██║╚██╔╝██║░╚═══██╗██╔═██╗░██║██╔══╝░░
#██║░░██║██║░░██║██████╔╝╚█████╔╝██║░╚═╝░██║██████╔╝██║░╚██╗██║███████╗
#╚═╝░░╚═╝╚═╝░░╚═╝╚═════╝░░╚════╝░╚═╝░░░░░╚═╝╚═════╝░╚═╝░░╚═╝╚═╝╚══════╝
#RADOMSKIE - FAZA PIERWSZA
 RADOM_01_BUREAUCRATIC_POETRY = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		original_tag = SKA
	}

	target_root_trigger = {
		has_war = no
		date > 2028.07.11
	}

	ai_will_do = {
		factor = 100
	}

	targets = {
		CZA
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}
	war_with_target_on_complete = yes
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision RADOM_01_BUREAUCRATIC_POETRY"
		declare_war_on = {
			target = CZA
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }

 RADOM_01_MILITARY_CLASH = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = AIR
			original_tag = PAB
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2028.04.05
	}

	targets = {
		AIR PAB
	}

	target_trigger = {
		FROM = { 
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}	
	war_with_target_on_complete = yes
	ai_will_do = {
		factor = 100
	}

	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision RADOM_01_MILITARY_CLASH"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}

	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }
 
 RADOM_01_COUNTY_OF_HORTEX = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = APC
			original_tag = SZY
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2028.06.01
	}

	targets = {
		APC SZY
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}	

	war_with_target_on_complete = yes
	ai_will_do = {
		factor = 100
	}

	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision RADOM_01_COUNTY_OF_HORTEX"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 } 

#RADOMSKIE - FAZA DRUGA
 RADOM_02_EAST_RADOM_WAR = {
	
	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = AIR
			original_tag = PAB
			original_tag = SKA
			original_tag = CZA
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2028.11.01
	}

	targets = {
		AIR PAB SKA CZA
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}
	war_with_target_on_complete = yes

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision wojna na wschodniej radomszczyznie"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}					
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

 RADOM_02_WEST_RADOM_WAR = {
	
	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = SAT
			original_tag = SZY
			original_tag = APC
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2028.11.01
	}
	targets = {
		SAT SZY APC
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision wojna na zachodniej radomszczyznie"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}								
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }


#RADOMSKIE - FAZA OSTATECZNA
 RADOM_03_RADOM_WAR = {
	
	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		is_radom_province = yes
	}

	target_root_trigger = {
		has_war = no
		date > 2029.05.01
		RAD = { has_country_flag = radom_committed }
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
			is_radom_province = yes
		}			
	}

	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision wojna na radomszczyznie"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }


#██╗░░░░░░█████╗░██████╗░███████╗██╗░░██╗██╗███████╗
#██║░░░░░██╔══██╗██╔══██╗╚════██║██║░██╔╝██║██╔════╝
#██║░░░░░██║░░██║██║░░██║░░███╔═╝█████═╝░██║█████╗░░
#██║░░░░░██║░░██║██║░░██║██╔══╝░░██╔═██╗░██║██╔══╝░░
#███████╗╚█████╔╝██████╔╝███████╗██║░╚██╗██║███████╗
#╚══════╝░╚════╝░╚═════╝░╚══════╝╚═╝░░╚═╝╚═╝╚══════╝
#ŁÓDZKIE - FAZA PIERWSZA
 the_monolog_war = { 
	icon = GFX_decision_generic_prepare_civil_war
	allowed = {
		original_tag = WML
	}

	ai_will_do = {
		factor = 100
	}

	target_root_trigger = {
		AND = { 
			date > 2031.03.01
			has_war = no
			NOT = { is_in_faction = yes }
			ROOT = { NOT = { has_idea = cooldown } }
		}
	}

	targets = {
		KAR TIG AMW RDM LKS RUT POG SAP BBW KRS ZHP ZYD
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}
	war_with_target_on_complete = yes
	complete_effect = {
		ROOT = { add_timed_idea = {
			idea = cooldown
			days = 9
		}
	 }
	}
	days_remove = 7
	remove_effect = {
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}

	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}

 }


 cleanup_lodzig_wars = { 
	icon = GFX_decision_generic_prepare_civil_war
	allowed = {
		OR = {
			original_tag = AMW
			original_tag = RDM
			original_tag = TIG
			original_tag = KAR
			original_tag = LKS
			original_tag = POG
			original_tag = RUT
		}
	}

	ai_will_do = {
		factor = 100
	}

	target_root_trigger = {
		AND = { 
			date > 2030.03.01
			has_war = no
			NOT = { country_exists = BBW }
			NOT = { country_exists = KRS }
			NOT = { country_exists = SAP }
			NOT = { country_exists = ZHP }
			NOT = { country_exists = ZYD }
			NOT = { country_exists = WML }
			ROOT = { NOT = { has_idea = cooldown } }
		}
	}

	targets = {
		KAR TIG AMW RDM LKS RUT POG
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}
	war_with_target_on_complete = yes
	complete_effect = {
		ROOT = { add_timed_idea = {
			idea = cooldown
			days = 9
		}
	 }
	}
	days_remove = 7
	remove_effect = {
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}

	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}

 }
#ŁÓDZKIE - FAZA DRUGA
#ŁÓDZKIE - FAZA OSTATECZNA
#░██╗░░░░░░░██╗░█████╗░██████╗░░██████╗███████╗░█████╗░░██╗░░░░░░░██╗░█████╗░
#░██║░░██╗░░██║██╔══██╗██╔══██╗██╔════╝╚════██║██╔══██╗░██║░░██╗░░██║██╔══██╗
#░╚██╗████╗██╔╝███████║██████╔╝╚█████╗░░░███╔═╝███████║░╚██╗████╗██╔╝███████║
#░░████╔═████║░██╔══██║██╔══██╗░╚═══██╗██╔══╝░░██╔══██║░░████╔═████║░██╔══██║
#░░╚██╔╝░╚██╔╝░██║░░██║██║░░██║██████╔╝███████╗██║░░██║░░╚██╔╝░╚██╔╝░██║░░██║
#░░░╚═╝░░░╚═╝░░╚═╝░░╚═╝╚═╝░░╚═╝╚═════╝░╚══════╝╚═╝░░╚═╝░░░╚═╝░░░╚═╝░░╚═╝░░╚═╝

#WARSZAWA - PRZED ZIMNĄ WOJNĄ

 WARSZAWA_TV_HYPERWAR = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = TVP
			original_tag = TVN
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2028.04.11 #Nie wiem kiedy dobrze by było zacząć, więc roboczo jest tutaj
		is_in_faction = no
	}
	targets = {
		TVP TVN
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}		
	}
	

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_TV_HYPERWAR"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }

 WARSZAWA_gospodarka_niemiecka = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		original_tag = ZIE
	}

	target_root_trigger = {
		has_war = no
		date > 2028.04.11 #tu to samo
		is_in_faction = no
	}

	targets = {
		ATO
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}		
	}

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_gospodarka_niemiecka"
		declare_war_on = {
			target = ATO
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }

 WARSZAWA_legia_podzielona = { #murem nienawiści

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = PLC
			original_tag = BLO
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2028.04.11 #tu to samo
		is_in_faction = no
	}

	targets = {
		LEG
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}		
	}
	
	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_legia_podzielona"
		declare_war_on = {
			target = LEG
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }

 WARSZAWA_chcialby_pan_porozmawiac = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		original_tag = JEH
	}

	target_root_trigger = {
		has_war = no
		date > 2028.04.11 #tu to samo
		is_in_faction = no
	}

	targets = {
		GRD
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}
	

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_chcialby_pan_porozmawiac"
		declare_war_on = {
			target = GRD
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }

 WARSZAWA_stan_wojenny = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		original_tag = ZYR
	}

	target_root_trigger = {
		has_war = no
		date > 2028.04.11 #tu to samo
		is_in_faction = no
	}
	
	targets = {
		CPK
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}		
	}

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_stan_wojenny "
		declare_war_on = {
			target = CPK
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }

 WARSZAWA_czersk_vs_dupa = { #nie miałem pomysłu na nazwę

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = CZR
			original_tag = DUP
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2028.04.11 #tu to samo
		is_in_faction = no
	}

	targets = {
		CZR DUP
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}
	

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_czersk_vs_dupa"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }

 WARSZAWA_zlomowanie = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		original_tag = MAF
	}

	target_root_trigger = {
		has_war = no
		date > 2028.04.11 #tu to samo
		is_in_faction = no
	}

	targets = {
		OKE
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}		
	}
	

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_zlomowanie "
		declare_war_on = {
			target = OKE
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }

 #WARSZAWA - START WOJNY

 WARSZAWA_pozew = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		original_tag = WRS
	}

	target_root_trigger = {
		has_war = no
		date > 2030.04.11 #tu to samo
		is_in_faction = yes
	}

	targets = {
		WRC
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}		
	}
	

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_pozew "
		declare_war_on = {
			target = WRC
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }
 
  #WARSZAWA - FAZA POWOJENNA OGÓLNE
 
 WARSZAWA_komunizacja = { # komuna vs grodzisk lub jehowi

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = ZYR
			original_tag = JEH
			original_tag = CPK
			original_tag = GRD
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.07.15 #placeholder
	}
	targets = {
		ZYR JEH CPK GRD
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_zielenina"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}
 
 ### WARSZAWA - FAZA 1 POWOJENNA LEWICA
 
WARSZAWA_zielenina = { #URSUS vs WŁOCHY

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = URS
			original_tag = ITA
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.07.15 #placeholder
		has_global_flag = warszawa_lewica_win
	}

	targets = {
		URS ITA
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}
	

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_zielenina"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_protestowanie = { #ZNP VS KOD

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = KOD
			original_tag = ZNP
		}
	}

	target_root_trigger= {
		has_war = no
		date > 2031.07.15 #placeholder
		has_global_flag = warszawa_lewica_win
	}
	
	targets = {
		ZNP KOD
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}		
	}

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_zielenina"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_car_cleanup = { #jakby FSO jakoś przetrwało

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = { 
			original_tag = WRC
			original_tag = WRS
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.07.15 #placeholder
		OR = {
			has_global_flag = warszawa_lewica_win
			has_global_flag = warszawa_prawica_win
		}
	}
	highlight_states = {
    highlight_state_targets = {
        state = 560
    }
	}
	targets = {
		FSO
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}		
	}

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_vroom_vroom"
		declare_war_on = {
			target = FSO
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_jebac_karczew = { #wawer vs karczew

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = ZIE
			original_tag = WRC
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.07.15 #placeholder
		has_global_flag = warszawa_lewica_win
	}

	targets = {
		ZIE WRC
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}

	
	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_zielenina"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_debile_sie_bija = { #mińsk vs garwolin

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = WOW
			original_tag = GAR
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.07.15 #placeholder
		has_global_flag = warszawa_lewica_win
	}
	
	ai_will_do = {
		factor = 100
	}

	targets = {
		WOW GAR
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}

	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_zielenina"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_antymafia = { # śpiewak vs mafia

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = WRS
			original_tag = WOL
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.07.15 #placeholder
		has_global_flag = warszawa_lewica_win
	}

	targets = {
		WOL WRS
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_antymafia"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_wycinka = { # lasy vs bielany vs wbg

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = BIE
			original_tag = WBG
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.07.15 #placeholder
		has_global_flag = warszawa_lewica_win
	}
	targets = {
		BIE LAS WBG
	}
	target_trigger = {
		FROM = { 
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}
	
	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_wycinka"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_recykling = { # urs, ita vs pru, oke

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = URS
			original_tag = OKE
			original_tag = MAF
			original_tag = ITA
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.07.15 #placeholder
		has_global_flag = warszawa_lewica_win
	}
	targets = {
		URS ITA OKE MAF
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_recykling"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_edukacja_wazna_rzecz = { # znp vs wat

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = WAT
			original_tag = ZNP
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.07.15 #placeholder
		has_global_flag = warszawa_lewica_win
	}

	targets = {
		WAT ZNP
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}
	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_recykling"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_prezydentura_szmezydentura = { # trz vs dup, czr

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = TRZ
			original_tag = CZR
			original_tag = DUP
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.07.15 #placeholder
		has_global_flag = warszawa_lewica_win
	}
	targets = {
		TRZ CZR DUP
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}
	
	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_recykling"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_policajt = { # kod vs plc

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = KOD
			original_tag = PLC
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.07.15 #placeholder
		has_global_flag = warszawa_lewica_win
	}

	ai_will_do = {
		factor = 100
	}

	targets = {
		KOD PLC
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_policajt"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

### WARSZAWA - FAZA 2 POWOJENNA LEWICA

WARSZAWA_zachod_slonca = { # zyr/grd/jeh vs wbg/las/bie

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = JEH
			original_tag = ZYR
			original_tag = WBG
			original_tag = BIE
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.10.01 #placeholder
		has_global_flag = warszawa_lewica_win
	}
	targets = {
		JEH ZYR BIE WBG LAS GRD
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_zachod_slonca"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_centrum_battle_royale = { # kod/znp/wat/plc vs urs/ita/maf/oke

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = KOD
			original_tag = WAT
			original_tag = ZNP
			original_tag = PLC
			original_tag = URS
			original_tag = ITA
			original_tag = MAF
			original_tag = OKE
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.10.01 #placeholder
		has_global_flag = warszawa_lewica_win
	}
	targets = {
		KOD ZNP WAT PLC URS ITA MAF OKE
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_centrum_battle_royale"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_poludnie_vs_dzielnice = { # dup/czr/trz vs urs/ita/maf/oke

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = URS
			original_tag = ITA
			original_tag = MAF
			original_tag = OKE
			original_tag = CZR
			original_tag = DUP
			original_tag = TRZ
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.10.01 #placeholder
		has_global_flag = warszawa_lewica_win
	}
	targets = {
		URS ITA MAF OKE CZR DUP TRZ
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_centrum_battle_royale"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_centrum_vs_poludnie = { # kod/znp/wat/plc vs trz/dup/czr

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = KOD
			original_tag = WAT
			original_tag = ZNP
			original_tag = PLC
			original_tag = TRZ
			original_tag = DUP
			original_tag = CZR
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.10.01 #placeholder
		has_global_flag = warszawa_lewica_win
	}
	targets = {
		KOD WAT ZNP PLC TRZ DUP CZR
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_centrum_vs_poludnie"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}		
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_wschodnia_wawa = { # wrs/fso/wol vs zie/ato/wrc

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = ZIE
			original_tag = ATO
			original_tag = WRC
			original_tag = WRS
			original_tag = FSO
			original_tag = WOL
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.10.01 #placeholder
		has_global_flag = warszawa_lewica_win
	}
	targets = {
		ZIE ATO WRC WRS FSO WOL
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_centrum_battle_royale"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_wschodnie_maz_vs_polnoc = { # wrs/fso/wol vs gar/wow

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = WOW
			original_tag = GAR
			original_tag = WRS
			original_tag = FSO
			original_tag = WOL
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.10.01 #placeholder
		has_global_flag = warszawa_lewica_win
	}
	targets = {
		WRS FSO WOL WOW GAR
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_centrum_battle_royale"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_wschodnie_maz_vs_chuj = { # zie/ato/wrc vs gar/wow

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = WOW
			original_tag = GAR
			original_tag = WRC
			original_tag = ZIE
			original_tag = ATO
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.10.01 #placeholder
		has_global_flag = warszawa_lewica_win
	}
	targets = {
		ZIE ATO WRC WOW GAR
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_centrum_battle_royale"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_zachod_beatdown = { # zyr/wbg/bie/grd/jeh/las vs trz/kod/znp/urs/ita/maf/oke/wat/plc/dup/czr/trz/

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = KOD
			original_tag = ZNP
			original_tag = URS
			original_tag = MAF
			original_tag = OKE
			original_tag = PLC
			original_tag = WAT
			original_tag = ITA
			original_tag = CZR
			original_tag = DUP
			original_tag = TRZ
			original_tag = BIE
			original_tag = LAS
			original_tag = GRD
			original_tag = WBG
			original_tag = ZYR
			original_tag = JEH
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2032.01.01 #placeholder
		has_global_flag = warszawa_lewica_win
	}
	targets = {
		KOD ZNP URS MAF OKE PLC WAT ITA CZR DUP TRZ BIE LAS GRD WBG ZYR JEH
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_centrum_battle_royale"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}	
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_total_war= { # lewo/prawo

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		is_capital_district = yes
	}

	target_root_trigger = {
		has_war = no
		date > 2033.03.01 #placeholder
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
			is_capital_district = yes
		}			
	}

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_centrum_battle_royale"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

# WARSZAWA POWOJENNA FAZA 1 PRAWICA

WARSZAWA_jaki_vs_mafia = { #wrc vs wol

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = WRC
			original_tag = WOL
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.07.15 #placeholder
		has_global_flag = warszawa_prawica_win
	}

	ai_will_do = {
		factor = 100
	}

	targets = {
		WRC WOL
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}

	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_jaki_vs_mafia"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_dziurozapychacz = { #blo vs ato

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = BLO
			original_tag = ATO
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.07.15 #placeholder
		has_global_flag = warszawa_prawica_win
	}
	
	targets = {
		BLO ATO
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}		
	}

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_jaki_vs_mafia"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_raiders_of_the_lost_kacz = { #kcz vs plc

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = KCZ
			original_tag = PLC
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.07.15 #placeholder
		has_global_flag = warszawa_prawica_win
	}

	ai_will_do = {
		factor = 100
	}

	targets = {
		KCZ PLC
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_jaki_vs_mafia"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_watvsipn = { #wat vs ipn

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = 	WAT
			original_tag = IPN
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.07.15 #placeholder
		has_global_flag = warszawa_prawica_win
	}
	
	ai_will_do = {
		factor = 100
	}

	targets = {
		WAT IPN
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}

	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_watvsipn"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_dekorowanie_zlomowiska = { #URS VS MAF

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = URS
			original_tag = MAF
		}
		}

	target_root_trigger = {
		has_war = no
		date > 2031.07.15 #placeholder
		has_global_flag = warszawa_prawica_win
	}

	targets = {
		URS MAF
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_watvsipn"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_dronotestowanie = { #WBG VS LAS

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = LAS
			original_tag = WBG
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.07.15 #placeholder
		has_global_flag = warszawa_prawica_win
	}

	targets = {
		LAS WBG
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_watvsipn"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_napierdalanka_o_centrum = { #KCZ/PLC VS IPN/WAT

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = WAT
			original_tag = IPN
			original_tag = KCZ
			original_tag = PLC
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.07.15 #placeholder
		has_global_flag = warszawa_prawica_win
	}
	
	targets = {
		WAT IPN KCZ PLC
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}	

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_watvsipn"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_napierdalanka_o_centropoludnie = { #MAF/URS VS TVP

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = MAF
			original_tag = URS
			original_tag = TVP
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.07.15 #placeholder
		has_global_flag = warszawa_prawica_win
	}
	
	targets = {
		MAF URS TVP
	}
	target_trigger = {
		FROM = { 
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_watvsipn"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_napierdalanka_o_poludnie = { #JEH/ZYR/CPK VS DUP

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = JEH
			original_tag = CPK
			original_tag = ZYR
			original_tag = DUP
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.07.15 #placeholder
		has_global_flag = warszawa_prawica_win
	}
	targets = {
		JEH CPK ZYR DUP
	}
	target_trigger = {
		FROM = { 
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_napierdalanka_o_poludnie"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_napierdalanka_o_polnoc = { #WBG/LAS VS KCZ/IPN/WAT/URS

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = WBG
			original_tag = LAS
			original_tag = KCZ
			original_tag = IPN
			original_tag = WAT
			original_tag = URS
			original_tag = PLC
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.07.15 #placeholder
		has_global_flag = warszawa_prawica_win
	}
	targets = {
		WBG LAS KCZ IPN WAT URS PLC
	}
	target_trigger = {
		FROM = { 
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}
	
	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_napierdalanka_o_poludnie"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_centrum_vs_south_prawica = { #MAF/TVP/URS VS DUP/JEH/ZYR/CPK

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = MAF
			original_tag = URS
			original_tag = TVP
			original_tag = JEH
			original_tag = ZYR
			original_tag = CPK
			original_tag = DUP
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.09.15 #placeholder
		has_global_flag = warszawa_prawica_win
	}
	targets = {
		MAF URS TVP JEH ZYR CPK DUP
	}
	target_trigger = {
		FROM = { 
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}
	
	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_napierdalanka_o_poludnie"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_centrum_vs_north_prawica = { #MAF/TVP/URS VS KCZ/IPN/WAT/PLC/WBG/LAS

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = MAF
			original_tag = URS
			original_tag = TVP
			original_tag = KCZ
			original_tag = IPN
			original_tag = WAT
			original_tag = PLC
			original_tag = WBG
			original_tag = LAS
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.09.15 #placeholder
		has_global_flag = warszawa_prawica_win
	}
	targets = {
		MAF URS TVP KCZ IPN WAT PLC WBG LAS
	}
	target_trigger = {
		FROM = { 
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}
	
	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_napierdalanka_o_poludnie"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_south_vs_north_prawica = { #MAF/TVP/URS VS KCZ/IPN/WAT/PLC/WBG/LAS

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = ZYR
			original_tag = JEH
			original_tag = CPK
			original_tag = DUP
			original_tag = KCZ
			original_tag = IPN
			original_tag = WAT
			original_tag = PLC
			original_tag = WBG
			original_tag = LAS
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.09.15 #placeholder
		has_global_flag = warszawa_prawica_win
	}
	targets = {
		ZYR JEH CPK DUP KCZ IPN WAT PLC WBG LAS
	}
	target_trigger = {
		FROM = { 
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}
	
	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_napierdalanka_o_poludnie"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_wrc_vs_blo = { #wrc vs blo

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = WRC
			original_tag = BLO
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.07.15 #placeholder
		has_global_flag = warszawa_prawica_win
	}

	targets = {
		WRC BLO
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_watvsipn"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_wrc_vs_wow = { #wrc vs wow

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = WRC
			original_tag = WOW
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.07.15 #placeholder
		has_global_flag = warszawa_prawica_win
	}

	ai_will_do = {
		factor = 100
	}

	targets = {
		WRC WOW
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_watvsipn"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_blo_vs_wow = { #blo vs wow

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = BLO
			original_tag = WOW
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.07.15 #placeholder
		has_global_flag = warszawa_prawica_win
	}
	
	ai_will_do = {
		factor = 100
	}

	targets = {
		BLO WOW
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}		
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_watvsipn"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_gar_vs_wow = { #gar vs wow

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = GAR
			original_tag = WOW
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.07.15 #placeholder
		has_global_flag = warszawa_prawica_win
	}

	ai_will_do = {
		factor = 100
	}
	targets = {
		GAR WOW
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_watvsipn"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_gar_vs_blo = { #gar vs blo

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = GAR
			original_tag = BLO
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.07.15 #placeholder
		has_global_flag = warszawa_prawica_win
	}

	ai_will_do = {
		factor = 100
	}
	targets = {
		GAR BLO
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_watvsipn"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

WARSZAWA_gar_vs_wrc = { #gar vs wrc

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = GAR
			original_tag = WRC
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2031.07.15 #placeholder
		has_global_flag = warszawa_prawica_win
	}

	targets = {
		GAR WRC
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision WARSZAWA_watvsipn"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
}

#██████╗░░█████╗░██████╗░██╗░░░░░░█████╗░░██████╗██╗███████╗
#██╔══██╗██╔══██╗██╔══██╗██║░░░░░██╔══██╗██╔════╝██║██╔════╝
#██████╔╝██║░░██║██║░░██║██║░░░░░███████║╚█████╗░██║█████╗░░
#██╔═══╝░██║░░██║██║░░██║██║░░░░░██╔══██║░╚═══██╗██║██╔══╝░░
#██║░░░░░╚█████╔╝██████╔╝███████╗██║░░██║██████╔╝██║███████╗
#╚═╝░░░░░░╚════╝░╚═════╝░╚══════╝╚═╝░░╚═╝╚═════╝░╚═╝╚══════╝
#PODLASIE - FAZA PIERWSZA


 end_of_popular_front = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		original_tag = AGR
	}

	available = {
		date > 2028.05.16
	}

	ai_will_do = {
		factor = 100
	}
	fire_only_once = yes
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision end_of_popular_front"
		AGR = {
			leave_faction = yes
		}
	}

 	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
 	}
 }

 greater_podlasie_debacle = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = AGR
			original_tag = SDL
		}
	}

	target_root_trigger = {
		date > 2028.06.16
		has_war = no
	}

	ai_will_do = {
		factor = 100
	}

	targets = {
		WIL SAW
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}		
	}

	days_remove = 7

	war_with_target_on_complete = yes

	remove_effect  = {
		log = "[GetDateText]: Decision POMGDA_gdansk_war"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }

 
 greater_podlasie_debacle_2 = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = AGR
			original_tag = SDL
		}
	}

	target_root_trigger = {
		date > 2029.03.01
		SAW = { exists = no }
		WIL = { exists = no }
		has_war = no
	}

	ai_will_do = {
		factor = 100
	}

	targets = {
		AGR SDL
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}

	days_remove = 7

	war_with_target_on_complete = yes

	remove_effect  = {
		log = "[GetDateText]: Decision POMGDA_gdansk_war"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }
 

	podlasie_plemiona = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			is_podlasie = yes
			NOT = {
				is_podlasie_tribe = yes
			}
		}

		target_root_trigger = {
			OR = {
				AND = {
					OR = {
						original_tag = BOM
						original_tag = KON
						original_tag = TAT
						original_tag = BEL
						original_tag = SAN
						original_tag = LMZ
					}
					ROOT = { any_neighbor_country = { is_podlasie_tribe = yes } }
					date > 2027.10.01
					has_war = no
					NOT = { has_idea = cooldown }
				}
				AND = {
					OR = {
						original_tag = SDL
						original_tag = AGR
						}
					ROOT = { any_neighbor_country = { is_podlasie_tribe = yes } }
					count_triggers = {
							amount = 5
							TAT = {exists = no}
							SAN = {exists = no}
							LMZ = {exists = no}
							BEL = {exists = no}
							BOM = {exists = no}
							KON = {exists = no}
					}
					has_war = no
					NOT = { has_idea = cooldown }
					date > 2031.04.01
				}
			}
		}		
		
		target_array = global.podlasie_tribes
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				is_podlasie_tribe = yes
				NOT = { has_war_with = ROOT }
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
			}	
		}


		ai_will_do = {
			factor = 100
		}

		days_remove = 10
		complete_effect = {
			add_timed_idea = {
				idea = cooldown
				days = 11
			}
		}
		remove_effect  = {
			log = "[GetDateText]: [ROOT.GetName] fires decision podlasie_plemiona"
			ROOT = {
				declare_war_on = {
					target = FROM
					type = voivodeship_war
				}
			}
		}
		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
		}
	}	
#PODLASIE - FAZA DRUGA

	podlasie_TAT = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {
				original_tag = KON
				original_tag = BOM
				original_tag = BEL
				original_tag = SAN
				original_tag = LMZ
			}
		}

		targets = {
			TAT
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}	
		}

		target_root_trigger = {
			NOT = { 
				has_idea = cooldown
			}
			has_war = no
			count_triggers = {
				amount = 16
					TNO = { exists = no }
					COL = { exists = no }
					ZAM = { exists = no }
					HMA = { exists = no }
					UHL = { exists = no }
					DRO = { exists = no }
					ZAB = { exists = no }
					SUP = { exists = no }
					GRO = { exists = no }
					TYK = { exists = no }
					OSO = { exists = no }
					DAM = { exists = no }
					BRD = { exists = no }
					AUG = { exists = no }
					SEI = { exists = no }
					MON = { exists = no }
					BAG = { exists = no }
			}
		}

		ai_will_do = {
			factor = 100
		}

		
		days_remove = 10
		complete_effect = {
			ROOT = { add_timed_idea = {
				idea = cooldown
				days = 11
			}
			}
		}
		remove_effect  = {
			log = "[GetDateText]: Decision podlasie_TAT"
			if = { limit = { country_exists = TAT }
				declare_war_on = {
					target = TAT
					type = voivodeship_war
				}
			}
		}

		cancel_trigger = {
			has_war = yes
		}
	}

	podlasie_SAN = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {
				original_tag = KON
				original_tag = TAT
				original_tag = BEL
				original_tag = BOM
				original_tag = LMZ
			}			
		}
		
		targets = {
			SAN
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}	
		}

		target_root_trigger = {
			NOT = { 
				has_idea = cooldown
			}
			has_war = no
			count_triggers = {
				amount = 16
					TNO = { exists = no }
					COL = { exists = no }
					ZAM = { exists = no }
					HMA = { exists = no }
					UHL = { exists = no }
					DRO = { exists = no }
					ZAB = { exists = no }
					SUP = { exists = no }
					GRO = { exists = no }
					TYK = { exists = no }
					OSO = { exists = no }
					DAM = { exists = no }
					BRD = { exists = no }
					AUG = { exists = no }
					SEI = { exists = no }
					MON = { exists = no }
					BAG = { exists = no }
				}
		}

		ai_will_do = {
			factor = 100
		}
		
		days_remove = 10
		complete_effect = {
			ROOT = { add_timed_idea = {
				idea = cooldown
				days = 11
			}
		}
		}
		remove_effect  = {
			log = "[GetDateText]: Decision podlasie_SAN"
				if = { limit = { country_exists = SAN }
				declare_war_on = {
					target = SAN
					type = voivodeship_war
				}
				}
		}
		cancel_trigger = {
			has_war = yes
		}
	}

	podlasie_BEL = {

		icon = GFX_decision_generic_prepare_civil_war
		allowed = {
			OR = {
				original_tag = KON
				original_tag = TAT
				original_tag = BOM
				original_tag = SAN
				original_tag = LMZ
			}			
		}
		target_root_trigger = {
			NOT = { 
				has_idea = cooldown
			}
			has_war = no
			count_triggers = {
				amount = 16
					TNO = { exists = no }
					COL = { exists = no }
					ZAM = { exists = no }
					HMA = { exists = no }
					UHL = { exists = no }
					DRO = { exists = no }
					ZAB = { exists = no }
					SUP = { exists = no }
					GRO = { exists = no }
					TYK = { exists = no }
					OSO = { exists = no }
					DAM = { exists = no }
					BRD = { exists = no }
					AUG = { exists = no }
					SEI = { exists = no }
					MON = { exists = no }
					BAG = { exists = no }
			}
		}

		targets = {
			BEL
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}	
		}

		ai_will_do = {
			factor = 100
		}
		
		days_remove = 10
		complete_effect = {
			ROOT = { add_timed_idea = {
				idea = cooldown
				days = 11
			}
		}
		}
		remove_effect  = {
			log = "[GetDateText]: Decision podlasie_BEL"
				if = { limit = { country_exists = BEL }
				declare_war_on = {
					target = BEL
					type = voivodeship_war
				}
				}
		}
		cancel_trigger = {
			has_war = yes
		}
	}

	podlasie_KON = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {
				original_tag = BOM
				original_tag = TAT
				original_tag = BEL
				original_tag = SAN
				original_tag = LMZ
			}
		}
		target_root_trigger = {
			NOT = { 
				has_idea = cooldown
			}
			has_war = no
			count_triggers = {
				amount = 16
					TNO = { exists = no }
					COL = { exists = no }
					ZAM = { exists = no }
					HMA = { exists = no }
					UHL = { exists = no }
					DRO = { exists = no }
					ZAB = { exists = no }
					SUP = { exists = no }
					GRO = { exists = no }
					TYK = { exists = no }
					OSO = { exists = no }
					DAM = { exists = no }
					BRD = { exists = no }
					AUG = { exists = no }
					SEI = { exists = no }
					MON = { exists = no }
					BAG = { exists = no }
				}
		}

		targets = {
			KON
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}
		
		days_remove = 10
		complete_effect = {
			ROOT = { add_timed_idea = {
				idea = cooldown
				days = 11
			}
		}
		}
		remove_effect  = {
			log = "[GetDateText]: Decision podlasie_KON"
			if = {
				limit = { country_exists = KON }
				declare_war_on = {
					target = KON
					type = voivodeship_war
				}
			}
		}
		cancel_trigger = {
			has_war = yes
		}
	}

	podlasie_BOM = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {
				original_tag = KON
				original_tag = TAT
				original_tag = BEL
				original_tag = SAN
				original_tag = LMZ
			}
		}

		target_root_trigger = {
			NOT = { 
				has_idea = cooldown
				}
			has_war = no
			count_triggers = {
				amount = 16
					TNO = { exists = no }
					COL = { exists = no }
					ZAM = { exists = no }
					HMA = { exists = no }
					UHL = { exists = no }
					DRO = { exists = no }
					ZAB = { exists = no }
					SUP = { exists = no }
					GRO = { exists = no }
					TYK = { exists = no }
					OSO = { exists = no }
					DAM = { exists = no }
					BRD = { exists = no }
					AUG = { exists = no }
					SEI = { exists = no }
					MON = { exists = no }
					BAG = { exists = no }
				}
		}

		targets = {
			BOM
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}
		
		days_remove = 10
		complete_effect = {
			ROOT = { add_timed_idea = {
				idea = cooldown
				days = 11
			}
		}
		}
		remove_effect  = {
			log = "[GetDateText]: Decision podlasie_BOM"
				if = { limit = { country_exists = BOM }
				declare_war_on = {
					target = BOM
					type = voivodeship_war
				}
			}
		}
		cancel_trigger = {
			has_war = yes
		}
	}

	podlasie_LMZ = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {
				original_tag = BOM
				original_tag = KON
				original_tag = TAT
				original_tag = BEL
				original_tag = SAN
			}
		}
		target_root_trigger = {
			NOT = { 
				has_idea = cooldown
				}
			has_war = no
			count_triggers = {
				amount = 16
					TNO = { exists = no }
					COL = { exists = no }
					ZAM = { exists = no }
					HMA = { exists = no }
					MON = { exists = no }
					BAG = { exists = no }
					UHL = { exists = no }
					DRO = { exists = no }
					ZAB = { exists = no }
					SUP = { exists = no }
					GRO = { exists = no }
					TYK = { exists = no }
					OSO = { exists = no }
					DAM = { exists = no }
					BRD = { exists = no }
					AUG = { exists = no }
					SEI = { exists = no }
			}
		}

		targets = {
			LMZ
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}
		
		days_remove = 10
		complete_effect = {
			ROOT = { add_timed_idea = {
				idea = cooldown
				days = 11
			}
		}
		}
		remove_effect  = {
			log = "[GetDateText]: Decision podlasie_LMZ"
				if = { limit = { country_exists = LMZ }
				declare_war_on = {
					target = LMZ
					type = voivodeship_war
			}
			}
		}
		cancel_trigger = {
			has_war = yes
		}
	}
#PODLASIE - FAZA OSTATECZNA

podlasie_traktor_boss = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = BOM
			original_tag = TAT
			original_tag = BEL
			original_tag = SAN
			original_tag = LMZ
			original_tag = KON
		}
	}

	target_root_trigger = {
		has_war = no
		count_triggers = {
			amount = 5
				LMZ = { exists = no }
				BOM = { exists = no }
				KON = { exists = no }
				BEL = { exists = no }
				TAT = { exists = no }
				SAN = { exists = no }
		}
	}
	targets = {
		AGR SDL SAW WIL
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision podlasie_traktor_boss"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}

	cancel_trigger = {
		has_war = yes
	}
}


	last_battle_of_podlasie = {
		
		allowed = {
			OR = {
				original_tag = SDL
				original_tag = AGR
			}
		}

		target_root_trigger = {
			date > 2031.6.1
			has_war = no
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				is_podlasie = yes
			}
		}

		ai_will_do = {
			factor = 100
		}

		
		days_remove = 10
		remove_effect  = {
			log = "[GetDateText]: last_battle_of_podlasie"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}													
		}
	}

#██████╗░██╗░░░░░███╗░░██╗  ███╗░░░███╗░█████╗░███████╗░█████╗░░██╗░░░░░░░██╗░██████╗███████╗███████╗
#██╔══██╗██║░░░░░████╗░██║  ████╗░████║██╔══██╗╚════██║██╔══██╗░██║░░██╗░░██║██╔════╝╚════██║██╔════╝
#██████╔╝██║░░░░░██╔██╗██║  ██╔████╔██║███████║░░███╔═╝██║░░██║░╚██╗████╗██╔╝╚█████╗░░░███╔═╝█████╗░░
#██╔═══╝░██║░░░░░██║╚████║  ██║╚██╔╝██║██╔══██║██╔══╝░░██║░░██║░░████╔═████║░░╚═══██╗██╔══╝░░██╔══╝░░
#██║░░░░░███████╗██║░╚███║  ██║░╚═╝░██║██║░░██║███████╗╚█████╔╝░░╚██╔╝░╚██╔╝░██████╔╝███████╗███████╗
#╚═╝░░░░░╚══════╝╚═╝░░╚══╝  ╚═╝░░░░░╚═╝╚═╝░░╚═╝╚══════╝░╚════╝░░░░╚═╝░░░╚═╝░░╚═════╝░╚══════╝╚══════╝
#PÓŁNOCNOMAZOWIECKIE - FAZA PIERWSZA
 potezny_obajtek_masakruje_sierpc = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		original_tag = ORL
	}

	target_root_trigger = {
		date > 2027.11.15
		has_war = no
	}

	targets = {
		SRP
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	ai_will_do = {
		factor = 100
	}

	
	days_remove = 10
	remove_effect  = {
		log = "[GetDateText]: potezny_obajtek_masakruje_sierpc"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}

	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }

 potezny_sasin_masakruje_kurpie = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = SAS
		}

		target_root_trigger= {
			date > 2027.11.01
			has_war = no
		}

		targets = {
			KUR
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}
		
		days_remove = 10
		remove_effect  = {
			log = "[GetDateText]: Decision potezny_sasin_masakruje_kurpie"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}
		cancel_trigger = {
			has_war = yes
		}
	}

 potezny_sasin_masakruje_zawkrze = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = SAS
		}

		target_root_trigger = {
			date > 2027.11.15
			has_war = no
		}

		targets = {
			ZWK
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}
		
		days_remove = 10
		remove_effect  = {
			log = "[GetDateText]: Decision potezny_sasin_masakruje_zawkrze"
				declare_war_on = {
					target = FROM
					type = voivodeship_war
				}
		}
	}
 mariawici_civil_war = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = MAR
		}

		target_root_trigger = {
			date > 2027.11.01
			has_war = no
		}

		ai_will_do = {
			factor = 100
		}

		targets = {
			PLO
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		
		days_remove = 10
		remove_effect  = {
			log = "[GetDateText]: mariawici_civil_war"
				declare_war_on = {
					target = PLO
					type = voivodeship_war
				}
		}

		cancel_trigger = {
			has_war = yes
		}
	}

	potezny_obajtek_masakruje_gostynin = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = ORL
		}

		target_root_trigger = {
			date > 2027.11.01
			has_war = no
		}

		targets = {
			GOS
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}
		
		days_remove = 10
		remove_effect  = {
			log = "[GetDateText]: potezny_obajtek_masakruje_gostynin"
				declare_war_on = {
					target = FROM
					type = voivodeship_war
				}
		}
		cancel_trigger = {
			has_war = yes
		}
	}
#PÓŁNOCNOMAZOWIECKIE - FAZA DRUGA
#PÓŁNOCNOMAZOWIECKIE - FAZA OSTATECZNA
 north_mazovia_war = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {
				original_tag = POL
				original_tag = ORL
				original_tag = SAS
				original_tag = MAR
			}
		}

		target_root_trigger = {
			date > 2029.04.01
			has_war = no
		}
		targets = {
			ORL POL SAS KUR ZWK MAR PLO GOS SRP SCH
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}
		days_remove = 4
		remove_effect  = {
			log = "[GetDateText]: Decision north_mazovia_war"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}				
		}
		cancel_trigger = {
			has_war = yes
		}
	}

#░██╗░░░░░░░██╗░█████╗░██████╗░███╗░░░███╗██╗░█████╗░░░░░░░███╗░░░███╗░█████╗░███████╗██╗░░░██╗██████╗░██╗░░░██╗
#░██║░░██╗░░██║██╔══██╗██╔══██╗████╗░████║██║██╔══██╗░░░░░░████╗░████║██╔══██╗╚════██║██║░░░██║██╔══██╗╚██╗░██╔╝
#░╚██╗████╗██╔╝███████║██████╔╝██╔████╔██║██║███████║█████╗██╔████╔██║███████║░░███╔═╝██║░░░██║██████╔╝░╚████╔╝░
#░░████╔═████║░██╔══██║██╔══██╗██║╚██╔╝██║██║██╔══██║╚════╝██║╚██╔╝██║██╔══██║██╔══╝░░██║░░░██║██╔══██╗░░╚██╔╝░░
#░░╚██╔╝░╚██╔╝░██║░░██║██║░░██║██║░╚═╝░██║██║██║░░██║░░░░░░██║░╚═╝░██║██║░░██║███████╗╚██████╔╝██║░░██║░░░██║░░░
#░░░╚═╝░░░╚═╝░░╚═╝░░╚═╝╚═╝░░╚═╝╚═╝░░░░░╚═╝╚═╝╚═╝░░╚═╝░░░░░░╚═╝░░░░░╚═╝╚═╝░░╚═╝╚══════╝░╚═════╝░╚═╝░░╚═╝░░░╚═╝░░░
################################################################################## WARMIŃSKO-MAZURSKIE - ROSYJSKA INWAZJA NA MAZURY
 russian_invasion = {
	icon = GFX_decision_generic_prepare_civil_war
	allowed = {
		original_tag = ROS
	}
	available = {
		ROS = { num_of_controlled_states > 1 
			has_government = autocracy
		}
		has_war = no
		date > 2028.02.19
	}

	ai_will_do = {
		factor = 100
	}

	fire_only_once = yes
	days_remove = 5
	remove_effect  = {
		log = "[GetDateText]: Decision russian_invasion"
			if = { limit = { country_exists = TEU
							 ROOT = { NOT = { original_tag = TEU } } }
			declare_war_on = {
				target = TEU
				type = voivodeship_war
			}
			}
			if = { limit = { country_exists = MAL
							 ROOT = { NOT = { original_tag = MAL } } }
			declare_war_on = {
				target = MAL
				type = voivodeship_war
			}
			}
			if = { limit = { country_exists = PRU
							 ROOT = { NOT = { original_tag = PRU } } }
			declare_war_on = {
				target = PRU
				type = voivodeship_war
			}
			}	
			if = { limit = { country_exists = WRM
							 ROOT = { NOT = { original_tag = WRM } } }
			declare_war_on = {
				target = WRM
				type = voivodeship_war
			}
			}
			if = { limit = { country_exists = PZW
							 ROOT = { NOT = { original_tag = PZW } } }
			declare_war_on = {
				target = PZW
				type = voivodeship_war
			}
			}
			if = { limit = { country_exists = ELB
							 ROOT = { NOT = { original_tag = ELB } } }
			declare_war_on = {
				target = ELB
				type = voivodeship_war
			}
			}
			if = { limit = { country_exists = MRG
							 ROOT = { NOT = { original_tag = MRG } } }
			declare_war_on = {
				target = MRG
				type = voivodeship_war
			}
			}
			if = { limit = { country_exists = OLS
							 ROOT = { NOT = { original_tag = OLS } } }
			declare_war_on = {
				target = OLS
				type = voivodeship_war
			}
			}
			if = { limit = { country_exists = GAL
							 ROOT = { NOT = { original_tag = GAL } } }
			declare_war_on = {
				target = GAL
				type = voivodeship_war
			}
			}
			if = { limit = { country_exists = RAJ
							 ROOT = { NOT = { original_tag = RAJ } } }
			declare_war_on = {
				target = RAJ
				type = voivodeship_war
			}
			}
			if = { limit = { country_exists = LUB
							 ROOT = { NOT = { original_tag = LUB } } }
			declare_war_on = {
				target = LUB
				type = voivodeship_war
			}
			}
			RMA = {
				if = { limit = { country_exists = TEU
								 ROOT = { NOT = { original_tag = TEU } } }
				declare_war_on = {
					target = TEU
					type = voivodeship_war
				}
				}
				if = { limit = { country_exists = MAL
								 ROOT = { NOT = { original_tag = MAL } } }
				declare_war_on = {
					target = MAL
					type = voivodeship_war
				}
				}
				if = { limit = { country_exists = PRU
								 ROOT = { NOT = { original_tag = PRU } } }
				declare_war_on = {
					target = PRU
					type = voivodeship_war
				}
				}	
				if = { limit = { country_exists = WRM
								 ROOT = { NOT = { original_tag = WRM } } }
				declare_war_on = {
					target = WRM
					type = voivodeship_war
				}
				}
				if = { limit = { country_exists = PZW
								 ROOT = { NOT = { original_tag = PZW } } }
				declare_war_on = {
					target = PZW
					type = voivodeship_war
				}
				}
				if = { limit = { country_exists = ELB
								 ROOT = { NOT = { original_tag = ELB } } }
				declare_war_on = {
					target = ELB
					type = voivodeship_war
				}
				}
				if = { limit = { country_exists = MRG
								 ROOT = { NOT = { original_tag = MRG } } }
				declare_war_on = {
					target = MRG
					type = voivodeship_war
				}
				}
				if = { limit = { country_exists = OLS
								 ROOT = { NOT = { original_tag = OLS } } }
				declare_war_on = {
					target = OLS
					type = voivodeship_war
				}
				}
				if = { limit = { country_exists = GAL
								 ROOT = { NOT = { original_tag = GAL } } }
				declare_war_on = {
					target = GAL
					type = voivodeship_war
				}
				}
				if = { limit = { country_exists = RAJ
								 ROOT = { NOT = { original_tag = RAJ } } }
				declare_war_on = {
					target = RAJ
					type = voivodeship_war
				}
				}
				if = { limit = { country_exists = LUB
								 ROOT = { NOT = { original_tag = LUB } } }
				declare_war_on = {
					target = LUB
					type = voivodeship_war
				}
				}
			}
		}

}
	

 anti_russian_front_decision = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		original_tag = WRM 
	}

	available = {
		OR = {				
			date > 2028.04.01
			has_war_with = ROS
		}
	}
	
	ai_will_do = {
		factor = 100
	}
	fire_only_once = yes
	days_remove = 5
	remove_effect  = {
		log = "[GetDateText]: Decision anti_russian_front"
		if = {
			limit = {
				country_exists = ROS
				ROOT = {
					NOT = { original_tag = ROS }
				}
				ROOT = { 
					NOT = { has_war_with = ROS }
				}
			}
			log = "[GetDateText]: Deccing on [ROS.GetName]"
			declare_war_on = {
				target = ROS
				type = voivodeship_war
			}
		}
		log = "[GetDateText]: [WRM.GetName] creates Anti-Russian Front"
		create_faction_from_template = faction_template_regional
		set_faction_name = anti_russian_front
		log = "[GetDateText]: [WRM.GetName] adds [TEU.GetName] to Anti-Russian Front"
		add_to_faction = TEU
		log = "[GetDateText]: [WRM.GetName] adds [PZW.GetName] to Anti-Russian Front"
		add_to_faction = PZW
		log = "[GetDateText]: [WRM.GetName] adds [MRG.GetName] to Anti-Russian Front"
		add_to_faction = MRG
		log = "[GetDateText]: [WRM.GetName] adds [ELB.GetName] to Anti-Russian Front"
		add_to_faction = ELB
		every_country = {
			limit = {
				is_ai = no
			}
			news_event = { 
				id = rosja.3
			}
		}
	}
 }

 ask_for_truce = {
	allowed = {
		original_tag = ROS
	}
	available = {
		OR = {
			NOT = {
				ROS = {
					controls_state = 878
				}
			}
			NOT = {
				ROS = {
					controls_state = 880
				}
			}
			NOT = {
				ROS = {
					controls_state = 881
				}
			}
			NOT = {
				ROS = {
					controls_state = 891
				}
			}
			NOT = {
				ROS = {
					controls_state = 893
				}
			}
		}
		date < 2033.03.01
	}
	ai_will_do = {
		factor = 100
	}
	complete_effect = {
		log = "[GetDateText]: PUTIN SIĘ ZESRAŁ I PROSI O ROZEJM"
	}
	days_remove = 2
	fire_only_once = yes
	remove_effect = {
		log = "[GetDateText]: ITS NOT OVER YET FOR RUSSIA BROS"
		if = {
			limit = {
				NOT = {
					ROS = {
						controls_state = 40
					}
				}
			}
			ELB = {
				transfer_state = 40
				add_state_core = 40
			}
			40 = {
				add_core_of = ELB
				transfer_state_to = ELB
				remove_core_of = RMA
			}
		}
		if = {
			limit = {
				NOT = {
					ROS = {
						controls_state = 993
					}
				}
			}
			ELB = {
				transfer_state = 993
				add_state_core = 993
			}
			993 = {
				add_core_of = ELB
				transfer_state_to = TEU
				remove_core_of = RMA
			}
		}
		if = {
			limit = {
				NOT = {
					ROS = {
						controls_state = 42
					}
				}
			}
			WRM = {
				transfer_state = 42
				add_state_core = 42
			}
			42 = {
				add_core_of = WRM
				transfer_state_to = WRM
				remove_core_of = RMA
			}
		}
		if = {
			limit = {
				NOT = {
					ROS = {
						controls_state = 47
					}
				}
			}
			WRM = {
				transfer_state = 47
				add_state_core = 47
			}
			47 = {
				add_core_of = WRM
				transfer_state_to = WRM
				remove_core_of = RMA
			}
		}
		if = {
			limit = {
				NOT = {
					ROS = {
						controls_state = 51
					}
				}
			}
			MRG = {
				transfer_state = 51
				add_state_core = 51
			}
			51 = {
				add_core_of = MRG
				transfer_state_to = MRG
				remove_core_of = RMA
			}
		}
		if = {
			limit = {
				NOT = {
					ROS = {
						controls_state = 994
					}
				}
			}
			MRG = {
				transfer_state = 994
				add_state_core = 994
			}
			994 = {
				add_core_of = MRG
				transfer_state_to = MRG
				remove_core_of = RMA
			}
		}
		if = {
			limit = {
				NOT = {
					ROS = {
						controls_state = 46
					}
				}
			}
			PZW = {
				transfer_state = 46
				add_state_core = 46
			}
			46 = {
				add_core_of = PZW
				transfer_state_to = PZW
				remove_core_of = RMA
			}
		}
		if = {
			limit = {
				NOT = {
					ROS = {
						controls_state = 875
					}
				}
			}
			PZW = {
				transfer_state = 875
				add_state_core = 875
			}
			875 = {
				add_core_of = PZW
				transfer_state_to = PZW
				remove_core_of = RMA
			}
		}
		
		white_peace = TEU
		log = "[GetDateText]: [ROOT.GetName] WPs with [TEU.GetName]"
		white_peace = WRM
		log = "[GetDateText]: [ROOT.GetName] WPs with [WRM.GetName]"
		white_peace = MAL
		log = "[GetDateText]: [ROOT.GetName] WPs with [MAL.GetName]"
		white_peace = PRU
		log = "[GetDateText]: [ROOT.GetName] WPs with [PRU.GetName]"
		white_peace = PZW
		log = "[GetDateText]: [ROOT.GetName] WPs with [PZW.GetName]"
		white_peace = MRG
		log = "[GetDateText]: [ROOT.GetName] WPs with [MRG.GetName]"
		white_peace = ELB
		log = "[GetDateText]: [ROOT.GetName] WPs with [PZW.GetName]"
		white_peace = LUB
		log = "[GetDateText]: [ROOT.GetName] WPs with [PZW.GetName]"
		white_peace = OLS
		log = "[GetDateText]: [ROOT.GetName] WPs with [PZW.GetName]"
		white_peace = RAJ
		log = "[GetDateText]: [ROOT.GetName] WPs with [PZW.GetName]"
		white_peace = GAL
		log = "[GetDateText]: [ROOT.GetName] WPs with [PZW.GetName]"
		PZW = { leave_faction = yes }
		log = "[GetDateText]: [PZW.GetName] quits faction"
		TEU = { leave_faction = yes }
		log = "[GetDateText]: [TEU.GetName] quits faction"
		ELB = { leave_faction = yes }
		log = "[GetDateText]: [ELB.GetName] quits faction"
		MRG = { leave_faction = yes }
		log = "[GetDateText]: [MRG.GetName] quits faction"
		WRM = { dismantle_faction = yes }
		log = "[GetDateText]: [WRM.GetName] dismantles faction"
		ROS = {
			remove_wargoal = {
				type = all
				target = TEU
			}
			remove_wargoal = {
				type = all
				target = LUB
			}
		}	
		every_state = {
			limit = {
				OR = {
					is_warmia_and_masuria_state = yes
				}
			}
			teleport_armies = {
				limit = {
					original_tag = TEU
					is_ai = yes
				}
			}
		}
		ROS = {
		random_list = {
				1 = {
					every_country = {
						limit = {  is_ai = no  }
						news_event = { 
							id = rosja.4
							days = 9
						}
					}
				}
				1 = {
					every_country = {
						limit = {  is_ai = no  }
						news_event = { 
							id = rosja.5
							days = 2
						}
					}
				}
			}
		}
	}
 }
################################################################################## WARMIŃSKO-MAZURSKIE - KRÓLEWSKA WOJNA DOMOWA

# nic na razie

################################################################################## WARMIŃSKO-MAZURSKIE - FAZA PIERWSZA

lake_war0 = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = RAJ
			original_tag = GAL
		}		
	}

	target_root_trigger = {
		date > 2028.04.01
		NOT = { has_war_with = ROS }
	}
	targets = {
		RAJ GAL MRG
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}
	war_with_target_on_complete = yes

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 10
	remove_effect  = {
		log = "[GetDateText]: Decision lake_war"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = { 
			has_war = yes
		}
	}
}

lake_war1 = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = PZW
			original_tag = WRM
		}		
	}

	target_root_trigger = {
		date > 2028.04.01
		NOT = { has_war_with = ROS }
	}
	targets = {
		PZW WRM MRG
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 20
	remove_effect  = {
		log = "[GetDateText]: Decision lake_war"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = { 
			has_war = yes
		}
	}
}

great_teutonic_war = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {	
		OR = {
			original_tag = MAL
			original_tag = PRU
			original_tag = ELB
		}
	}
	
	available = {
			date > 2028.05.01
			NOT = { has_war_with = ROS }
			country_exists = TEU
			country_exists = LUB
	}
	ai_will_do = {
		factor = 100
	}
	fire_only_once = yes
	days_remove = 13
	remove_effect  = {
		log = "[GetDateText]: Decision great_teutonic_war"
		MAL = {
			create_faction_from_template = faction_template_generic
			set_faction_name = anti_teuton_league
			add_to_faction = ELB
			add_to_faction = PRU
			declare_war_on = {
				target = TEU
				type = voivodeship_war
			}
			declare_war_on = {
				target = LUB
				type = voivodeship_war
			}
		}
		ELB = {
			declare_war_on = {
				target = TEU
				type = voivodeship_war
			}
			declare_war_on = {
				target = LUB
				type = voivodeship_war
			}
		}
		PRU = {
			declare_war_on = {
				target = TEU
				type = voivodeship_war
			}
			declare_war_on = {
				target = LUB
				type = voivodeship_war
			}
		}
		TEU = {
			create_faction_from_template = faction_template_generic
			set_faction_name = teutonic_alliance
			add_to_faction = LUB
		}
	}
	cancel_trigger = {
		ROOT = { 
			has_war = yes
		}
	}
}

################################################################################## WARMIŃSKO-MAZURSKIE - FAZA DRUGA

lake_war2 = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {	
		OR = {
			original_tag = PZW
			original_tag = WRM
			original_tag = RAJ
			original_tag = GAL
		}
	}

	target_root_trigger = {
		date > 2029.07.01
		NOT = { has_war_with = ROS }
		has_war = no
	}
	targets = {
		PZW WRM RAJ GAL
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	ai_will_do = {
		factor = 100
	}
	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision lake_war2"
		declare_war_on = {
			target = FROM
			type = voivodeship_war

		}
	}
		cancel_trigger = {
			ROOT = { 
				has_war = yes
			}
		}
}

	warmia_support = {
    
		icon = GFX_decision_generic_prepare_civil_war
			
		allowed = {
			original_tag = OLS
		}

		available = {
			date > 2028.10.01
			NOT = { has_war_with = ROS }
			country_exists = TEU
			country_exists = LUB
		}

		ai_will_do = {
			factor = 100
		}
		fire_only_once = yes
		days_remove = 10
		remove_effect  = {
			log = "[GetDateText]: Decision warmia_support"
			OLS = {
				declare_war_on = {
					target = TEU
					type = voivodeship_war
				}
				declare_war_on = {
					target = LUB
					type = voivodeship_war
				}
				
			MAL = {
				add_to_faction = OLS
			}
			}
		}
		cancel_trigger = {
			ROOT = { 
				has_war = yes
			}
		}
	}

	dissolve_front = {
    
		icon = GFX_decision_generic_prepare_civil_war
			
		allowed = {
			OR = {
			original_tag = MAL
			original_tag = PRU
			original_tag = ELB
			}
		}

		visible = {
			date > 2028.05.02
			NOT = { country_exists = TEU }
			NOT = { country_exists = LUB }
		}

		ai_will_do = {
			factor = 100
		}

		remove_effect  = {
			log = "[GetDateText]: Decision anti-teuton league falls"
			OLS = {
				dismantle_faction = yes
				leave_faction = yes
			}
			ELB = {
				dismantle_faction = yes
				leave_faction = yes
			}
			MAL = {
				dismantle_faction = yes
				leave_faction = yes
			}
			PRU = {
				dismantle_faction = yes
				leave_faction = yes
			}
		}
	}

	mal_elb_war = {
    
		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {
				original_tag = MAL
				original_tag = ELB
			}
		}

		target_root_trigger = {
			date > 2028.10.01
			NOT = { has_war_with = ROS }
			NOT = { country_exists = TEU }
			NOT = { country_exists = LUB }
			has_war = no
		}
		targets = {
			MAL ELB
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}
		
		days_remove = 17
		remove_effect  = {
			log = "[GetDateText]: Decision great_teutonic_war2"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}
		cancel_trigger = {
			ROOT = { 
				has_war = yes
			}
		}
	}

	pru_ols_war = {
    
		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {
				original_tag = OLS
				original_tag = PRU
			}
		}

		target_root_trigger = {
			date > 2028.10.01
			NOT = { has_war_with = ROS }
			NOT = { country_exists = TEU }
			NOT = { country_exists = LUB }
			has_war = no
		}
		targets = {
			OLS PRU
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}
		
		days_remove = 23
		remove_effect  = {
			log = "[GetDateText]: Decision great_teutonic_war2"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}
		cancel_trigger = {
			ROOT = { 
				has_war = yes
			}
		}
	}

	teuton_warmia_war = {
    
		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {
			original_tag = TEU
			original_tag = OLS
			}
		}

		target_root_trigger = {
			date > 2029.01.01
			NOT = { has_war_with = ROS }
			NOT = { has_war_with = OLS }
			NOT = { country_exists = ELB }
			NOT = { country_exists = MAL }
			NOT = { country_exists = PRU }
			has_war = no
		}
		targets = {
			TEU OLS LUB
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 20
		remove_effect  = {
			log = "[GetDateText]: Decision teuton_warmia_war"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}
		cancel_trigger = {
			ROOT = { 
				has_war = yes
			}
		}
	}
	

	warmia_reclamation = {
    
		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {
			original_tag = OLS
			original_tag = ELB
			original_tag = MAL
			original_tag = PRU
			original_tag = TEU
			}
		}

		target_root_trigger = {
			date > 2029.01.01
			NOT = { has_war_with = ROS }
			NOT = { country_exists = TEU }
			NOT = { country_exists = LUB }
			has_war = no
		}
		targets = {
			OLS ELB MAL PRU TEU LUB
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		
		days_remove = 7
		remove_effect  = {
			log = "[GetDateText]: Decision warmia_reclamation"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}
	}

################################################################################## WARMIŃSKO-MAZURSKIE - FAZA OSTATECZNA

warmia_mazury_lastwar = {
    
	icon = GFX_decision_generic_prepare_civil_war
		
	allowed = {		
		OR = {
			original_tag = OLS
			original_tag = ELB
			original_tag = MAL
			original_tag = PRU
			original_tag = TEU
			original_tag = WRM
			original_tag = PZW
			original_tag = GAL
			original_tag = RAJ
		}
	}

	target_root_trigger = {
		date > 2029.06.01
		NOT = { has_war_with = ROS }
		has_war = no
	}
	targets = {
		OLS ELB MAL PRU TEU WRM PZW GAL RAJ LUB MRG
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	ai_will_do = {
		factor = 100
	}

	
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision warmia_mazury_lastwar"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
}


#██████╗░░█████╗░███╗░░░███╗░█████╗░██████╗░░██████╗██╗░░██╗██╗███████╗
#██╔══██╗██╔══██╗████╗░████║██╔══██╗██╔══██╗██╔════╝██║░██╔╝██║██╔════╝
#██████╔╝██║░░██║██╔████╔██║██║░░██║██████╔╝╚█████╗░█████═╝░██║█████╗░░
#██╔═══╝░██║░░██║██║╚██╔╝██║██║░░██║██╔══██╗░╚═══██╗██╔═██╗░██║██╔══╝░░
#██║░░░░░╚█████╔╝██║░╚═╝░██║╚█████╔╝██║░░██║██████╔╝██║░╚██╗██║███████╗
#╚═╝░░░░░░╚════╝░╚═╝░░░░░╚═╝░╚════╝░╚═╝░░╚═╝╚═════╝░╚═╝░░╚═╝╚═╝╚══════╝
#POMORSKIE - FAZA PIERWSZA
 POMGDA_gdansk_war = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		original_tag = GDA
	}

	target_root_trigger = {
		date > 2028.03.05
		has_war = no
	}

	ai_will_do = {
		factor = 100
	}

	targets = {
		MPR BOL
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}		
	}
	
	days_remove = 7

	war_with_target_on_complete = yes

	remove_effect  = {
		log = "[GetDateText]: Decision POMGDA_gdansk_war"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }

 POMGDA_gdansk_war_rozpad_sojuszu = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = MPR
			original_tag = BOL
		}
	}

	target_root_trigger = {
		date > 2028.03.05
		NOT = {
			country_exists = GDA
		}
		has_war = no
	}

	ai_will_do = {
		factor = 100
	}

	targets = {
		MPR BOL
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}		
	}

	days_remove = 7

	war_with_target_on_complete = yes

	remove_effect  = {
		log = "[GetDateText]: Decision POMGDA_gdansk_war"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }

 POMGDA_kociewie_war = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		original_tag = LOT
	}

	target_root_trigger = {
		date > 2028.03.12
		has_war = no
	}

	ai_will_do = {
		factor = 100
	}

	targets = {
		LTS
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}		
	}

	

	days_remove = 7

	war_with_on_remove = LTS

	remove_effect  = {
		log = "[GetDateText]: Decision POMGDA_kociewie_war"
		declare_war_on = {
			target = LTS
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }

 POMGDA_chojnice_war = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		original_tag = CHG
	}

	target_root_trigger = {
		date > 2028.03.18
		has_war = no
	}

	ai_will_do = {
		factor = 100
	}

	targets = {
		CZL
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}		
	}

	

	days_remove = 7

	war_with_on_remove = CZL

	remove_effect  = {
		log = "[GetDateText]: Decision POMGDA_chojnice_war"
		declare_war_on = {
			target = CZL
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }

 POMGDA_kashubia_war = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = KSZ
			original_tag = KSC
		}
	}

	target_root_trigger = {
		date > 2028.03.25
		has_war = no
	}

	ai_will_do = {
		factor = 100
	}

	targets = {
		KSZ KSC
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}

	

	days_remove = 7

	war_with_target_on_complete = yes

	remove_effect  = {
		log = "[GetDateText]: Decision POMGDA_kashubia_war"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }

 POMGDA_WHR_OWS_war = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = OWS
			original_tag = WHR
		}
	}

	target_root_trigger = {
		date > 2028.03.22
		has_war = no
	}

	ai_will_do = {
		factor = 100
	}

	targets = {
		OWS WHR
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}				
	}

	

	days_remove = 7

	war_with_target_on_complete = yes

	remove_effect  = {
		log = "[GetDateText]: Decision POMGDA_WHR_OWS_war"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }

 POMGDA_korsarz_tan_viet_war = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		original_tag = KOR
	}

	target_root_trigger = {
		date > 2028.03.10
		has_war = no
	}

	ai_will_do = {
		factor = 100
	}

	targets = {
		TVI
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	

	days_remove = 7

	war_with_on_remove = TVI

	remove_effect  = {
		log = "[GetDateText]: Decision POMGDA_korsarz_tan_viet_war"
		declare_war_on = {
			target = TVI
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }

 POMGDA_slovince_invasion_war = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = GAY
			original_tag = RLE
			original_tag = HEL
		}
	}

	target_root_trigger = {
		date > 2028.03.29
		has_war = no
	}

	ai_will_do = {
		factor = 100
	}

	targets = {
		SVG
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}		
	}

	

	days_remove = 7

	war_with_on_remove = SVG

	remove_effect  = {
		log = "[GetDateText]: Decision POMGDA_slovince_invasion_war"
		declare_war_on = {
			target = SVG
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }

 POMGDA_HEL_OWS_war = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {	
		OR = {
			original_tag = HEL
			original_tag = OWS
		}
	}

	target_root_trigger = {
		date > 2028.05.22
		has_war = no
	}

	ai_will_do = {
		factor = 100
	}

	targets = {
		HEL OWS
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}			
	}

	

	days_remove = 7

	war_with_target_on_complete = yes

	remove_effect  = {
		log = "[GetDateText]: Decision POMGDA_HEL_OWS_war"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }

#POMORSKIE - FAZA DRUGA
 POMGDA_borowiacy_kociewie_war = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = CZL
			original_tag = LOT
		}
	}

	target_root_trigger = {
		date > 2028.09.01
		has_war = no
	}
	targets = {
		CZL LOT LTS CHG
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	ai_will_do = {
		factor = 100
	}

	

	days_remove = 7

	war_with_target_on_complete = yes

	remove_effect  = {
		log = "[GetDateText]: Decision POMGDA_borowiacy_kociewie_war"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }

 POMGDA_helows_kaszuby_war = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = HEL
			original_tag = OWS
			original_tag = KSZ
			original_tag = KSC
		}
	}

	target_root_trigger = {
		date > 2028.09.01
		has_war = no
	}
	targets = {
		HEL OWS KSZ KSC
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	ai_will_do = {
		factor = 100
	}

	days_remove = 7

	war_with_target_on_complete = yes

	remove_effect  = {
		log = "[GetDateText]: Decision POMGDA_helows_kaszuby_war"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }

 POMGDA_slupsk_lebork_war = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = GAY
			original_tag = RLE
		}
	}

	target_root_trigger = {
		date > 2028.09.01
		has_war = no
	}
	targets = {
		GAY RLE
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	ai_will_do = {
		factor = 100
	}

	days_remove = 7

	war_with_target_on_complete = yes

	remove_effect  = {
		log = "[GetDateText]: Decision POMGDA_slupsk_lebork_war"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }

 POMGDA_korsarze_invasion_war = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		original_tag = KOR
	}

	target_root_trigger = {
		date > 2028.09.01
		has_war = no
	}
	targets = {
		GDA BOL MPR
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	ai_will_do = {
		factor = 100
	}

	days_remove = 7

	war_with_target_on_complete = yes

	remove_effect  = {
		log = "[GetDateText]: Decision POMGDA_korsarze_invasion_war"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }

 POMGDA_gdansk_invades_korsarz_war = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = GDA
			original_tag = BOL
			original_tag = MPR
		}
	}

	target_root_trigger = {
		date > 2028.09.01
		has_war = no
	}
	targets = {
		KOR
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	ai_will_do = {
		factor = 100
	}


	days_remove = 7

	war_with_target_on_complete = yes

	remove_effect  = {
		log = "[GetDateText]: Decision POMGDA_gdansk_invades_korsarz_war"
		declare_war_on = {
			target = KOR
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }

#POMORSKIE - FAZA OSTATECZNA
 POMGDA_pomorze_jednoczenie_war = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		AND = {
			is_pomerelia = yes
			NOT = {
				OR = {
					original_tag = GDY
					original_tag = ARK
				}
			}	
		}	
	}

	target_root_trigger = {
		AND = {
			has_war = no
			is_subject = no
			OR = {
				count_triggers = {
					amount = 8
					HEL = { exists = no }
					OWS = { exists = no }
					GAY = { exists = no }
					RLE = { exists = no }
					KSZ = { exists = no }
					KSC = { exists = no }	
					GDA = { exists = no }
					BOL = { exists = no }
					MPR = { exists = no }
					KOR = { exists = no }
					CZL = { exists = no }
					LOT = { exists = no }	
				}
				date > 2029.06.01
			}
			NOT = { 
				has_idea = war_declaration_cooldown
			}
		}
	}

	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	war_with_target_on_complete = yes

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
			is_pomerelia = yes
		}
	}

	complete_effect = {
		#set_country_flag = getting_ready_to_war_pomerelia
		log = "[GetDateText]: [ROOT.GetName] is getting ready to war in Pomorze..."
		add_timed_idea = {
			idea = war_declaration_cooldown
			days = 20
		}
	}
	remove_effect  = {
		log = "[GetDateText]: Decision POMGDA_pomorze_jednoczenie_war"
		#clr_country_flag = getting_ready_to_war_pomerelia
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }

#██╗░░██╗██╗░░░██╗░░░░░██╗░█████╗░░██╗░░░░░░░██╗░██████╗██╗░░██╗██╗███████╗
#██║░██╔╝██║░░░██║░░░░░██║██╔══██╗░██║░░██╗░░██║██╔════╝██║░██╔╝██║██╔════╝
#█████═╝░██║░░░██║░░░░░██║███████║░╚██╗████╗██╔╝╚█████╗░█████═╝░██║█████╗░░
#██╔═██╗░██║░░░██║██╗░░██║██╔══██║░░████╔═████║░░╚═══██╗██╔═██╗░██║██╔══╝░░
#██║░╚██╗╚██████╔╝╚█████╔╝██║░░██║░░╚██╔╝░╚██╔╝░██████╔╝██║░╚██╗██║███████╗
#╚═╝░░╚═╝░╚═════╝░░╚════╝░╚═╝░░╚═╝░░░╚═╝░░░╚═╝░░╚═════╝░╚═╝░░╚═╝╚═╝╚══════╝
#KUJAWSKIE - FAZA PIERWSZA

ama_gyp_war = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = AMA
		}

		target_root_trigger = {
			date > 2028.10.22
			has_war = no
		}

		targets = {
			GYP
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7
		war_with_on_remove = GYP
		remove_effect  = {
			log = "[GetDateText]: Decision ama_gyp_war"
			declare_war_on = {
				target = GYP
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}

ino_first_war = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = INO
		}

		target_root_trigger = {
			date > 2028.9.28
			has_war = no
		}

		targets = {
			UFO SKJ
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}
		days_remove = 7
		war_with_on_remove = UFO
		war_with_on_remove = SKJ
		remove_effect  = {
			log = "[GetDateText]: Decision ino_first_war"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}

ufo_followup = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = UFO
		}

		target_root_trigger = {
			date > 2028.10.18
			OR = {
			country_exists = INO
			country_exists = SKJ
			}
			has_war = no
		}

		targets = {
			INO SKJ
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7
		war_with_on_remove = INO
		war_with_on_remove = SKJ
		remove_effect  = {
			log = "[GetDateText]: Decision ufo_followup"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}


krajna_setup_war = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = KUJ
		}

		target_root_trigger = {
			date > 2028.10.12
			has_war = no
		}

		ai_will_do = {
			factor = 100
		}

		targets = {
			NED FMT
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		days_remove = 7
		war_with_on_remove = FMT
		war_with_on_remove = NED
		remove_effect  = {
			log = "[GetDateText]: Decision krajna_setup_war"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}

swiecie_cleanup = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = BYD
		}

		target_root_trigger = {
			date > 2028.10.12
			SWC = {
			is_in_faction = no
			is_subject = no
			}
			has_war = no
		}

		targets = {
			SWC
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7
		war_with_on_remove = SWC
		remove_effect  = {
			log = "[GetDateText]: Decision swiecie_cleanup"
			declare_war_on = {
				target = SWC
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}

swiecie_cleanup_kuj = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = KUJ
		}

		target_root_trigger = {
			date > 2028.10.12
			BYD = {
			exists = no
			}
			SWC = {
			is_in_faction = no
			is_subject = no
			}
			has_war = no
		}

		targets = {
			SWC
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}
		days_remove = 7
		war_with_on_remove = SWC
		remove_effect  = {
			log = "[GetDateText]: Decision swiecie_cleanup_kuj"
			declare_war_on = {
				target = SWC
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}

gru_brodnica = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = GRU
		}

		target_root_trigger = {
			date > 2028.09.29
			has_war = no
		}

		targets = {
			GAS
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7
		war_with_on_remove = GAS
		remove_effect  = {
			log = "[GetDateText]: Decision gru_brodnica"
			declare_war_on = {
				target = GAS
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}

hog_brodnica = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = HOG
		}

		target_root_trigger = {
			date > 2028.10.01
			has_war = no
		}

		targets = {
			GAS
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}
		ai_will_do = {
			factor = 100
		}
		days_remove = 7
		war_with_on_remove = GAS
		remove_effect  = {
			log = "[GetDateText]: Decision hog_brodnica"
			declare_war_on = {
				target = GAS
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}


#KUJAWSKIE - FAZA DRUGA

siege_bydgoszczy = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = KUJ
		}

		target_root_trigger = {
			date > 2029.02.15
			has_war = no
		}

		targets = {
			BYD
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}
		days_remove = 7
		war_with_target_on_complete = yes
		remove_effect  = {
			log = "[GetDateText]: Decision siege_bydgoszczy"
			declare_war_on = {
				target = BYD
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}

bydzia_atakuje = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = BYD
		}

		target_root_trigger = {
			date > 2029.02.15
			has_war = no
		}

		targets = {
			KUJ FMT NED PLK
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}
		days_remove = 7
		war_with_target_on_complete = yes
		remove_effect  = {
			log = "[GetDateText]: Decision bydzia_atakuje"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}

kobiecy_antyklerykalizm = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = AMA
		}

		target_root_trigger = {
			date > 2029.02.18
			has_war = no
		}

		targets = {
			INO UFO SKJ
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}
		days_remove = 7
		war_with_target_on_complete = yes
		remove_effect  = {
			log = "[GetDateText]: Decision kobiecy_antyklerykalizm"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}

pobicie_kobiet = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {
			original_tag = UFO
			original_tag = INO
			}			
		}

		target_root_trigger = {
			date > 2029.02.18
			has_war = no
		}

		targets = {
			AMA GYP
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}
		days_remove = 7
		war_with_target_on_complete = yes
		remove_effect  = {
			log = "[GetDateText]: Decision pobicie_kobiet"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}

wojenka_na_prawicy_wisly_hog_tor = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = HOG
		}

		target_root_trigger = {
			date > 2029.01.29
			has_war = no
		}

		ai_will_do = {
			factor = 100
		}

		targets = {
			TOR
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		days_remove = 7
		war_with_target_on_complete = yes
		remove_effect  = {
			log = "[GetDateText]: Decision wojenka_na_prawicy_wisly_hog_tor"
			declare_war_on = {
				target = TOR
				type = voivodeship_war
			}
		}
		cancel_trigger = {
		ROOT = {
				has_war = yes
			}
		}
	}

wojenka_na_prawicy_wisly_hog_gru = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = HOG
		}

		target_root_trigger = {
			date > 2029.01.29
			has_war = no
		}

		ai_will_do = {
			factor = 100
		}

		targets = {
			GRU
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		days_remove = 7
		war_with_target_on_complete = yes
		remove_effect  = {
			log = "[GetDateText]: Decision wojenka_na_prawicy_wisly_hog_gru"
			
			declare_war_on = {
				target = GRU
				type = voivodeship_war
			}
		}
		cancel_trigger = {
		ROOT = {
				has_war = yes
			}
		}
	}

wojenka_na_prawicy_wisly_gru_tor = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = GRU
		}

		target_root_trigger = {
			date > 2029.01.29
			has_war = no
		}

		targets = {
			TOR
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7
		war_with_target_on_complete = yes
		remove_effect  = {
			log = "[GetDateText]: Decision wojenka_na_prawicy_wisly_gru_tor"
			
			declare_war_on = {
				target = TOR
				type = voivodeship_war
			}
		}
		cancel_trigger = {
		ROOT = {
				has_war = yes
			}
		}
	}

wojenka_na_prawicy_wisly_gru_hog = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = GRU
		}

		target_root_trigger = {
			date > 2029.01.29
			has_war = no
		}

		ai_will_do = {
			factor = 100
		}

		targets = {
			HOG
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		days_remove = 7
		war_with_target_on_complete = yes
		remove_effect  = {
			log = "[GetDateText]: Decision wojenka_na_prawicy_wisly_gru_hog"
			
			declare_war_on = {
				target = HOG
				type = voivodeship_war
			}
		}
		cancel_trigger = {
		ROOT = {
				has_war = yes
			}
		}
	}



#KUJAWSKIE - FAZA OSTATECZNA

kujawsko_krajenska = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {
			original_tag = INO
			original_tag = UFO
			original_tag = AMA
			}			
		}

		target_root_trigger = {
			date > 2029.06.23
			has_war = no
		}
		targets = {
			BYD SWC KUJ PLK FMT NED
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}
		days_remove = 7
		war_with_target_on_complete = yes
		remove_effect  = {
			log = "[GetDateText]: Decision kujawsko_krajenska"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}

chelminsko_krajenska = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {
			original_tag = GRU
			original_tag = TOR
			original_tag = HOG
			}			
		}

		target_root_trigger = {
			date > 2029.06.23
			has_war = no
		}
		targets = {
			BYD SWC KUJ PLK FMT NED
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7
		war_with_target_on_complete = yes
		remove_effect  = {
			log = "[GetDateText]: Decision chelminsko_krajenska"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}

kujawsko_chelminska = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {
				original_tag = INO
				original_tag = UFO
				original_tag = AMA
			}			
		}

		target_root_trigger = {
			date > 2029.06.23
			has_war = no
		}
		targets = {
			TOR BRO GRU HOG GAS
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}
		war_with_target_on_complete = yes
		days_remove = 7
		remove_effect  = {
			log = "[GetDateText]: kujawsko_chelminska"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}

krajensko_chelminska = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {
			original_tag = BYD
			original_tag = KUJ
			}			
		}

		target_root_trigger = {
			date > 2029.06.23
			has_war = no
		}
		targets = {
			TOR BRO GRU HOG GAS
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7
		war_with_target_on_complete = yes
		remove_effect  = {
			log = "[GetDateText]: krajensko_chelminska"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}

krajensko_kujawska = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {
			original_tag = BYD
			original_tag = KUJ
			}			
		}

		target_root_trigger = {
			date > 2029.06.23
			has_war = no
		}
		targets = {
			AMA GYP UFO INO SKJ
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}
		war_with_target_on_complete = yes

		ai_will_do = {
			factor = 100
		}

		days_remove = 7

		remove_effect  = {
			log = "[GetDateText]: krajensko_kujawska"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}

chelminsko_kujawska = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {
			original_tag = HOG
			original_tag = GRU
			}			
		}

		target_root_trigger = {
			date > 2029.06.23
			has_war = no
		}
		targets = {
			AMA GYP UFO INO SKJ
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7
		war_with_target_on_complete = yes
		remove_effect  = {
			log = "[GetDateText]: krajensko_kujawska"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}



#███████╗░█████╗░░█████╗░██╗░░██╗░░░  ██████╗░░█████╗░███╗░░░███╗░█████╗░██████╗░░██████╗██╗░░██╗██╗███████╗
#╚════██║██╔══██╗██╔══██╗██║░░██║░░░  ██╔══██╗██╔══██╗████╗░████║██╔══██╗██╔══██╗██╔════╝██║░██╔╝██║██╔════╝
#░░███╔═╝███████║██║░░╚═╝███████║░░░  ██████╔╝██║░░██║██╔████╔██║██║░░██║██████╔╝╚█████╗░█████═╝░██║█████╗░░
#██╔══╝░░██╔══██║██║░░██╗██╔══██║░░░  ██╔═══╝░██║░░██║██║╚██╔╝██║██║░░██║██╔══██╗░╚═══██╗██╔═██╗░██║██╔══╝░░
#███████╗██║░░██║╚█████╔╝██║░░██║██╗  ██║░░░░░╚█████╔╝██║░╚═╝░██║╚█████╔╝██║░░██║██████╔╝██║░╚██╗██║███████╗
#╚══════╝╚═╝░░╚═╝░╚════╝░╚═╝░░╚═╝╚═╝  ╚═╝░░░░░░╚════╝░╚═╝░░░░░╚═╝░╚════╝░╚═╝░░╚═╝╚═════╝░╚═╝░░╚═╝╚═╝╚══════╝
#ZACHODNIOPOMORSKIE - FAZA PIERWSZA
	zachpom_EKW_vs_FRE = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {
				original_tag = EKW				
			}			
		}

		target_root_trigger = {
			has_war = no
			date > 2028.03.03
		}

		targets = {
			FRE
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7

		remove_effect  = {
			log = "[GetDateText]: Decision zachpom_EKW_vs_FRE"
			declare_war_on = {
				target = FRE
				type = voivodeship_war
			}
		}
		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}


	zachpom_JAS_vs_KTOKURWA = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = JAS
		}

		available = {
			#ROOT = { any_neighbor_country = { original_tag = EKW } }
			has_war = no
			date > 2028.01.03
		}

		ai_will_do = {
			factor = 100
		}
		fire_only_once = yes
		days_remove = 7
		#war_with_on_remove = KOL
		remove_effect  = {
			log = "[GetDateText]: Decision zachpom_JAS_vs_KTOKURWA"
				country_event = { id = jaszczur.3 }
		}
		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}

	zachpom_JAS_vs_SNFBOR = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = JAS				
		}

		target_root_trigger = {
			has_war = no
			date > 2028.02.03
		}

		targets = {
			SNF BOR
		}

		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}		
		}
		ai_will_do = {
			factor = 100
		}

		days_remove = 7
		remove_effect  = {
			log = "[GetDateText]: Decision zachpom_JAS_vs_SNFBOR"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}
		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}
	zachpom_JAS_UNITED_SNF = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = JAS
		}

		available = {				
				BOR = { exists = no }
				JAS = { is_in_faction_with = SNF }
				has_war = no
				date > 2030.01.03
		}

		ai_will_do = {
			factor = 200
		}
		fire_only_once = yes
		complete_effect  = {
			log = "[GetDateText]: Decision zachpom_JAS_UNITED_SNF"
				country_event = { id = jaszczur.4 }
		}
	}
	zachpom_JAS_UNITED_BOR = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = JAS	
		}

		available = {			
			SNF = { exists = no }
			JAS = { is_in_faction_with = BOR }
			has_war = no
			date > 2030.01.03
		}

		ai_will_do = {
			factor = 200
		}
		fire_only_once = yes
		complete_effect  = {
			log = "[GetDateText]: Decision zachpom_JAS_UNITED_BOR"
				country_event = { id = jaszczur.4 }
		}
	}
#ZACHODNIOPOMORSKIE - FAZA DRUGA
	zachpom_EKW_vs_STE = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {
				original_tag = EKW					
			}			
		}

		target_root_trigger = {
			has_war = no
			date > 2028.08.01
		}

		targets = {
			STE
		}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7
		remove_effect  = {
			log = "[GetDateText]: Decision zachpom_EKW_vs_STE"
			declare_war_on = {
				target = STE
				type = voivodeship_war
			}
		}
		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}
	zachpom_USP_vs_JAS = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {
				original_tag = USP	
				original_tag = JAS						
			}			
		}

		target_root_trigger = {
			has_war = no
			date > 2028.08.01
		}

		ai_will_do = {
			factor = 100
		}

	targets = {
		USP JAS
	}

	target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}		
	}

		days_remove = 7
		remove_effect  = {
			log = "[GetDateText]: Decision zachpom_USP_vs_JAS"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}
		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}
	zachpom_JOM_vs_SWI = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {
				original_tag = SWI						
			}			
		}

		target_root_trigger = {
			has_war = no
			date > 2028.08.01
		}

		ai_will_do = {
			factor = 100
		}

	targets = {
		JOM
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}		
	}
		
		days_remove = 7
		remove_effect  = {
			log = "[GetDateText]: Decision zachpom_JOM_vs_SWI"
			declare_war_on = {
				target = JOM
				type = voivodeship_war
			}
		}
		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}
	#NORDBAT SIĘ SZYKUJE

#ZACHODNIOPOMORSKIE - FAZA TRZECIA
	zachpom_USPJAS_vs_NOR = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {
				original_tag = USP	
				original_tag = JAS	
				original_tag = SNF	
				original_tag = BOR	
				original_tag = NOR	
			}
		}

		target_root_trigger = {
			has_war = no
			date > 2029.06.01
		}
		targets = {
			NOR USP JAS SNF BOR
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}		
		}

		ai_will_do = {
			factor = 100
		}
		
		days_remove = 7
		remove_effect  = {
			log = "[GetDateText]: Decision zachpom_USPJAS_vs_NOR"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}
		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}

	zachpom_EKWSTE_vs_JOM = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = EKW	
		}

		target_root_trigger = {
			has_war = no
			date > 2029.04.01
		}

		targets = {
			JOM
		}

		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7
		remove_effect  = {
			log = "[GetDateText]: Decision zachpom_EKWSTE_vs_JOM"
			declare_war_on = {
				target = JOM
				type = voivodeship_war
			}
		}
		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}
	zachpom_EKWSTE_vs_SWI = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {
				original_tag = EKW	
				original_tag = SWI	
			}
			}

		target_root_trigger = {
			has_war = no
			date > 2029.04.01
		}
		targets = {
			SWI EKW STE
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7
		remove_effect  = {
			log = "[GetDateText]: Decision zachpom_EKWSTE_vs_SWI"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}
		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}
	
	#ZACHODNIOPOMORSKIE - FAZA CZWARTA
	zachpom_TOTAL_WAR_1 = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {
				original_tag = USP	
				original_tag = JAS	
				original_tag = BOR	
				original_tag = SNF	
				original_tag = NOR	
			}			
		}

		target_root_trigger = {
			has_war = no
			date > 2030.01.01
		}
		targets = {
			SWI EKW JOM STE NOR STR CHE LIG FRE KOS KLB
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7
		remove_effect  = {
			log = "[GetDateText]: Decision zachpom_TOTAL_WAR_1"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}	
		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}
	zachpom_TOTAL_WAR_2 = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {	
				original_tag = SWI
				original_tag = EKW
			}
		}

		target_root_trigger = {
			has_war = no
			date > 2030.01.01
		}
		targets = {
			USP NOR JAS SNF BOR POM
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7
		remove_effect  = {
			log = "[GetDateText]: Decision zachpom_TOTAL_WAR_2"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}
		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}	
	}
	#EWENTUALNE FIXY GDY BĘDZIE NIEZAPLANOWANY UKŁAD WATAŻKÓW

	zachpom_minor_solution_reverse = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {	
				original_tag = STR
				original_tag = CHE
				original_tag = LIG
				original_tag = FRE
				original_tag = KLB	
				original_tag = KOS
				original_tag = POM
			}			
		}

		target_root_trigger = {
			has_war = no
			date > 2031.06.01
		}
		targets = {
			SWI EKW JOM STE USP JAS SNF BOR NOR
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7
		remove_effect  = {
			log = "[GetDateText]: Decision zachpom_minor_solution_reverse"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}	
		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}
	zachpom_JOM_and_USP_fuckup = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			AND = { 
			OR = {	
				original_tag = JOM
				original_tag = USP
			}
			}
		}
		visible = {
			is_ai = yes
		}

		available = {
			OR = {
				AND = {
					USP = { has_war_with = KOS }
					NOT = { USP = { owns_state = 622 } }
					ROOT = { original_tag = USP }
				}
				AND = {
					JOM = { has_war_with = POM }
					NOT = { JOM = { owns_state = 68 } }
					ROOT = { original_tag = JOM }
				}
			}
			date > 2029.01.01
			date < 2033.01.01
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7
		remove_effect  = {
			log = "[GetDateText]: Decision zachpom_JOM_and_USP_fuckup"
				if = { 
					limit = { 
						ROOT = { original_tag = JOM }
						#JOM = { has_war_with = POM }
						#NOT = { JOM = { owns_state = 68 } }
					}
					DEN = {
						annex_country = {
							target = JOM
							transfer_troops = no
						}
					}
				}
				else_if = {
					limit = { 
						ROOT = { original_tag = USP }
						#USP = { has_war_with = KOS }
						#NOT = { USP = { owns_state = 622 } }
					}
					USA = {
						annex_country = {
							target = USP
							transfer_troops = no
						}
					}
				}
		}	
	}
	zachpom_USP_landing = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = USP
		}

		available = {
			USP = { has_country_flag = KOLONKO_LONDUJ }
		}

		ai_will_do = {
			factor = 100
		}
		fire_only_once = yes
		days_remove = 2
		remove_effect  = {
			log = "[GetDateText]: Decision zachpom_USP_landing"
				if = {
					limit = { 
						ROOT = { original_tag = USP }
					}
					623 = {
						create_unit = {
							division = "name = \"Tearington National Guard\" division_template = \"National Guard\""
							allow_spawning_on_enemy_provs = yes				
							prioritize_location = 17000
							owner = USP
						}
						create_unit = {
							division = "name = \"Slavton National Guard\" division_template = \"National Guard\""
							allow_spawning_on_enemy_provs = yes				
							prioritize_location = 16989
							owner = USP
						}
						create_unit = {
							division = "name = \"Basketown National Guard\" division_template = \"National Guard\""
							allow_spawning_on_enemy_provs = yes
							prioritize_location = 16990
							owner = USP
						}
						create_unit = {
							division = "name = \"Backshore National Guard\" division_template = \"National Guard\""
							allow_spawning_on_enemy_provs = yes
							prioritize_location = 16991
							owner = USP			
						}
					}
				}
		}	
	}
	

#██████╗░░█████╗░███████╗███╗░░██╗░█████╗░███╗░░██╗░██████╗██╗░░██╗██╗███████╗
#██╔══██╗██╔══██╗╚════██║████╗░██║██╔══██╗████╗░██║██╔════╝██║░██╔╝██║██╔════╝
#██████╔╝██║░░██║░░███╔═╝██╔██╗██║███████║██╔██╗██║╚█████╗░█████═╝░██║█████╗░░
#██╔═══╝░██║░░██║██╔══╝░░██║╚████║██╔══██║██║╚████║░╚═══██╗██╔═██╗░██║██╔══╝░░
#██║░░░░░╚█████╔╝███████╗██║░╚███║██║░░██║██║░╚███║██████╔╝██║░╚██╗██║███████╗
#╚═╝░░░░░░╚════╝░╚══════╝╚═╝░░╚══╝╚═╝░░╚═╝╚═╝░░╚══╝╚═════╝░╚═╝░░╚═╝╚═╝╚══════╝
#POZNAŃSKIE - FAZA PIERWSZA

 POZ_poznan_war = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = { 
			original_tag = POZ
			original_tag = SYJ
			original_tag = SLU
			}
		}

		target_root_trigger = {
			date > 2028.06.28
			has_war = no
		}

		ai_will_do = {
			factor = 100
		}
		targets = {
			SLU SYJ POZ
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}
		war_with_target_on_complete = yes
		days_remove = 7
		remove_effect  = {
			log = "[GetDateText]: Decision POZ_poznan_war"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}

	
 NAC_SUS_war = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {
			original_tag = NAC
			original_tag = SUS
			}
		}

		target_root_trigger = {
			date > 2028.05.03
			has_war = no
		}

		ai_will_do = {
			factor = 100
		}
		targets = {
			SUS NAC
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		days_remove = 7
		war_with_target_on_complete = yes
		remove_effect  = {
			log = "[GetDateText]: Decision NAC_SUS_war"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}
	
	
 PIL_WRT_war = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {
			original_tag = PIL
			original_tag = WRT
			}
		}

		target_root_trigger = {
			date > 2028.04.04
			has_war = no
		}
		targets = {
			WRT PIL
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7
		war_with_target_on_complete = yes
		remove_effect  = {
			log = "[GetDateText]: Decision PIL_WRT_war"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}
	
	
 KAM_MOT_war = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			OR = {
			original_tag = KAM
			original_tag = MOT
			}
		}

		target_root_trigger = {
			date > 2028.03.03
			has_war = no
		}

		ai_will_do = {
			factor = 100
		}
		targets = {
			MOT KAM
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		days_remove = 7
		war_with_target_on_complete = yes
		remove_effect  = {
			log = "[GetDateText]: Decision KAM_MOT_war"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}

	
#POZNAŃSKIE - FAZA DRUGA

 poznan_pila_war = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
		    OR = {
			original_tag = POZ
			original_tag = SYJ
			original_tag = SLU
			original_tag = PIL
			original_tag = WRT
			}
		}

		target_root_trigger = {
			date > 2028.11.10
			has_war = no
		}
		targets = {
			PIL WRT POZ SYJ SLU
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7
		war_with_target_on_complete = yes
		remove_effect  = {
			log = "[GetDateText]: Decision poznan_pila_war"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}
	
	
 nac_gorzow_war = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
		    OR = {
			original_tag = SUS
			original_tag = NAC
			original_tag = KAM
			original_tag = MOT
			}			
		}

		target_root_trigger = {
			date > 2028.09.02
			has_war = no
		}
		targets = {
			MOT KAM NAC SUS
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7
		war_with_target_on_complete = yes
		remove_effect  = {
			log = "[GetDateText]: Decision NAC_gorzow_war"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}
	
	
#POZNAŃSKIE - FAZA OSTATECZNA

 gorzow_poznan_war = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
		    OR = {
			original_tag = SUS
			original_tag = NAC
			original_tag = MOT
			original_tag = KAM
			original_tag = POZ
			original_tag = SLU
			original_tag = SYJ
			original_tag = PIL
			original_tag = WRT			
			}
		}

		target_root_trigger = {
			date > 2029.01.10
			has_war = no
		}
		targets = {
			POZ SLU SYJ PIL WRT NAC SUS KAM MOT
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}


		days_remove = 7
		war_with_target_on_complete = yes
		remove_effect  = {
			log = "[GetDateText]: Decision gorzow_poznan_war"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}
	


#██╗░░██╗░█████╗░██╗░░░░░██╗░██████╗██╗░░██╗██╗███████╗
#██║░██╔╝██╔══██╗██║░░░░░██║██╔════╝██║░██╔╝██║██╔════╝
#█████═╝░███████║██║░░░░░██║╚█████╗░█████═╝░██║█████╗░░
#██╔═██╗░██╔══██║██║░░░░░██║░╚═══██╗██╔═██╗░██║██╔══╝░░
#██║░╚██╗██║░░██║███████╗██║██████╔╝██║░╚██╗██║███████╗
#╚═╝░░╚═╝╚═╝░░╚═╝╚══════╝╚═╝╚═════╝░╚═╝░░╚═╝╚═╝╚══════╝
#KALISKIE - FAZA PIERWSZA
 FEL_leszno_war = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = FEL
		}

		target_root_trigger = {
			date > 2028.10.01
			has_war = no
		}

		targets = {
			LSZ
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7

		remove_effect  = {
			log = "[GetDateText]: Decision FEL_leszno_war"
			declare_war_on = {
				target = LSZ
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}
	LIS_leszno_war = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = LIS
		}

		target_root_trigger = {
			date > 2028.10.01
			country_exists = LSZ
			NOT = { country_exists = FEL }
			has_war = no
		}
		targets = {
			LSZ
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7

		remove_effect  = {
			log = "[GetDateText]: Decision LIS_leszno_war"
			declare_war_on = {
				target = LSZ
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}
	
	CAL_gniezno_cleanup = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = CAL
		}

		target_root_trigger = {
			date > 2028.09.01
			NOT = {
				country_exists = GNI
			}
			has_war = no
		}
		targets = {
			KLO KNN
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}
		days_remove = 7

		remove_effect  = {
			log = "[GetDateText]: Decision CAL_gniezno_cleanup"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}

 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}

	GNI_kalisz_war = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = GNI
		}

		target_root_trigger = {
			date > 2028.09.01
			has_war = no
		}
		targets = {
			CAL
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7
		war_with_target_on_complete = yes
		remove_effect  = {
			log = "[GetDateText]: Decision GNI_kalisz_war"
			declare_war_on = {
				target = CAL
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}
	
	
	LSZ_sroda_war = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = LSZ
		}

		target_root_trigger = {
			date > 2028.10.01
			has_war = no
		}
		targets = {
			FEL
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7
		war_with_target_on_complete = yes
		remove_effect  = {
			log = "[GetDateText]: Decision LSZ_sroda_war"
			declare_war_on = {
				target = FEL
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}
	
	LSZ_LIS_war = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = LSZ
		}

		target_root_trigger = {
			date > 2028.10.01
			NOT = { country_exists = FEL }
		}
		targets = {
			LIS
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7
		war_with_target_on_complete = yes
		remove_effect  = {
			log = "[GetDateText]: Decision LSZ_LIS_war"
			declare_war_on = {
				target = LIS
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}
	
#KALISKIE - FAZA OSTATECZNA
 barbarian_raid = {
	allowed = {
		always = yes
	}
	target_root_trigger = {
		has_idea = barbarians_idea
		CAL = {
			OR = {
				has_completed_focus = CAL_empire_or_death
				has_completed_focus = CAL_adapt_or_perish
			}
		}
		NOT = {
			has_global_flag = CAL_barbarians_win_flag
		}
	}
	target_array = root.neighbors
	target_trigger = {
		FROM = {
			# NOT = {
			# 	has_war = yes
			# }
			is_somsiad = no
			NOT = {
				original_tag = ZON
			}
			NOT = {
				is_in_faction_with = ROOT
			}
			NOT = {
				original_tag = WIZ
				original_tag = SAX
				original_tag = OST
				original_tag = JUT
				original_tag = FRK
				original_tag = HNN
				original_tag = SCY
				original_tag = AES
				original_tag = VAN
			}
			OR = {
				NOT = {
					original_tag = CAL
					original_tag = PRT
				}
				OR = {
					has_completed_focus = CAL_empire_or_death
					OR = {
						ROOT = {
							original_tag = JUT
						}
						ROOT = {
							original_tag = HNN
						}
						ROOT = {
							original_tag = VAN
						}
						
					}
				}
			}
		}
	}
	war_with_target_on_complete = yes
	ai_will_do = {
		base = 1
	}

	remove_effect = {
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
 }

 CAL_declare_war_on_lisowczycy = {
	allowed = {
		original_tag = CAL
	}
	target_root_trigger = {
		has_completed_focus = CAL_steppe_savages
		NOT = {
			country_exists = FEL
			country_exists = LSZ
		}
		country_exists = LIS
	}
	targets = { LIS }
	target_trigger = {
		FROM = {
			is_subject = no
		}
	}
	ai_will_do = {
		factor = 1000
	}
	complete_effect = {
		declare_war_on = {
			target = LIS
			type = voivodeship_war
		}
	}
 }

 FEL_kalisz_war = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = FEL
		}

		target_root_trigger = {
			date > 2029.09.01
			has_war = no
		}

		ai_will_do = {
			factor = 100
		}
		targets = {
			CAL
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		days_remove = 7

		remove_effect  = {
			log = "[GetDateText]: Decision FEL_kalisz_war"
			declare_war_on = {
				target = CAL
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}
	
	
	FEL_gniezno_war = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = FEL
		}

		target_root_trigger = {
			date > 2029.09.01
			has_war = no
		}
		targets = {
			GNI KNN KLO
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7

		remove_effect  = {
			log = "[GetDateText]: Decision FEL_gniezno_war"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}
	
	LIS_kalisz_war = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = LIS
		}

		target_root_trigger = {
			date > 2029.09.01
			NOT = { country_exists = FEL }
			has_war = no
		}
		targets = {
			CAL
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7

		remove_effect  = {
			log = "[GetDateText]: Decision LIS_kalisz_war"
			declare_war_on = {
				target = CAL
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}
	
	
	LIS_gniezno_war = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = LIS
		}

		target_root_trigger = {
			date > 2029.09.01
			NOT = { country_exists = FEL }
			has_war = no
		}
		targets = {
			GNI KNN KLO
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7

		remove_effect  = {
			log = "[GetDateText]: Decision LIS_gniezno_war"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}
	
    GNI_sroda_war = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = GNI
		}

		target_root_trigger = {
			date > 2029.09.01
			has_war = no
		}
		targets = {
			FEL
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7

		remove_effect  = {
			log = "[GetDateText]: Decision GNI_sroda_war"
			declare_war_on = {
				target = FEL
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}
	
	GNI_LIS_war = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = GNI
		}

		target_root_trigger = {
			date > 2029.09.01
			NOT = { country_exists = FEL }
			has_war = no
		}

		targets = {
			LIS
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7

		remove_effect  = {
			log = "[GetDateText]: Decision GNI_LIS_war"
			declare_war_on = {
				target = LIS
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}
	
	
	GNI_leszno_war = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = GNI
		}

		target_root_trigger = {
			date > 2029.09.01
			has_war = no
		}
		targets = {
			LSZ
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7

		remove_effect  = {
			log = "[GetDateText]: Decision GNI_leszno_war"
			declare_war_on = {
				target = LSZ
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}
	
	
	LSZ_kalisz_war = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = LSZ
		}

		target_root_trigger = {
			date > 2029.09.01
			has_war = no
		}
		targets = {
			CAL
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}

		days_remove = 7

		remove_effect  = {
			log = "[GetDateText]: Decision LSZ_kalisz_war"
			declare_war_on = {
				target = CAL
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}
	
	
	LSZ_gniezno_war = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = LSZ
		}

		target_root_trigger = {
			date > 2029.09.01
			has_war = no
		}
		targets = {
			GNI KLO KNN
		}
		target_trigger = {
			FROM = {
				is_neighbor_of = ROOT
				NOT = { is_subject_of = ROOT }
				NOT = { is_in_faction_with = ROOT }
				NOT = { has_war_with = ROOT }
			}
		}

		ai_will_do = {
			factor = 100
		}
		days_remove = 7
		remove_effect  = {
			log = "[GetDateText]: Decision LSZ_gniezno_war"
			declare_war_on = {
				target = FROM
				type = voivodeship_war
			}
		}
 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
	}
	
#██╗░░░░░███████╗░██████╗░███╗░░██╗██╗░█████╗░██╗░░██╗██╗███████╗
#██║░░░░░██╔════╝██╔════╝░████╗░██║██║██╔══██╗██║░██╔╝██║██╔════╝
#██║░░░░░█████╗░░██║░░██╗░██╔██╗██║██║██║░░╚═╝█████═╝░██║█████╗░░
#██║░░░░░██╔══╝░░██║░░╚██╗██║╚████║██║██║░░██╗██╔═██╗░██║██╔══╝░░
#███████╗███████╗╚██████╔╝██║░╚███║██║╚█████╔╝██║░╚██╗██║███████╗
#╚══════╝╚══════╝░╚═════╝░╚═╝░░╚══╝╚═╝░╚════╝░╚═╝░░╚═╝╚═╝╚══════╝
#LEGNICKIE - FAZA PIERWSZA
 LEGNICA_01_EKS_LOSU_LOSU = { #a wiecie co kurwa, polosujmy

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		original_tag = EKS
	}
	visible = {
		is_ai = yes
	}

	available = {
		date > 2028.04.01
		country_exists = ECO
		country_exists = BOG
	}

	ai_will_do = {
		factor = 100
	}
	fire_only_once = yes
	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: Decision EKS_annex_EKS_BOG"
		random_list = {
			70 = {
				annex_country = {
					target = ECO
					transfer_troops = yes
				}
				annex_country = {
					target = BOG
					transfer_troops = yes
				}				
			}
			20 = {
				ECO = {
					annex_country = {
						target = EKS
						transfer_troops = yes
					}
					annex_country = {
						target = BOG
						transfer_troops = yes
					}
				}
			}
			10 = {
				BOG = {
					annex_country = {
						target = EKS
						transfer_troops = yes
					}
					annex_country = {
						target = ECO
						transfer_troops = yes
					}
					set_cosmetic_tag = BOG_eurokom
				}
			}
		}

	}
	
 }

#LEGNICKIE - FAZA DRUGA
 LEGNICA_015_TOURISTENKRIEG = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = EKS
			original_tag = BOG
			original_tag = ECO
			original_tag = KGH
			original_tag = CYG
		}
	}

	target_root_trigger = {
		date > 2028.08.01
		has_war = no
		if = {
			limit = {
				ROOT = { original_tag = KGH }
			}
			CYG = { exists = no }
		}
		if = {
			limit = {
				ROOT = { original_tag = CYG }
			}
			KGH = { exists = no }
		}
		if = {
			limit = {
				ROOT = { original_tag = EKS }
			}
			ECO = { exists = no }
			BOG = { exists = no }
		}
		if = {
			limit = {
				ROOT = { original_tag = ECO }
			}
			EKS = { exists = no }
			BOG = { exists = no }
		}
		if = {
			limit = {
				ROOT = { original_tag = BOG }
			}
			EKS = { exists = no }
			ECO = { exists = no }
		}								
	}

	targets = {
		TOU
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			is_subject = no
			NOT = { is_in_faction_with = ROOT }
		}
	}


	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	war_with_on_remove = KGH
	remove_effect  = {
		log = "[GetDateText]: Decision EKS_KGH_war"
		declare_war_on = {
			target = TOU
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }

 LEGNICA_02_SOUTHERN_WAR = {

	icon = GFX_decision_generic_prepare_civil_war

	allowed = {
		OR = {
			original_tag = EKS
			original_tag = BOG
			original_tag = ECO
			original_tag = CYG
			original_tag = KGH
			original_tag = TOU
		}
	}

	target_root_trigger = {
		date > 2029.03.15
		has_war = no
		if = {
			limit = {
				original_tag = EKS
			}
			OR = {
				TOU = { exists = no }
				TOU = { is_subject = yes }
			}
			OR = {
				KGH = {exists=yes}
				CYG = {exists=yes}
			}
		}
		if = {
			limit = {
				original_tag = ECO
			}
			OR = {
				TOU = { exists = no }
				TOU = { is_subject = yes }
			}
			OR = {
				KGH = {exists=yes}
				CYG = {exists=yes}
			}
		}
		if = {
			limit = {
				original_tag = BOG
			}
			OR = {
				TOU = { exists = no }
				TOU = { is_subject = yes }
			}
			OR = {
				KGH = {exists=yes}
				CYG = {exists=yes}
			}
		}
		if = {
			limit = {
				original_tag = KGH
			}
			OR = {
				TOU = { exists = no }
				TOU = { is_subject = yes }
			}
			OR = {
				EKS = {exists=yes}
				ECO = {exists=yes}
				BOG = {exists=yes}
			}
			CYG = { exists = no }
		}
		if = {
			limit = {
				original_tag = CYG
			}
			OR = {
				TOU = { exists = no }
				TOU = { is_subject = yes }
			}
			OR = {
				EKS = {exists=yes}
				ECO = {exists=yes}
				BOG = {exists=yes}
			}
			KGH = { exists = no }
		}								
	}
	targets = {
		TOU EKS ECO BOG KGH CYG
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	war_with_target_on_complete = yes
	remove_effect  = {
		log = "[GetDateText]: Decision EKS_KGH_war"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}		
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }

 LEGNICA_02_NORTHERN_WAR = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = ZAG
			original_tag = WIN
		}
	}

	target_root_trigger = {
		date > 2029.02.01
		has_war = no
		if = {
			limit = {
				original_tag = ZAG
			}
			OR = {
				PZL = {exists = yes}
				WIN = {exists = yes}
			}
		}
		if = {
			limit = {
				original_tag = WIN
			}
			OR = {
				PZL = {exists = yes}
				ZAG = {exists = yes}
			}
		}		
	}
	targets = {
		ZAG PZL WIN
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	ai_will_do = {
		factor = 100
	}

	days_remove = 7

	remove_effect  = {
		log = "[GetDateText]: Decision ZAG_WIN_war"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }


#LEGNICKIE - FAZA OSTATECZNA

LEGNICA_03_FINAL_WAR = {

	icon = GFX_decision_generic_prepare_civil_war

	allowed = {
		OR = {
			original_tag = EKS
			original_tag = BOG
			original_tag = ECO
			original_tag = CYG
			original_tag = KGH
			original_tag = TOU
			original_tag = WIN
			original_tag = PZL
			original_tag = ZAG
		}
	}

	target_root_trigger = {
		date > 2029.08.15
		has_war = no
		if = {
			limit = {
				original_tag = EKS
			}
			OR = {
				TOU = { exists = no }
				TOU = { is_subject_of = ROOT }
			}
			KGH = {exists=no}
			CYG = {exists=no}
		}
		if = {
			limit = {
				original_tag = ECO
			}
			TOU = { exists = no }
			KGH = { exists = no }
			CYG = { exists = no }
		}
		if = {
			limit = {
				original_tag = BOG
			}
			OR = {
				TOU = { exists = no }
				TOU = { is_subject_of = ROOT }
			}
			KGH = {exists=no}
			CYG = {exists=no}
		}
		if = {
			limit = {
				original_tag = KGH
			}
			OR = {
				TOU = { exists = no }
				TOU = { is_subject_of = ROOT }
			}
			EKS = { exists = no }
			ECO = { exists = no }
			BOG = { exists = no }
			CYG = { exists = no }
		}
		if = {
			limit = {
				original_tag = CYG
			}
			OR = {
				TOU = { exists = no }
				TOU = { is_subject_of = ROOT }
			}
			EKS = { exists = no }
			ECO = { exists = no }
			BOG = { exists = no }
			KGH = { exists = no }
		}
		if = {
			limit = {
				original_tag = PZL
			}
			WIN = { exists = no }
			ZAG = { exists = no }
		}
		if = {
			limit = {
				original_tag = WIN
			}
			PZL = { exists = no }
			ZAG = { exists = no }
		}
		if = {
			limit = {
				original_tag = ZAG
			}
			PZL = { exists = no }
			WIN = { exists = no }
		}														
	}
	targets = {
		TOU EKS ECO BOG KGH CYG WIN PZL ZAG
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	war_with_on_remove = KGH
	remove_effect  = {
		log = "[GetDateText]: Decision EKS_KGH_war"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}			
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }


#░██╗░░░░░░░██╗██████╗░░█████╗░░█████╗░██╗░░░░░░█████╗░░██╗░░░░░░░██╗░██████╗██╗░░██╗██╗███████╗
#░██║░░██╗░░██║██╔══██╗██╔══██╗██╔══██╗██║░░░░░██╔══██╗░██║░░██╗░░██║██╔════╝██║░██╔╝██║██╔════╝
#░╚██╗████╗██╔╝██████╔╝██║░░██║██║░░╚═╝██║░░░░░███████║░╚██╗████╗██╔╝╚█████╗░█████═╝░██║█████╗░░
#░░████╔═████║░██╔══██╗██║░░██║██║░░██╗██║░░░░░██╔══██║░░████╔═████║░░╚═══██╗██╔═██╗░██║██╔══╝░░
#░░╚██╔╝░╚██╔╝░██║░░██║╚█████╔╝╚█████╔╝███████╗██║░░██║░░╚██╔╝░╚██╔╝░██████╔╝██║░╚██╗██║███████╗
#░░░╚═╝░░░╚═╝░░╚═╝░░╚═╝░╚════╝░░╚════╝░╚══════╝╚═╝░░╚═╝░░░╚═╝░░░╚═╝░░╚═════╝░╚═╝░░╚═╝╚═╝╚══════╝
#WROCŁAWSKIE - FAZA PIERWSZA
WROC_01_OLE_OLE = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		is_lower_silesia = yes 
		NOT = {
			original_tag = OLE
		}
	}

	target_root_trigger = {
		OR = {
			AND = {
				date > 2027.11.01
				original_tag = RYB
			}
			AND = {
				date > 2029.01.01
				OR = {
					AND = {
					original_tag = MPK
					NOT = { is_subject_of = WRO }
					}
					original_tag = WRO
				}
			}
			AND = {
				date > 2031.01.01
				OR = {
					original_tag = ANA
					original_tag = MNL
					original_tag = ZUL
					original_tag = CZE
				}
			}			
		}
		has_war = no
	}
	targets = {
		OLE
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	ai_will_do = {
		factor = 100
	}
	days_remove = 7
	war_with_target_on_complete	= yes
	remove_effect  = {
		log = "[GetDateText]: Decision RYB_OLE_war"
		declare_war_on = {
			target = OLE
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }
 WROC_01_ZUL_WARS = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = ZUL
			original_tag = ANA
			original_tag = MNL
		}
	}

	target_root_trigger = {
		has_war = no
		date > 2028.03.01
		has_country_flag = zul_wars_begun
		count_triggers = {
			amount = 1
			ZUL = { exists = no }
			MNL = { exists = no }
			ANA = { exists = no }
		}
	}
	targets = {
		ZUL MNL ANA
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	remove_effect  = {
		log = "[GetDateText]: ZUL_ANA_MNL_war"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}				
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }	
#WROCŁAWSKIE - FAZA DRUGA
 WROC_02_KONICHIWA_ZUL_SENPAI = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		original_tag = CZE
	}

	target_root_trigger = {
		date > 2028.09.16	
		has_war = no
		OR = {
			ZUL = { exists = yes }
			MNL = { exists = yes }
			ANA = { exists = yes }
		}	
	}

	ai_will_do = {
		factor = 100
	}

	targets = {
		ZUL MNL ANA
	}

	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}	

	days_remove = 7
	war_with_target_on_complete = yes
	remove_effect  = {
		log = "[GetDateText]: Decision okamura_ZUL_conquest_war"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }

 WROC_02_FISHING_FOR_WROCLAW = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		original_tag = RYB
	}

	target_root_trigger = {
		date > 2028.06.01
		has_war = no
		MPK = { is_subject = yes }
	}
	targets = {
		WRO
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}


	ai_will_do = {
		factor = 100
	}

	days_remove = 7
	war_with_target_on_complete = yes
	remove_effect  = {
		log = "[GetDateText]: Decision RYB_MPK_war"
		if = { limit = {
			MPK = { exists = yes }
			}
		declare_war_on = {
			target = MPK
			type = voivodeship_war
		}			
		}
		if = { limit = {
			WRO = { exists = yes }
			}
		declare_war_on = {
			target = WRO
			type = voivodeship_war
		}			
		}
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}	
 }

 WROC_02_SUTRYK_GAMBIT = {
	allowed = {
		original_tag = WRO
	}
	available = {
		original_tag = WRO
		has_country_flag = WRO_makao
		has_war = no
	}
	
	ai_will_do = {
		factor = 100
	}
	fire_only_once = yes
	days_remove = 7
	remove_effect  = {
		if = {
			limit = {
				ZUL = {
					has_country_leader = {
						character = ZUL_szelemej
					}
					exists = yes
				}
			}
		random_list = {
			10 = {
				annex_country = {
					target = ZUL
				}
			}
			90 = {
				ZUL = {
					declare_war_on = {
						target = WRO
					}
				}
			}
		}
		}
		if = {
			limit = {
				ZUL = {
					NOT = { has_country_leader = {
						character = ZUL_szelemej
					}
					}
					exists = yes
				}
			}
		declare_war_on = {
			target = ZUL
			type = voivodeship_war
		}
		}		
		if = { limit = {
			MNL = { exists = yes }
			}
		declare_war_on = {
			target = MNL
			type = voivodeship_war
		}			
		}
		if = { limit = {
			ANA = { exists = yes }
			}
		declare_war_on = {
			target = ANA
			type = voivodeship_war
		}			
		}
		if = { limit = {
			CZE = { exists = yes }
			}
		declare_war_on = {
			target = CZE
			type = voivodeship_war
		}			
		}				
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }
#WROCŁAWSKIE - FAZA OSTATECZNA
 WROC_03_MARCH_SOUTH = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = WRO
			original_tag = RYB
			original_tag = MPK
		}		
	}

	target_root_trigger= {
		has_war = no
		if = {
			limit = {
				original_tag = WRO
			}
			OR = {
				AND = {
					WRO = { NOT = { has_country_flag = WRO_makao} }
					OR = {
						MPK = {exists = no}
						MPK = {is_subject_of = WRO}
					}
					RYB = {exists=no}
				}
				AND = {
					WRO = { has_country_flag = WRO_makao }
					OR = {
						ZUL = { exists = yes}
						ANA = { exists = yes}
						MNL = { exists = yes}
						CZE = { exists = yes}
					}
				}
			}
		}
		if = {
			limit = {
				original_tag = MPK
			}
			OR = {
				AND = {
					WRO = { exists = no }
					RYB = { exists = no }
				}
				AND = {
					WRO = { has_country_flag = WRO_makao }
					RYB = { exists = no }
					MPK = {
						NOT = {
						any_owned_state = {
							has_dynamic_modifier = {
								modifier = dam_blown_modifier
							}
						}
						}
					}
				}
			}
		}
		if = {
			limit = {
				original_tag = RYB
			}
			OR = {
				AND = {
					WRO = { exists = no }
					MPK = { exists = no }
				}
				AND = {
					WRO = { has_country_flag = WRO_makao }
					MPK = { exists = no }
				}
			}
		}				
	}
	targets = {
		WRO MPK RYB ZUL MNL ANA CZE
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}
	
	ai_will_do = {
		factor = 100
	}

	days_remove = 20
	remove_effect  = {
		log = "[GetDateText]: Decision wroclaw_war"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}			
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 }


 WROC_03_MARCH_NORTH = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		OR = {
			original_tag = WRO
			original_tag = ANA
			original_tag = MNL
			original_tag = CZE
			original_tag = ZUL
		}		
	}

	target_root_trigger = {
		has_war = no
		if = {
			limit = {
				original_tag = WRO
			}
			WRO = { has_country_flag = WRO_makao }
			AND = {
				ZUL = { exists = no}
				ANA = { exists = no}
				MNL = { exists = no}
				CZE = { exists = no}
			}
		}
		if = {
			limit = {
				original_tag = ANA
			}
			AND = {
				ZUL = { exists = no}
				MNL = { exists = no}
				CZE = { exists = no}
			}			
		}
		if = {
			limit = {
				original_tag = CZE
			}
			AND = {
				ZUL = { exists = no}
				MNL = { exists = no}
				ANA = { exists = no}
			}			
		}
		if = {
			limit = {
				original_tag = ZUL
			}
			AND = {
				CZE = { exists = no}
				MNL = { exists = no}
				ANA = { exists = no}
			}			
		}
		if = {
			limit = {
				original_tag = MNL
			}
			AND = {
				CZE = { exists = no}
				ZUL = { exists = no}
				ANA = { exists = no}
			}			
		}								
	}
	targets = {
		MNL WRO MPK CZE RYB ZUL ANA
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}
	
	ai_will_do = {
		factor = 100
	}

	days_remove = 20
	remove_effect  = {
		log = "[GetDateText]: Decision wroclaw_war"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}					
	}
	cancel_trigger = {
		ROOT = {
			has_war = yes
		}
	}
 } 


#███╗░░██╗██╗░░░██╗░██████╗██╗░░██╗██╗███████╗
#████╗░██║╚██╗░██╔╝██╔════╝██║░██╔╝██║██╔════╝
#██╔██╗██║░╚████╔╝░╚█████╗░█████═╝░██║█████╗░░
#██║╚████║░░╚██╔╝░░░╚═══██╗██╔═██╗░██║██╔══╝░░
#██║░╚███║░░░██║░░░██████╔╝██║░╚██╗██║███████╗
#╚═╝░░╚══╝░░░╚═╝░░░╚═════╝░╚═╝░░╚═╝╚═╝╚══════╝
#NYSKIE - FAZA PIERWSZA
 battle_of_nysa = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		is_opolskie = yes
	}

	target_root_trigger = {
		OR = {
			original_tag = NYS
			original_tag = LEH
			OR = {
				AND = {
					original_tag = LCH
					OPO = { exists = no }
				}
				AND = {
					original_tag = OPO
					LCH = { exists = no }
				}				
			}
		}
		has_global_flag = OPOLE_BORDER_WAR_END
		date > 2028.06.27
	}

	targets = {
		NYS LEH
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	ai_will_do = {
		factor = 100

	}

	days_remove = 7
	remove_effect  = {	
		log = "[GetDateText]: Decision opole_battle_of_nysa"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}

 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
 }

 opole_war_lechici_kukiz = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		is_opolskie = yes
			}

	target_root_trigger = {
		has_global_flag = OPOLE_BORDER_WAR_END
		OR = {
				original_tag = LCH
				original_tag = OPO
				OR = {
					AND = {
					original_tag = LEH
					NYS = { exists = no }
					}
					AND = {
					original_tag = NYS
					LEH = { exists = no }
					}					
				}
			}
		date > 2028.06.04
	}

	targets = {
		LCH OPO
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	ai_will_do = {
		factor = 100

	}

	days_remove = 7
	remove_effect  = {	
		log = "[GetDateText]: Decision opole_war_lechici_kukiz"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}

 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
 }

 opole_war_wandale_niemcy = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		is_opolskie = yes
	}

	target_root_trigger = {
		has_global_flag = OPOLE_BORDER_WAR_END
			OR = {
				original_tag = WAN
				original_tag = MNI
				OR = {
					AND = {
					original_tag = LEH
					NYS = { exists = no }
					}
					AND = {
					original_tag = NYS
					LEH = { exists = no }
					}					
				}
				OR = {
					AND = {
						original_tag = LCH
						OPO = { exists = no }
					}
					AND = {
						original_tag = OPO
						LCH = { exists = no }
					}				
				}
			}
		date > 2028.09.01
	}
	targets = {
		WAN MNI
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}


	ai_will_do = {
		factor = 100

	}

	days_remove = 7
	remove_effect  = {	
		log = "[GetDateText]: Decision opole_war_1"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}

 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
 }
#NYSKIE - FAZA DRUGA
#NYSKIE - FAZA OSTATECZNA
 opole_gotterdammerung = {

	icon = GFX_decision_generic_prepare_civil_war
	
	allowed = {
		is_opolskie = yes
	}

	target_root_trigger = {
		has_global_flag = OPOLE_BORDER_WAR_END
		OR = {
			AND = {
				date > 2029.06.01
				has_war = no
				count_triggers = {
					amount = 3
					OPO = { exists = no }
					MNI = { exists = no }
					LCH = { exists = no }
					LEH = { exists = no }
					WND = { exists = no }
					NYS = { exists = no }
				}
			}
			AND = {
				date > 2030.08.01
				count_triggers = {
					amount = 1
					OPO = { exists = no }
					MNI = { exists = no }
					LCH = { exists = no }
					LEH = { exists = no }
					WND = { exists = no }
					NYS = { exists = no }
				}
			}
		}
	}
	targets = {
		NYS LEH LCH OPO WAN MNI
	}
	target_trigger = {
		FROM = {
			is_neighbor_of = ROOT
			NOT = { is_subject_of = ROOT }
			NOT = { is_in_faction_with = ROOT }
		}
	}

	ai_will_do = {
		factor = 100
	}

	days_remove = 7

	remove_effect  = {
		log = "[GetDateText]: Decision opole_gotterdammerung"
		declare_war_on = {
			target = FROM
			type = voivodeship_war
		}
	}

 		cancel_trigger = {
			ROOT = {
				has_war = yes
			}
 		}
 }


}