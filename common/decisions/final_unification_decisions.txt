
unification_category = {

poland_unification = {

	icon = GFX_decision_poland_uni
	
	available = {
		OR = {
			is_in_faction = no
			is_faction_leader = yes
		}
		custom_trigger_tooltip = {
			tooltip = controls_poland
			can_unify_poland = yes
		}
		if = {
			limit = { original_tag = CAL }
			OR = {
				
				has_completed_focus = CAL_concordia
				has_completed_focus = CAL_discordia
				
				# AND = {
				# 	has_completed_focus = CAL_reward_the_most_loyal
				# 	has_completed_focus = CAL_constantine_plan
					
				# }
				has_completed_focus = CAL_progedi_populus
				has_completed_focus = CAL_renovatio_imperii
				# AND = {
				# 	has_completed_focus = CAL_expulsion_of_barbarians
				# 	has_completed_focus = CAL_surround_the_palace
				# }
				has_completed_focus = CAL_cry_havoc
			}
		}
	}
	
	fire_only_once = yes

	cost = 0
	
	visible = { 
		is_poland_unifier = yes
	}

	complete_effect = {
		log = "[GetDateText]: Decision lodz_final_unification"
		set_country_flag = poland_mountain
		add_research_slot = 1
		hidden_effect = {
			set_technology = {
				industry_revival = 1
			}
			every_other_country = {
				limit = {
					is_ally_with = ROOT
					is_ai = yes
				}
				root = {
					log = "[prev.GetName]"
					annex_country = {
						target = prev
						transfer_troops = yes
					}
				}
			}
		}
		if = {
			limit = { original_tag = WML
					  has_country_leader = {
						character = WML_szymon_hołownia
						ruling_only = yes
						}
			}
			set_cosmetic_tag = WML_hoiownia_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.1 }
				}
			}		
		}
		else_if = {
			limit = { original_tag = WML
					  has_country_leader = {
						character = WML_jasmina
						ruling_only = yes
						}
			}
			set_cosmetic_tag = WML_dialog_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.1 }
				}
			}
			
		}
		else_if = {
			limit = { original_tag = BBW
					  has_government = reactionary 
					  }
			set_cosmetic_tag = BBW_karbonizacja_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.2 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = BBW
					  has_government = autocracy 
					  }
			set_cosmetic_tag = BBW_roksi_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.2 }
				}
			}
			
		}
		else_if = {
			limit = { original_tag = ZYD
					 has_government = marxism_cranism }
			set_cosmetic_tag = ZYD_urban_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.37 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = ZYD
					 has_government = squattism
				}
			set_cosmetic_tag = ZYD_widzew_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.3 }
				}
			}
			
		}
		else_if = {
			limit = { original_tag = ZHP
						has_government = postpolonism }
			set_cosmetic_tag = ZHP_zhp_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.4 }
				}
			}
			
		}
		else_if = {
			limit = { original_tag = ZHP
					  has_government = autocracy }
			set_cosmetic_tag = ZHP_zawisza_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.4 }
				}
			}
			
		}
		else_if = {
			limit = { original_tag = ZHP
					  has_government = paternal_conservatism }
			set_cosmetic_tag = ZHP_zhr_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_b.4 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = SAP
					 has_government = autocracy }
			set_cosmetic_tag = SAP_insane_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.5 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = SAP
					  has_government = squattism }
			set_cosmetic_tag = SAP_sane_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.5 }
				}
			}
			
		}
		else_if = {
			limit = { original_tag = KAR }
			set_cosmetic_tag = KAR_carlistas_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_b.5 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = KRS
					  has_government = corporativism }
			set_cosmetic_tag = KRS_skrzydlewscy_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.6 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = RUT }
			set_cosmetic_tag = RUT_rutkowski_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.36 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = POG }
			set_cosmetic_tag = POG_pogotowie_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.35 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = CRN }
			set_cosmetic_tag = CRN_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.7 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = PUL }
			set_cosmetic_tag = PUL_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.8 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = KSN }
			set_cosmetic_tag = KSN_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.9 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = SWD }
			set_cosmetic_tag = SWD_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.10 }
				}
			}
			
		}
		
		else_if = {
			limit = {
				original_tag = ZUK
				NOT = { has_government = autocracy }
			}
			set_cosmetic_tag = ZUK_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.11 }
				}
			}
			
		}
		
		else_if = {
			limit = {
				original_tag = ZUK
				has_government = autocracy
			}
			set_cosmetic_tag = ZUK_dark_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.11 }
				}
			}
			
		}

		else_if = {
			limit = { original_tag = BIL }
			set_cosmetic_tag = BIL_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.12 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = BPD }
			set_cosmetic_tag = BPD_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.13 }
				}
			}
			
		}
	
		else_if = {
			limit = { original_tag = DGR
					  has_government = neonationalism 
					  }
			set_cosmetic_tag = DGR_nazbol_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.14 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = DGR
					  has_government = postpolonism
					  }
			set_cosmetic_tag = DGR_rasta_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.14 }
				}
			}
			
		}
		
		else_if = {
			limit = { original_tag = DGR
					  has_government = marxism_cranism
					  }
			set_cosmetic_tag = DGR_zsrr_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_b.14 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = SOS
					 has_government = marxism_cranism
				}
			set_cosmetic_tag = SOS_zukowska_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.15 }
				}
			}
			
		}


		else_if = {
			limit = { 
					 original_tag = SOS
					 has_government = socialdemocrature
				}
			set_cosmetic_tag = SOS_litewka_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.15 }
				}
			}
			
		}

	
		else_if = {
			limit = { original_tag = JAW
					  has_country_leader = {
						character = JAW_antifascist_council
						ruling_only = yes
						}
			}
			set_cosmetic_tag = JAW_antifa_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.16 }
				}
			}
			
		}


		else_if = {
			limit = { original_tag = JAW
					  has_country_leader = {
						character = JAW_konieczny
						ruling_only = yes
						}
			}
			set_cosmetic_tag = JAW_razem_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.16 }
				}
			}
			
		}	

		else_if = {
			limit = { 
					 original_tag = JUR
					 has_government = autocracy
				}
			set_cosmetic_tag = JUR_gowin_aut_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.17 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = JUR
					 has_government = freemarketism
				}
			set_cosmetic_tag = JUR_gowin_free_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.17 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = CCH
					 has_government = reactionary
				}
			set_cosmetic_tag = CCH_paulins_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.18 }
				}
			}
			
		}


		else_if = {
			limit = { 
					 original_tag = CCH
					 has_government = socialdemocrature
				}
			set_cosmetic_tag = CCH_joannites_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.18 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = CCH
					 has_government = postpolonism
				}
			set_cosmetic_tag = CCH_masons_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_b.18 }
				}
			}
			
		}
		
		
		
		else_if = {
			limit = { 
					 original_tag = BRZ
					 has_government = marxism_cranism
				}
			set_cosmetic_tag = BRZ_buzzer_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.19 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = BRZ
					 has_government = socialdemocrature
				}
			set_cosmetic_tag = BRZ_kuba_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.19 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = BRZ
					 has_government = euroliberalism
				}
			set_cosmetic_tag = BRZ_lewy_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_b.19 }
				}
			}
			
		}


		else_if = {
			limit = { 
					 original_tag = JWK
					 has_government = reactionary
				}
			set_cosmetic_tag = JWK_sparta_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.20 }
				}
			}
			
		}


		else_if = {
			limit = { 
					 original_tag = JWK
					 has_government = autocracy
				}
			set_cosmetic_tag = JWK_komando_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.20 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = WOD
					 has_government = reactionary
				}
			set_cosmetic_tag = WOD_bekier_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.22 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = WOD
					 has_government = neonationalism
				}
			set_cosmetic_tag = WOD_szczery_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.22 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = WOD
					 has_government = postpolonism
				}
			set_cosmetic_tag = WOD_ragnarok_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_b.22 }
				}
			}
			
		}
		

		else_if = {
			limit = { 
					 original_tag = SOL
				}
			set_cosmetic_tag = SOL_nszz_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.23 }
				}
			}
			
		}	
		
		
		else_if = {
			limit = { 
					 original_tag = JSW
					 has_government = corporativism
				}
			set_cosmetic_tag = JSW_jsw_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.23 }
				}
			}
			
		}


		else_if = {
			limit = { 
					 original_tag = SBW
				}
			set_cosmetic_tag = SBW_coal_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_b.23 }
				}
			}
			
		}
		
		
		else_if = {
			limit = { 
					 original_tag = GLI
					 has_government = autocracy
				}
			set_cosmetic_tag = GLI_rajch_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.24 }
				}
			}
			
		}


		else_if = {
			limit = { 
					 original_tag = GLI
					 has_government = socialdemocrature
				}
			set_cosmetic_tag = GLI_murzinytancujom_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.24 }
				}
			}
			
		}
		

		else_if = {
			limit = { 
					 original_tag = CHO
				}
			set_cosmetic_tag = CHO_ras_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.25 }
				}
			}
			
		}	
		
		else_if = {
			limit = { 
					 original_tag = RAS
				}
			set_cosmetic_tag = RAS_schaffgotsch_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.26 }
				}
			}
			
		}		
		
		else_if = {
			limit = { 
					 original_tag = KAT
					 has_government = euroliberalism
				}
			set_cosmetic_tag = KAT_budka_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.29 }
				}
			}
			
		}	
		
		
		else_if = {
			limit = { 
					 original_tag = KAT
					 has_government = paternal_conservatism
				}
			set_cosmetic_tag = KAT_senior_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.29 }
				}
			}
			
		}


		else_if = {
			limit = { 
					 original_tag = KAT
					 has_government = freemarketism
				}
			set_cosmetic_tag = KAT_junior_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_b.29 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = CAL
					 has_completed_focus = CAL_renovatio_imperii
				}
			set_cosmetic_tag = CAL_optimates_final
			
			hidden_effect = {
				CAL_ricardus_calisius = {
					set_portraits = {
						civilian = {
							large = "gfx/leaders/CAL/CAL_Kalisz2.dds"
						}
						army = {
							large = "gfx/leaders/CAL/CAL_Kalisz2.dds"
						}
					}
				}
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.31 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = CAL
					 has_completed_focus = CAL_progedi_populus
				}
			set_cosmetic_tag = CAL_populares_final
			hidden_effect = {
				CAL_ricardus_calisius = {
					set_portraits = {
						civilian = {
							large = "gfx/leaders/CAL/CAL_Kalisz3.dds"
						}
						army = {
							large = "gfx/leaders/CAL/CAL_Kalisz3.dds"
						}
					}
				}
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.31 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = CAL
					 has_completed_focus = CAL_cry_havoc
				}
			set_cosmetic_tag = CAL_triumwirat_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_b.31 }
				}
			}
			
		}		
		
		else_if = {
			limit = { 
					 original_tag = CAL
					 has_completed_focus = CAL_discordia
				}
			set_cosmetic_tag = CAL_pretoriae_imperium_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.311 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = CAL
					 has_completed_focus = CAL_concordia
				}
			set_cosmetic_tag = CAL_pretoriae_respublica_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.311 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = GNI
				}
			set_cosmetic_tag = GNI_piast_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.32 }
				}
			}
			
		}		
		else_if = {
			limit = { 
					 original_tag = FEL
				}
			set_cosmetic_tag = FEL_sarmatia_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.33 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = SUS
				}
			set_cosmetic_tag = SUS_sus_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.34 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = CIE
					 has_government = euroliberalism
				}
			set_cosmetic_tag = CIE_malysz_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.38 }
				}
			}
			
		}
		else_if = {
			limit = { 
					 original_tag = BSK
					 has_government = autocracy
				}
			set_cosmetic_tag = BSK_bpierdolec_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.39 }
				}
			}
			
		}	

		else_if = {
			limit = { 
					 original_tag = GUR
					 has_government = neonationalism
				}
			set_cosmetic_tag = GUR_piton_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.40 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = GUR
					 has_government = postpolonism
				}
			set_cosmetic_tag = GUR_zapierdalanie_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.40 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = GUR
					 has_government = paternal_conservatism
				}
			set_cosmetic_tag = GUR_tokp_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_b.40 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = MSL
				}
			set_cosmetic_tag = MSL_spadaki_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.41 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = SCZ
				}
			set_cosmetic_tag = SCZ_drip_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.42 }
				}
			}
			
		}
		else_if = {
			limit = { 
					 original_tag = BSK
					 has_government = freemarketism
				}
			set_cosmetic_tag = BSK_bgolec_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.44 }
				}
			}
			
		}
		else_if = {
			limit = { 
					 original_tag = EKS
					 has_government = euroliberalism
				}
			set_cosmetic_tag = EKS_schetyna_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.45 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = BOG
				}
			set_cosmetic_tag = BOG_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.45 }
				}
			}
			
		}
		else_if = {
			limit = { 
					original_tag = ECO
				}
			set_cosmetic_tag = ECO_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_b.45 }
				}
			}
			
		}
		else_if = {
			limit = { 
					 original_tag = WRO
					 OR = {
					 has_government = euroliberalism
					 has_government = paternal_conservatism
					 }
				}
			set_cosmetic_tag = WRO_dem_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.46 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = WRO
					 has_government = socialdemocrature
				}
			set_cosmetic_tag = WRO_adb_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.46 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = WRO
					 has_government = squattism
				}
			set_cosmetic_tag = WRO_lempart_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_b.46 }
				}
			}
			
		}		

		else_if = {
			limit = { 
					 original_tag = CZE
					 has_government = autocracy
				}
			set_cosmetic_tag = CZE_okamura_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.47 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = CZE
					 has_government = neonationalism
				}
			set_cosmetic_tag = CZE_sengoku_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.47 }
				}
			}
			
		}
		else_if = {
			limit = { 
					 original_tag = CZE
					 has_government = paternal_conservatism
				}
			set_cosmetic_tag = CZE_komorowski_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_b.47 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = KGH
					 has_government = corporativism
				}
			set_cosmetic_tag = KGH_board_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.48 }
				}
			}
			
		}		
		
		else_if = {
			limit = { 
					 original_tag = KGH
					 has_government = postpolonism
				}
			set_cosmetic_tag = KGH_singularity_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.48 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = RYB
					 has_government = autocracy
				}
			set_cosmetic_tag = RYB_galecki_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.49 }
				}
			}
			
		}		
		
		else_if = {
			limit = { 
					 original_tag = RYB
					 has_government = socialdemocrature
				}
			set_cosmetic_tag = RYB_sum_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.49 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = POZ
				}
			set_cosmetic_tag = POZ_jjay_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.50 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = SYJ
				}
			set_cosmetic_tag = SYJ_buzkabuzka_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.51 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = NAC
					 has_country_leader = {
						character = NAC_krzysztof_bosak
						ruling_only = yes
					}
				}
			set_cosmetic_tag = NAC_bosak_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.52 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = NAC
					 has_country_leader = {
						character = NAC_robert_winnicki
						ruling_only = yes
					}
				}
			set_cosmetic_tag = NAC_winnicki_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.52 }
				}
			}
			
		}
		else_if = {
			limit = { 
					 original_tag = NAC
					 has_country_leader = {
						character = NAC_roman_giertych
						ruling_only = yes
					}
				}
			set_cosmetic_tag = NAC_giertych_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_b.52 }
				}
			}
			
		}	

		else_if = {
			limit = { 
					 original_tag = PIL
					 has_government = corporativism
				}
			set_cosmetic_tag = PIL_stoklosa_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.53 }
				}
			}
			
		}		
		
		else_if = {
			limit = { 
					 original_tag = PIL
					 has_government = postpolonism
				}
			set_cosmetic_tag = PIL_kania_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.53 }
				}
			}
			
		}	
		
		else_if = {
			limit = { 
					 original_tag = EKW
					 has_government = euroliberalism
				}
			set_cosmetic_tag = EKW_tusklib_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.54 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = EKW
					 has_government = autocracy
				}
			set_cosmetic_tag = EKW_furdeutschland_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.54 }
				}
			}
			
		}
		else_if = {
			limit = { 
					 original_tag = EKW
					 has_government = postpolonism
				}
			set_cosmetic_tag = EKW_slonceperu_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_b.54 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = SAS
				}
			set_cosmetic_tag = SAS_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.55 }
				}
			}
			
		}		
		

		else_if = {
			limit = { 
					 original_tag = ROS
				}
			set_cosmetic_tag = ROS_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.56 }
				}
			}
			
		}		

		else_if = {
			limit = { 
					 original_tag = ANA
				}
			set_cosmetic_tag = ANA_kustosz_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.57 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = MNL
				}
			set_cosmetic_tag = MNL_amarena_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.58 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = ZUL
				}
			set_cosmetic_tag = ZUL_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.59 }
				}
			}
			
		}		

		else_if = {
			limit = { 
					 original_tag = CYG
					 has_government = neonationalism
				}
			set_cosmetic_tag = CYG_bagno_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.60 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = CYG
					 has_government = euroliberalism
				}
			set_cosmetic_tag = CYG_donwasyl_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.60 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = SLU
				}
			set_cosmetic_tag = SLU_peja_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.61 }
				}
			}
			
		}		
		
		else_if = {
			limit = { 
					 original_tag = PRK
				}
			set_cosmetic_tag = PRK_zero_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.62 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = MNI
				}
			set_cosmetic_tag = MNI_galla_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.63 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = MNI
				}
			set_cosmetic_tag = MNI_merkel_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.63 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = OPO
				}
			set_cosmetic_tag = OPO_cookies_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.64 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = OPO
				}
			set_cosmetic_tag = OPO_ojczyzna_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.64 }
				}
			}
			
		}		
		
		else_if = {
			limit = { 
					 original_tag = LCH
					 has_government = reactionary
				}
			set_cosmetic_tag = LCH_bieszk_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.65 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = WAN
					 has_government = squattism
				}
			set_cosmetic_tag = WAN_szydlowski_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.66 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = WAN
					 has_government = postpolonism
				}
			set_cosmetic_tag = WAN_siwa_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.66 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = LEH
					 has_government = postpolonism
				}
			set_cosmetic_tag = LEH_sanjaj_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.67 }
				}
			}
			
		}	

		else_if = {
			limit = { 
					 original_tag = NYS
				}
			set_cosmetic_tag = NYS_juche_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.68 }
				}
			}
			
		}	

		else_if = {
			limit = { 
					 original_tag = ZAG
				}
			set_cosmetic_tag = ZAG_pancerni_final
			#TODO-EVENT
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.69 }
				}
			}
		}

		else_if = {
			limit = { 
					 original_tag = WIN
				}
			set_cosmetic_tag = WIN_wino_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.70 }
				}
			}
		}
	

		else_if = {
			limit = { 
					 original_tag = KAM
				}
			set_cosmetic_tag = KAM_kampfgruppe_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.71 }
				}
			}
		}	

		else_if = {
			limit = { 
					 original_tag = MOT
				}
			set_cosmetic_tag = MOT_gorzow_final
			#TODO-EVENT
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.72 }
				}
			}
		}

		else_if = {
			limit = { 
					 original_tag = JAS
					 has_government = postpolonism
				}
			set_cosmetic_tag = JAS_jablonowski_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.73 }
				}
			}
		}		

		else_if = {
			limit = { 
					 original_tag = JAS
					 has_government = autocracy
				}
			set_cosmetic_tag = JAS_poreba_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.731 }
				}
			}
		}		

		else_if = {
			limit = { 
					 original_tag = SNF
				}
			set_cosmetic_tag = SNF_sendecki_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.732 }
				}
			}
		}

		else_if = {
			limit = { 
					 original_tag = BOR
				}
			set_cosmetic_tag = BOR_poreba_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.733 }
				}
			}
		}

		else_if = {
			limit = { 
					 original_tag = STE
					 has_country_leader = {
						character = STE_piotr_krzystek
						ruling_only = yes
						}
				}
			set_cosmetic_tag = STE_krystek_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.74 }
				}
			}
		}
		
		else_if = {
			limit = { 
					 original_tag = STE
					 has_government = postpolonism
				}
			set_cosmetic_tag = STE_jacyna_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.74 }
				}
			}
		}
		
		else_if = {
			limit = { 
					 original_tag = STE
					 has_government = corporativism
				}
			set_cosmetic_tag = STE_hanza_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_b.74 }
				}
			}
		}	
		
		else_if = {
			limit = { 
					 original_tag = SWI
					 has_government = marxism_cranism
				}
			set_cosmetic_tag = SWI_zmurkiewicz_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.75 }
				}
			}
		}
		
		else_if = {
			limit = { 
					 original_tag = SWI
					 has_government = neonationalism
				}
			set_cosmetic_tag = SWI_brudzinski_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.75 }
				}
			}
		}		
		
		else_if = {
			limit = { 
					 original_tag = JOM
					 has_government = reactionary
				}
			set_cosmetic_tag = JOM_harald_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.76 }
				}
			}
		}
		
		else_if = {
			limit = { 
					 original_tag = JOM
					 has_government = squattism
				}
			set_cosmetic_tag = JOM_halfdan_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.76 }
				}
			}
		}
		
		else_if = {
			limit = { 
					 original_tag = JOM
					 has_government = socialdemocrature
				}
			set_cosmetic_tag = JOM_zawisza_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_b.76 }
				}
			}
		}

		else_if = {
			limit = { 
					 original_tag = USP
					 has_government = freemarketism
				}
			set_cosmetic_tag = USP_murrica_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.77 }
				}
			}
		}
		
		else_if = {
			limit = { 
					 original_tag = USP
					 has_government = postpolonism
				}
			set_cosmetic_tag = USP_nemezis_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.77 }
				}
			}
		}		

		else_if = {
			limit = { 
					 original_tag = MPK
				}
			set_cosmetic_tag = MPK_tramwaje_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.78 }
				}
			}
		}			

		
		else_if = {
			limit = { 
					 original_tag = INO
				}
			set_cosmetic_tag = INO_polak_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.79 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = TOR
					 has_government = corporativism
				}
			set_cosmetic_tag = TOR_krol_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.80 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = TOR
					 has_government = reactionary
				}
			set_cosmetic_tag = TOR_rydzyk_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.80 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = MEM
				}
			set_cosmetic_tag = MEM_memcen_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_b.80 }
				}
			}
			
		}

		
		else_if = {
			limit = { 
					 original_tag = GRU
				}
			set_cosmetic_tag = GRU_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.81 }
				}
			}
		}	

		else_if = {
			limit = { 
					 original_tag = KUJ
				}
			set_cosmetic_tag = KUJ_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.82 }
				}
			}
		}		

		else_if = {
			limit = { original_tag = BYD 
			          has_government = euroliberalism 
					}
			set_cosmetic_tag = BYD_final_bru
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.83 }
				}
			}
		}

		else_if = {
			limit = { original_tag = BYD 
			          has_government = neonationalism 
					}
			set_cosmetic_tag = BYD_final_scb
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.83 } #do zmiany
				}
			}
		}	
		
		else_if = {
			limit = { original_tag = BYD 
			          has_government = paternal_conservatism 
					}
			set_cosmetic_tag = BYD_final_pre
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.83 } #do zmiany
				}
			}
		}			

		else_if = {
			limit = { 
					 original_tag = OWS
				}
			set_cosmetic_tag = OWS_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.84 }
				}
			}
		}

		else_if = {
			limit = { 
					 original_tag = BOL
				}
			set_cosmetic_tag = BOL_bolek_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.85 }
				}
			}
		}

		else_if = {
			limit = { 
					 original_tag = GDA
				}
			set_cosmetic_tag = GDA_dulkiewicz_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.86 }
				}
			}
		}			

		else_if = {
			limit = { 
					 original_tag = MPR
				}
			set_cosmetic_tag = MPR_plazynski_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.86 }
				}
			}
		}
		
		else_if = {
			limit = { 
					 original_tag = GAY
				}
			set_cosmetic_tag = GAY_biedron_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.87 }
				}
			}
		}	
		
		else_if = {
			limit = { 
					 original_tag = HEL
				}
			set_cosmetic_tag = HEL_dziambor_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.89 }
				}
			}
		}			
		
		else_if = {
			limit = { 
					 original_tag = CZL
					 has_country_leader = {
						character = CZL_krzysztof_jackowski
						ruling_only = yes
						}
				}
			set_cosmetic_tag = CZL_jackowski_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.90 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = CZL
					 has_country_leader = {
						character = CZL_ezoteriusz_maximus
						ruling_only = yes
						}
				}
			set_cosmetic_tag = CZL_ezoteriusz_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.90 }
				}
			}
			
		}		

		else_if = {
			limit = { 
					 original_tag = LOT
				}
			set_cosmetic_tag = LOT_cejro_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.91 }
				}
			}
			
		}		
		
		else_if = {
			limit = { 
					 original_tag = KOR
				}
			set_cosmetic_tag = KOR_korsarz_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.92 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = MAL
				}
			set_cosmetic_tag = MAL_bubel_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.93 }
				}
			}
			
		}		

		else_if = {
			limit = { 
					 original_tag = TEU
				}
			set_cosmetic_tag = TEU_hochmajster_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.94 }
				}
			}
			
		}	
		
		else_if = {
			limit = { 
				OR = {
				original_tag = PRU
				original_tag = GAL
				}
				}
			set_cosmetic_tag = PRU_prusy_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.95 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = PZW
				}
			set_cosmetic_tag = PZW_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.96 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = WRM
				}
			set_cosmetic_tag = WRM_taliban_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.97 }
				}
			}
			
		}		
		
		else_if = {
			limit = { 
					 original_tag = SAN
					 has_government = reactionary
				}
			set_cosmetic_tag = SAN_sanacja_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.98 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = SAN
					 has_government = autocracy
				}
			set_cosmetic_tag = SAN_potocki_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.98 }
				}
			}
			
		}	
		
		else_if = {
			limit = { 
					 original_tag = TAT
					 has_government = neonationalism
				}
			set_cosmetic_tag = TAT_tatarzy_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.99 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = TAT
					 has_government = reactionary
				}
			set_cosmetic_tag = TAT_mongolia_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.99 }
				}
			}
			
		}			
		
		else_if = {
			limit = { 
					 original_tag = BOM
				}
			set_cosmetic_tag = BOM_major_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.100 }
				}
			}
			
		}
		else_if = {
			limit = { 
					 original_tag = KON
				}
			set_cosmetic_tag = KON_knur_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.101 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = BEL
					 has_government = autocracy
				}
			set_cosmetic_tag = BEL_lukaszenka_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.102 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = BEL
					 has_government = paternal_conservatism
				}
			set_cosmetic_tag = BEL_kartoszki_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.102 }
				}
			}
		}	
		
		
		else_if = {
			limit = { 
					 original_tag = LMZ
				}
			set_cosmetic_tag = LMZ_piwo_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.103 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = SDL
				}
			set_cosmetic_tag = SDL_zmiana_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.104 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = AGR
				}
			set_cosmetic_tag = AGR_agrounia_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.105 }
				}
			}
			
		}	

		else_if = {
			limit = { 
					 original_tag = SAT
					 has_government = corporativism
				}
			set_cosmetic_tag = SAT_solorz_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.106 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = SAT
					 has_government = euroliberalism
				}
			set_cosmetic_tag = SAT_sluganarodu_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.106 }
				}
			}
			
		}		

		else_if = {
			limit = { 
					 original_tag = RAD
					 NOT = {
						has_country_leader = {
							character = RAD_pierre_dadak
							ruling_only = yes
							}	
					 }
				}
			set_cosmetic_tag = RAD_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.107 }
				}
			}
			
		}		

		else_if = {
			limit = { 
					 original_tag = GAR
				}
			set_cosmetic_tag = GAR_fogiel_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.108 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = SKA
				}
			set_cosmetic_tag = SKA_kopacz_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.109 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = ORL
				}
			set_cosmetic_tag = ORL_orlen_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.110 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = POL
					 has_government = paternal_conservatism
				}
			set_cosmetic_tag = POL_nuda_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.111 }
				}
			}
			
		}	
		
		else_if = {
			limit = { 
					 original_tag = POL
					 has_government = corporativism
				}
			set_cosmetic_tag = POL_bankierzy_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.111 }
				}
			}
			
		}			
		

		else_if = {
			limit = { 
					 original_tag = MAF
				}
			set_cosmetic_tag = MAF_zlom_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.112 }
				}
			}
			
		}	
		
		else_if = {
			limit = { 
					 original_tag = WOW
					 has_government = autocracy
				}
			set_cosmetic_tag = WOW_antoni_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.113 }
				}
			}
		}	
		
		else_if = {
			limit = { 
					 original_tag = WOW
					 has_government = postpolonism
				}
			set_cosmetic_tag = WOW_inka_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.113 }
				}
			}
		}	
		
		else_if = {
			limit = { 
					 original_tag = ZYR
				}
			set_cosmetic_tag = ZYR_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.114 }
				}
			}
		}		

		else_if = {
			limit = { 
					 original_tag = DUP
					 has_government = freemarketism
				}
			set_cosmetic_tag = DUP_tanajno_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.115 }
				}
			}
		}	
		
		else_if = {
			limit = { 
					 original_tag = DUP
					 has_government = autocracy
				}
			set_cosmetic_tag = DUP_walaszek_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.115 }
				}
			}
		}			

		else_if = {
			limit = { 
					 original_tag = BIE
				}
			set_cosmetic_tag = BIE_malomiasteczkowy_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.116 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = PLC
				}
			set_cosmetic_tag = PLC_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.117 }
				}
			}
			
		}	

		else_if = {
			limit = { 
					 original_tag = ZIE
					 has_government = squattism
				}
			set_cosmetic_tag = ZIE_spurek_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.118 }
				}
			}
			
		}			

		else_if = {
			limit = { 
					 original_tag = ZIE
					 has_government = postpolonism
				}
			set_cosmetic_tag = ZIE_dziki_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.118 }
				}
			}
			
		}	
		
		else_if = {
			limit = { 
					 original_tag = TVP
				}
			set_cosmetic_tag = TVP_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.119 }
				}
			}
			
		}		
		
		else_if = {
			limit = { 
					 original_tag = TRZ
				}
			set_cosmetic_tag = TRZ_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.120 }
				}
			}
			
		}		

		else_if = {
			limit = { 
					 original_tag = KEL
					 has_government = freemarketism
				}
			set_cosmetic_tag = KEL_rynek_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.121 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = KEL
					 has_government = reactionary
				}
			set_cosmetic_tag = KEL_dziensznura_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.121 }
				}
			}
			
		}
				
		else_if = {
			limit = { 
					 original_tag = KEL
					 has_government = postpolonism
				}
			set_cosmetic_tag = KEL_nihilizm_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_b.121 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = NRA
				}
			set_cosmetic_tag = NRA_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.122 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = NSZ
				}
			set_cosmetic_tag = NSZ_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.123 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = SND
				}
			set_cosmetic_tag = SND_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.124 }
				}
			}
			
		}		
		
		else_if = {
			limit = { 
					 original_tag = KIE
				}
			set_cosmetic_tag = KIE_tarczynski_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.125 }
				}
			}
			
		}
		else_if = {
			limit = { 
					 original_tag = KIE
				}
			set_cosmetic_tag = KIE_sienkiewicz_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.125 }
				}
			}
			
		}
		else_if = {
			limit = { 
					 original_tag = KIE
				}
			set_cosmetic_tag = KIE_liroy_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_b.125 }
				}
			}
			
		}		
		

		else_if = {
			limit = { 
					 original_tag = STO
				}
			set_cosmetic_tag = STO_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.126 }
				}
			}
			
		}			

		else_if = {
			limit = { 
					 original_tag = STA
				}
			set_cosmetic_tag = STA_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.127 }
				}
			}
			
		}		

		else_if = {
			limit = { 
					 original_tag = RZE
					 has_government = postpolonism
				}
			set_cosmetic_tag = RZE_regent_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.128 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = RZE
					 has_government = autocracy
				}
			set_cosmetic_tag = RZE_breun_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.128 }
				}
			}
			
		}		

		else_if = {
			limit = { 
					 original_tag = CHR
				}
			set_cosmetic_tag = CHR_natanek_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.129 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = UPA
				}
			set_cosmetic_tag = UPA_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.130 }
				}
			}
			
		}			

		else_if = {
			limit = { 
					 original_tag = KOZ
				}
			set_cosmetic_tag = KOZ_kozacy_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.131 }
				}
			}
			
		}			
		
		else_if = {
			limit = { 
					 original_tag = BSC
				}
			set_cosmetic_tag = BSC_ksu_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.132 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = KRO
				}
			set_cosmetic_tag = KRO_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.133 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = TAR
				}
			set_cosmetic_tag = TAR_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.134 }
				}
			}
			
		}		
		
		else_if = {
			limit = { 
					 original_tag = LGP
				}
			set_cosmetic_tag = LGP_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.135 }
				}
			}
			
		}		
		
		else_if = {
			limit = { 
					 original_tag = KRK
					 has_government = paternal_conservatism
				}
			set_cosmetic_tag = KRK_majchrowski_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.136 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = KRK
					 has_government = autocracy
				}
			set_cosmetic_tag = KRK_krol_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.136 }
				}
			}
			
		}	
		
		#else_if = {
		#	limit = { 
		#			 original_tag = KRK
		#			 has_government = corporativism
		#		}
		#	set_cosmetic_tag = KRK_rektor_final
		#	hidden_effect = {
		#		every_country = {
		#			limit = { NOT = { is_ai = yes } }
		#			news_event = { id = super_b.136 }
		#		}
		#	}
		#	
		#}	
		
		else_if = {
			limit = { 
					 original_tag = NHT
				}
			set_cosmetic_tag = NHT_krak_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.137 }
				}
			}
			
		}			

		else_if = {
			limit = { 
					 original_tag = WDW
					 has_government = marxism_cranism
				}
			set_cosmetic_tag = WDW_mao_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.138 }
				}
			}
			
		}	
		
		else_if = {
			limit = { 
					 original_tag = WDW
					 has_government = postpolonism
				}
			set_cosmetic_tag = WDW_polpot_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.138 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = TRN
					 has_government = euroliberalism
				}
			set_cosmetic_tag = TRN_psl_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.139 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = TRN
					 has_government = autocracy
				}
			set_cosmetic_tag = TRN_baszar_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.139 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = OSP
				}
			set_cosmetic_tag = OSP_pawlak_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_b.139 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = OSW
				}
			set_cosmetic_tag = OSW_beata_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.140 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = UMN
				}
			set_cosmetic_tag = UMN_solniczka_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.141 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = HOR
					 has_government = postpolonism
				}
			set_cosmetic_tag = HOR_horda_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.142 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = HOR
					 has_government = autocracy
				}
			set_cosmetic_tag = HOR_papiez_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super_a.142 }
				}
			}
			
		}		

		else_if = {
			limit = { 
					 original_tag = KRA
				}
			set_cosmetic_tag = KRA_gibala_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.143 }
				}
			}
			
		}	
	
		else_if = {
			limit = { 
					 original_tag = BRK
				}
			set_cosmetic_tag = BRK_korwin_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.144 }
				}
			}
			
		}	
			
		else_if = {
			limit = { 
					 original_tag = WND
				}
			set_cosmetic_tag = WND_popis_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.145 }
				}
			}
			
		}			

		else_if = {
			limit = { 
					 original_tag = HAB
				}
			set_cosmetic_tag = HAB_maklowicz_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.146 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = AGH
				}
			set_cosmetic_tag = AGH_agh_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.147 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = UJG
				}
			set_cosmetic_tag = UJG_faust_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.148 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = AMA
				}
			set_cosmetic_tag = AMA_kobiety_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.149 }
				}
			}
			
		}	


		else_if = {
			limit = { 
					 original_tag = HOG
				}
			set_cosmetic_tag = HOG_hoga_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.151 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = WRT
				}
			set_cosmetic_tag = WRT_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.153 }
				}
			}
			
		}		
		
		else_if = {
			limit = { 
					 original_tag = TEN
				}
			set_cosmetic_tag = TEN_sznuk_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.155 }
				}
			}
			
		}		
	
		else_if = {
			limit = { 
					 original_tag = ATM
				}
			
			set_cosmetic_tag = ATM_krucjata_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.156 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = KCZ
				}
			
			set_cosmetic_tag = KCZ_pawlowicz_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.161 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = URS
				}
			
			set_cosmetic_tag = URS_homokomando_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.162 }
				}
			}
			
		}		

		else_if = {
			limit = { 
					 original_tag = ITA
				}
			
			set_cosmetic_tag = ITA_solar_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.163 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = WOL
				}
			
			set_cosmetic_tag = WOL_mafia_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.164 }
				}
			}
			
		}		

		else_if = {
			limit = { 
					 original_tag = NOR
				}
			
			set_cosmetic_tag = NOR_onz_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.166 }
				}
			}
			
		}		

		else_if = {
			limit = { 
					 original_tag = BLO
				}
			
			set_cosmetic_tag = BLO_bloki_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.167 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = MAR
				}
			
			set_cosmetic_tag = MAR_mariawitki_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.170 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = PAB
				}
			
			set_cosmetic_tag = PAB_piechocinski_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.173 }
				}
			}
			
		}	
		
		else_if = {
			limit = { 
					 original_tag = TVI
				}
			
			set_cosmetic_tag = TVI_vietnam_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.175 }
				}
			}
			
		}		
		
		else_if = {
			limit = { 
					 original_tag = NPL
				}
			
			set_cosmetic_tag = NPL_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.176 }
				}
			}
			
		}		

		else_if = {
			limit = { 
					 original_tag = BRS
				}
			
			set_cosmetic_tag = BRS_optimus_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.179 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = CHW
				}
			
			set_cosmetic_tag = CHW_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.180 }
				}
			}
			
		}	
		
		else_if = {
			limit = { 
					 original_tag = ELB
				}
			
			set_cosmetic_tag = ELB_hanza_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.182 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = LKS
				}
			
			set_cosmetic_tag = LKS_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.188 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = ROZ
				}
			
			set_cosmetic_tag = ROZ_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.195 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = ZBK
				}
			
			set_cosmetic_tag = ZBK_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.196 }
				}
			}
			
		}		
		
		else_if = {
			limit = { 
					 original_tag = TOU
				}
			
			set_cosmetic_tag = TOU_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.199 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = ORD
				}
			
			set_cosmetic_tag = ORD_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.209 }
				}
			}
			
		}
		
		else_if = {
			limit = { 
					 original_tag = LIS
				}
			
			set_cosmetic_tag = LIS_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.187 }
				}
			}
			
		}

		else_if = {
			limit = { 
					 original_tag = LSZ
				}
			
			set_cosmetic_tag = LSZ_final
			hidden_effect = {
				every_country = {
					limit = { NOT = { is_ai = yes } }
					news_event = { id = super.157 }
				}
			}
			
		}				
	
	}
	ai_will_do = { 
		factor = 1
	}
}


}
