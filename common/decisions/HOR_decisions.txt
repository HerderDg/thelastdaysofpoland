HOR_pilgrimage = {

	HOR_exodus_decision = {

		icon = GFX_decision_generic_prepare_civil_war
		
		allowed = {
			original_tag = HOR
		}
		
		target_array = global.procession_targets
		target_trigger = {
			FROM = {
			every_neighbor_country = {
				is_hosting_exile = HOR
			}
			}
		}

		visible = {
			original_tag = HOR
			is_government_in_exile = yes
			FROM = {
			any_neighbor_country = {
				is_hosting_exile = HOR
			}
			}
		}

		available = {
			FROM = {
			any_neighbor_country = {
				is_hosting_exile = HOR
			}
			}
			FROM = { NOT = { has_country_flag = HOR_procession_negotiation_failed } }
			FROM = { NOT = { has_country_flag = HOR_procession_hosted } }
			any_country = { NOT = { has_country_flag = HOR_procession_envoy_sent } }
		}

		cost = 1

		ai_will_do = {
			factor = 100
		}

		days_remove = 3
		complete_effect = {
			hidden_effect = {
				FROM = {
					set_country_flag = HOR_procession_envoy_sent
				}
			}
		}
		remove_effect  = {
			log = "[GetDateText]: [ROOT.GetName] fires decision podlasie_plemiona"
			FROM = {
				country_event = {
					id = procesja.4
				}
			}
			add_to_array = { global.procession_hosted = FROM }
		}
	}	

}