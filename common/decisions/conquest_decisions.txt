regional_conquest_category = {
	attack_other_voi = {
		visible = {
			ROOT = {
				has_war = no
				not = {
					has_country_flag = getting_ready_to_war
				}
			}
			has_country_flag = unified_voivodeship
		}
		available = {
			date > "2031.12.01"
			custom_trigger_tooltip = {
				and = {
					is_podlasie_unifier  = no
					is_capital_district = no
				}
				tooltip = "Is NOT a Podlachia or Capital District unifier"
			}
		}
		target_array = root.neighbors
		target_trigger = {
			FROM = {
				has_country_flag = unified_voivodeship
				not = {
					or = {
						has_war = yes
						#has_country_flag = getting_ready_to_war
					} 
				}
			}
		}
		war_with_target_on_complete = yes
		ai_will_do = {
			base = 1000
			modifier = {
				date < "2033.12.01"
				factor = 0
				or = {
					from = {
						or = {
							is_podlasie = yes
							is_capital_district = yes
						}
					}
					# warszawka <-> kujawy
					and = {
						ROOT = {
							is_mazovia = yes
						}
						from = {
							not = {
								is_kuyavia = yes
							}
						}
					}
					and = {
						ROOT = {
							is_kuyavia = yes
						}
						from = {
							not = {
								is_mazovia = yes
							}
						}
					}
					# szczecin <-> gdańsk
					and = {
						ROOT = {
							is_pomerelia = yes
						}
						from = {
							not = {
								is_west_pomerania = yes
							}
						}
					}
					and = {
						ROOT = {
							is_west_pomerania = yes
						}
						from = {
							not = {
								is_pomerelia = yes
							}
						}
					}
					# wielkopolska
					and = {
						ROOT = {
							is_southern_greater_poland  = yes
						}
						from = {
							not = {
								is_northern_greater_poland = yes
							}
						}
					}
					and = {
						ROOT = {
							is_northern_greater_poland = yes
						}
						from = {
							not = {
								is_southern_greater_poland = yes
							}
						}
					}
					# wrocław <-> legnica
					and = {
						ROOT = {
							is_lower_silesia  = yes
						}
						from = {
							not = {
								is_legnica = yes
							}
						}
					}
					and = {
						ROOT = {
							is_legnica = yes
						}
						from = {
							not = {
								is_lower_silesia = yes
							}
						}
					}
					# wrocław <-> legnica
					and = {
						ROOT = {
							is_lower_silesia  = yes
						}
						from = {
							not = {
								is_legnica = yes
							}
						}
					}
					and = {
						ROOT = {
							is_legnica = yes
						}
						from = {
							not = {
								is_lower_silesia = yes
							}
						}
					}
					# opole <-> katowice
					and = {
						ROOT = {
							is_opolskie  = yes
						}
						from = {
							not = {
								is_upper_silesia = yes
							}
						}
					}
					and = {
						ROOT = {
							is_upper_silesia = yes
						}
						from = {
							not = {
								is_opolskie = yes
							}
						}
					}
					# łódź <-> częstochowa
					and = {
						ROOT = {
							is_lodzkie  = yes
						}
						from = {
							not = {
								is_zaglebie_and_jura = yes
							}
						}
					}
					and = {
						ROOT = {
							is_zaglebie_and_jura = yes
						}
						from = {
							not = {
								is_lodzkie = yes
							}
						}
					}
					# krakow <-> tatry
					and = {
						ROOT = {
							is_tatry_region  = yes
						}
						from = {
							not = {
								is_lesser_poland = yes
							}
						}
					}
					and = {
						ROOT = {
							is_lesser_poland = yes
						}
						from = {
							not = {
								is_tatry_region = yes
							}
						}
					}
					# radom <-> kielce
					and = {
						ROOT = {
							is_radom_province = yes
						}
						from = {
							not = {
								is_holy_cross = yes
							}
						}
					}
					and = {
						ROOT = {
							is_holy_cross = yes
						}
						from = {
							not = {
								is_radom_province = yes
							}
						}
					}
					# lublin <-> podkarpacie
					and = {
						ROOT = {
							is_subcarpathia = yes
						}
						from = {
							not = {
								is_lubelskie = yes
							}
						}
					}
					and = {
						ROOT = {
							is_lubelskie = yes
						}
						from = {
							not = {
								is_subcarpathia = yes
							}
						}
					}
				}
			}
		}
		cost = 100
		days_remove = 14
		complete_effect = {
			set_country_flag = getting_ready_to_war
			log = "[GetDateText]: [ROOT.GetName] is getting ready to war..."
		}
		remove_effect = {
			declare_war_on = {
				target = FROM
				type = annex_everything
			}
			clr_country_flag = getting_ready_to_war
		}
	}
	final_solution_of_zigger_question = {
		visible = {
			is_warmia_and_masuria = yes
			country_exists = ROS
		}
		available = {
			date > "2031.12.01"
			has_war = no
		}
		cost = 100
		days_remove = 14
		ai_will_do = {
			base = 9001
		}
		fire_only_once = yes
		complete_effect = {
			set_country_flag = getting_ready_to_war
			log = "[GetDateText]: [ROOT.GetName] is getting ready to war..."
		}
		remove_effect = {
			declare_war_on = {
				target = ROS
				type = annex_everything
			}
			clr_country_flag = getting_ready_to_war
		}
	}
	sweep_westpom_warlords = {
		visible = {
		 	any_neighbor_country = {
				is_west_pomerania = yes
				NOT = {
					has_country_flag = unified_voivodeship
					has_country_flag = unified_regional
					has_country_flag = unified_supregional
				}
			}
		}
		available = {
			date > "2033.12.01"
		}
		ai_will_do = {
			base = 1000
			modifier = {
				date < "2034.12.01"
				factor = 0
				not = {
					is_pomerelia = yes
				}
			}
		}
		days_remove = 7
		remove_effect = {
			every_country = {
				limit = {
					is_west_pomerania = yes
				}
				ROOT = {
					declare_war_on = {
						target = PREV
					}
				}
			}
		}

	}
	sweep_pomerelia_warlords = {
		visible = {
			has_war = no
		 	any_neighbor_country = {
				is_pomerelia = yes
				NOT = {
					has_country_flag = unified_voivodeship
					has_country_flag = unified_regional
					has_country_flag = unified_supregional
				}
			}
		}
		available = {
			date > "2033.12.01"
		}
		ai_will_do = {
			base = 1000
			modifier = {
				date < "2034.12.01"
				factor = 0
				not = {
					is_west_pomerania = yes
				}
			}
		}
		fire_only_once = yes
		days_remove = 7
		remove_effect = {
			every_country = {
				limit = {
					is_pomerelia = yes
				}
				ROOT = {
					declare_war_on = {
						target = PREV
						type = annex_everything
					}
				}
			}
		}

	}
	sweep_kuyavia_warlords = {
		visible = {
			has_war = no
		 	any_neighbor_country = {
				is_kuyavia = yes
				NOT = {
					has_country_flag = unified_voivodeship
					has_country_flag = unified_regional
					has_country_flag = unified_supregional
				}
			}
		}
		available = {
			date > "2033.12.01"
		}
		ai_will_do = {
			base = 1000
			modifier = {
				date < "2034.12.01"
				factor = 0
				not = {
					is_mazovia = yes
				}
			}
		}
		fire_only_once = yes
		days_remove = 7
		remove_effect = {
			every_country = {
				limit = {
					is_kuyavia = yes
				}
				ROOT = {
					declare_war_on = {
						target = PREV
						type = annex_everything
					}
				}
			}
		}

	}
	sweep_mazovia_warlords = {
		visible = {
			has_war = no
		 	any_neighbor_country = {
				is_mazovia = yes
				NOT = {
					has_country_flag = unified_voivodeship
					has_country_flag = unified_regional
					has_country_flag = unified_supregional
				}
			}
		}
		available = {
			date > "2033.12.01"
		}
		ai_will_do = {
			base = 1000
			modifier = {
				date < "2034.12.01"
				factor = 0
				not = {
					is_kuyavia = yes
				}
			}
		}
		fire_only_once = yes
		days_remove = 7
		remove_effect = {
			every_country = {
				limit = {
					is_mazovia = yes
				}
				ROOT = {
					declare_war_on = {
						target = PREV
						type = annex_everything
					}
				}
			}
		}

	}
	sweep_lublin_warlords = {
		visible = {
			has_war = no
		 	any_neighbor_country = {
				is_lubelskie = yes
				NOT = {
					has_country_flag = unified_voivodeship
					has_country_flag = unified_regional
					has_country_flag = unified_supregional
				}
			}
		}
		available = {
			date > "2033.12.01"
		}
		ai_will_do = {
			base = 1000
			modifier = {
				date < "2034.12.01"
				factor = 0
				not = {
					is_subcarpathia = yes
				}
			}
		}
		fire_only_once = yes
		days_remove = 7
		remove_effect = {
			every_country = {
				limit = {
					is_lubelskie  = yes
				}
				ROOT = {
					declare_war_on = {
						target = PREV
						type = annex_everything
					}
				}
			}
		}

	}
	sweep_subcarpathia_warlords = {
		visible = {
			has_war = no
		 	any_neighbor_country = {
				is_subcarpathia = yes
				NOT = {
					has_country_flag = unified_voivodeship
					has_country_flag = unified_regional
					has_country_flag = unified_supregional
				}
			}
		}
		available = {
			date > "2033.12.01"
		}
		ai_will_do = {
			base = 1000
			modifier = {
				date < "2034.12.01"
				factor = 0
				not = {
					is_lubelskie = yes
				}
			}
		}
		fire_only_once = yes
		days_remove = 7
		remove_effect = {
			every_country = {
				limit = {
					is_subcarpathia = yes
				}
				ROOT = {
					declare_war_on = {
						target = PREV
						type = annex_everything
					}
				}
			}
		}

	}
	sweep_holycross_warlords = {
		visible = {
			has_war = no
		 	any_neighbor_country = {
				is_holy_cross = yes
				NOT = {
					has_country_flag = unified_voivodeship
					has_country_flag = unified_regional
					has_country_flag = unified_supregional
				}
			}
		}
		available = {
			date > "2033.12.01"
		}
		ai_will_do = {
			base = 1000
			modifier = {
				date < "2034.12.01"
				factor = 0
				not = {
					is_radom_province = yes
				}
			}
		}
		fire_only_once = yes
		days_remove = 7
		remove_effect = {
			every_country = {
				limit = {
					is_holy_cross = yes
				}
				ROOT = {
					declare_war_on = {
						target = PREV
						type = annex_everything
					}
				}
			}
		}

	}
	sweep_radom_warlords = {
		visible = {
			has_war = no
		 	any_neighbor_country = {
				is_radom_province = yes
				NOT = {
					has_country_flag = unified_voivodeship
					has_country_flag = unified_regional
					has_country_flag = unified_supregional
				}
			}
		}
		available = {
			date > "2033.12.01"
		}
		ai_will_do = {
			base = 1000
			modifier = {
				date < "2034.12.01"
				factor = 0
				not = {
					is_holy_cross = yes
				}
			}
		}
		fire_only_once = yes
		days_remove = 7
		remove_effect = {
			every_country = {
				limit = {
					is_radom_province = yes
				}
				ROOT = {
					declare_war_on = {
						target = PREV
						type = annex_everything
					}
				}
			}
		}

	}
	sweep_tatry_warlords = {
		visible = {
			has_war = no
		 	any_neighbor_country = {
				is_tatry_region = yes
				NOT = {
					has_country_flag = unified_voivodeship
					has_country_flag = unified_regional
					has_country_flag = unified_supregional
				}
			}
		}
		available = {
			date > "2033.12.01"
		}
		ai_will_do = {
			base = 1000
			modifier = {
				date < "2034.12.01"
				factor = 0
				not = {
					is_lesser_poland  = yes
				}
			}
		}
		fire_only_once = yes
		days_remove = 7
		remove_effect = {
			every_country = {
				limit = {
					is_tatry_region = yes
				}
				ROOT = {
					declare_war_on = {
						target = PREV
						type = annex_everything
					}
				}
			}
		}

	}
	sweep_lesserpoland_warlords = {
		visible = {
			has_war = no
		 	any_neighbor_country = {
				is_lesser_poland = yes
				NOT = {
					has_country_flag = unified_voivodeship
					has_country_flag = unified_regional
					has_country_flag = unified_supregional
				}
			}
		}
		available = {
			date > "2033.12.01"
		}
		ai_will_do = {
			base = 1000
			modifier = {
				date < "2034.12.01"
				factor = 0
				not = {
					is_tatry_region  = yes
				}
			}
		}
		fire_only_once = yes
		days_remove = 7
		remove_effect = {
			every_country = {
				limit = {
					is_lesser_poland = yes
				}
				ROOT = {
					declare_war_on = {
						target = PREV
						type = annex_everything
					}
				}
			}
		}

	}
	sweep_zaglebie_warlords = {
		visible = {
			has_war = no
		 	any_neighbor_country = {
				is_zaglebie_and_jura = yes
				NOT = {
					has_country_flag = unified_voivodeship
					has_country_flag = unified_regional
					has_country_flag = unified_supregional
				}
			}
		}
		available = {
			date > "2033.12.01"
		}
		ai_will_do = {
			base = 1000
			modifier = {
				date < "2034.12.01"
				factor = 0
				not = {
					is_lodzkie  = yes
				}
			}
		}
		fire_only_once = yes
		days_remove = 7
		remove_effect = {
			every_country = {
				limit = {
					is_zaglebie_and_jura  = yes
				}
				ROOT = {
					declare_war_on = {
						target = PREV
						type = annex_everything
					}
				}
			}
		}

	}
	sweep_lodz_warlords = {
		visible = {
			has_war = no
		 	any_neighbor_country = {
				is_lodzkie = yes
				NOT = {
					has_country_flag = unified_voivodeship
					has_country_flag = unified_regional
					has_country_flag = unified_supregional
				}
			}
		}
		available = {
			date > "2033.12.01"
		}
		ai_will_do = {
			base = 1000
			modifier = {
				date < "2034.12.01"
				factor = 0
				not = {
					is_zaglebie_and_jura = yes
				}
			}
		}
		fire_only_once = yes
		days_remove = 7
		remove_effect = {
			every_country = {
				limit = {
					is_lodzkie  = yes
				}
				ROOT = {
					declare_war_on = {
						target = PREV
						type = annex_everything
					}
				}
			}
		}

	}
	sweep_opole_warlords = {
		visible = {
			has_war = no
		 	any_neighbor_country = {
				is_opolskie = yes
				NOT = {
					has_country_flag = unified_voivodeship
					has_country_flag = unified_regional
					has_country_flag = unified_supregional
				}
			}
		}
		available = {
			date > "2033.12.01"
		}
		ai_will_do = {
			base = 1000
			modifier = {
				date < "2034.12.01"
				factor = 0
				not = {
					is_upper_silesia = yes
				}
			}
		}
		fire_only_once = yes
		days_remove = 7
		remove_effect = {
			every_country = {
				limit = {
					is_opolskie = yes
				}
				ROOT = {
					declare_war_on = {
						target = PREV
						type = annex_everything
					}
				}
			}
		}

	}
	sweep_upper_silesian_warlords = {
		visible = {
			has_war = no
		 	any_neighbor_country = {
				is_upper_silesia = yes
				NOT = {
					has_country_flag = unified_voivodeship
					has_country_flag = unified_regional
					has_country_flag = unified_supregional
				}
			}
		}
		available = {
			date > "2033.12.01"
		}
		ai_will_do = {
			base = 1000
			modifier = {
				date < "2034.12.01"
				factor = 0
				not = {
					is_opolskie  = yes
				}
			}
		}
		fire_only_once = yes
		days_remove = 7
		remove_effect = {
			every_country = {
				limit = {
					is_upper_silesia  = yes
				}
				ROOT = {
					declare_war_on = {
						target = PREV
						type = annex_everything
					}
				}
			}
		}

	}
	sweep_legnica_warlords = {
		visible = {
			has_war = no
		 	any_neighbor_country = {
				is_legnica = yes
				NOT = {
					has_country_flag = unified_voivodeship
					has_country_flag = unified_regional
					has_country_flag = unified_supregional
				}
			}
		}
		available = {
			date > "2033.12.01"
		}
		ai_will_do = {
			base = 1000
			modifier = {
				date < "2034.12.01"
				factor = 0
				not = {
					is_lower_silesia = yes
				}
			}
		}
		fire_only_once = yes
		days_remove = 7
		remove_effect = {
			every_country = {
				limit = {
					is_legnica = yes
				}
				ROOT = {
					declare_war_on = {
						target = PREV
						type = annex_everything
					}
				}
			}
		}

	}
	sweep_lower_silesian_warlords = {
		visible = {
			has_war = no
		 	any_neighbor_country = {
				is_lower_silesia = yes
				NOT = {
					has_country_flag = unified_voivodeship
					has_country_flag = unified_regional
					has_country_flag = unified_supregional
				}
			}
		}
		available = {
			date > "2033.12.01"
		}
		ai_will_do = {
			base = 1000
			modifier = {
				date < "2034.12.01"
				factor = 0
				not = {
					is_legnica  = yes
				}
			}
		}
		fire_only_once = yes
		days_remove = 7
		remove_effect = {
			every_country = {
				limit = {
					is_lower_silesia = yes
				}
				ROOT = {
					declare_war_on = {
						target = PREV
						type = annex_everything
					}
				}
			}
		}

	}
	sweep_southern_gp_warlords = {
		visible = {
			has_war = no
		 	any_neighbor_country = {
				is_southern_greater_poland = yes
				NOT = {
					has_country_flag = unified_voivodeship
					has_country_flag = unified_regional
					has_country_flag = unified_supregional
				}
			}
		}
		available = {
			date > "2033.12.01"
		}
		ai_will_do = {
			base = 1000
			modifier = {
				date < "2034.12.01"
				factor = 0
				not = {
					is_northern_greater_poland  = yes
				}
			}
		}
		fire_only_once = yes
		days_remove = 7
		remove_effect = {
			every_country = {
				limit = {
					is_southern_greater_poland = yes
				}
				ROOT = {
					declare_war_on = {
						target = PREV
						type = annex_everything
					}
				}
			}
		}

	}
	sweep_northern_gp_warlords = {
		visible = {
			has_war = no
		 	any_neighbor_country = {
				is_northern_greater_poland = yes
				NOT = {
					has_country_flag = unified_voivodeship
					has_country_flag = unified_regional
					has_country_flag = unified_supregional
				}
			}
		}
		available = {
			date > "2033.12.01"
		}
		ai_will_do = {
			base = 1000
			modifier = {
				date < "2034.12.01"
				factor = 0
				not = {
					is_southern_greater_poland  = yes
				}
			}
		}
		fire_only_once = yes
		days_remove = 7
		remove_effect = {
			every_country = {
				limit = {
					is_northern_greater_poland  = yes
				}
				ROOT = {
					declare_war_on = {
						target = PREV
						type = annex_everything
					}
				}
			}
		}

	}
	
	# declare_yourself_a_region = {
	# 	visible = {
	# 		date > "2031.12.01"
	# 	}
	# 	available = {
	# 		count_triggers = {
	# 			amount = 3
	# 			check_variable = {
	# 				var = lodz_ownership
	# 				value = 0.75
	# 				compare = greater_than_or_equals
	# 			}
	# 			check_variable = {
	# 				var = holy_cross_ownership
	# 				value = 0.75
	# 				compare = greater_than_or_equals
	# 			}
	# 			check_variable = {
	# 				var = zaglebie_and_jura_ownership
	# 				value = 0.75
	# 				compare = greater_than_or_equals
	# 			}
	# 			check_variable = {
	# 				var = upper_silesia_ownership
	# 				value = 0.75
	# 				compare = greater_than_or_equals
	# 			}
	# 			check_variable = {
	# 				var = tatry_region_ownership
	# 				value = 0.75
	# 				compare = greater_than_or_equals
	# 			}
	# 			check_variable = {
	# 				var = opolskie_ownership
	# 				value = 0.75
	# 				compare = greater_than_or_equals
	# 			}
	# 			check_variable = {
	# 				var = lower_silesia_ownership
	# 				value = 0.75
	# 				compare = greater_than_or_equals
	# 			}
	# 			check_variable = {
	# 				var = lubuskie_ownership
	# 				value = 0.75
	# 				compare = greater_than_or_equals
	# 			}
	# 			check_variable = {
	# 				var = west_pomerania_ownership
	# 				value = 0.75
	# 				compare = greater_than_or_equals
	# 			}
	# 			check_variable = {
	# 				var = northern_greater_poland_ownership
	# 				value = 0.75
	# 				compare = greater_than_or_equals
	# 			}
	# 			check_variable = {
	# 				var = kuyavia_ownership
	# 				value = 0.75
	# 				compare = greater_than_or_equals
	# 			}
	# 			check_variable = {
	# 				var = pomerelia_ownership
	# 				value = 0.75
	# 				compare = greater_than_or_equals
	# 			}
	# 			check_variable = {
	# 				var = warmia_and_masuria_ownership
	# 				value = 0.75
	# 				compare = greater_than_or_equals
	# 			}
	# 			check_variable = {
	# 				var = podlasie_ownership
	# 				value = 0.75
	# 				compare = greater_than_or_equals
	# 			}
	# 			check_variable = {
	# 				var = podlasie_ownership
	# 				value = 0.75
	# 				compare = greater_than_or_equals
	# 			}
	# 			check_variable = {
	# 				var = mazovia_ownership
	# 				value = 0.75
	# 				compare = greater_than_or_equals
	# 			}
	# 			check_variable = {
	# 				var = capital_district_ownership
	# 				value = 0.75
	# 				compare = greater_than_or_equals
	# 			}
	# 			check_variable = {
	# 				var = radom_province_ownership
	# 				value = 0.75
	# 				compare = greater_than_or_equals
	# 			}
	# 			check_variable = {
	# 				var = lubelskie_ownership
	# 				value = 0.75
	# 				compare = greater_than_or_equals
	# 			}
	# 			check_variable = {
	# 				var = subcarpathia_ownership
	# 				value = 0.75
	# 				compare = greater_than_or_equals
	# 			}
	# 			check_variable = {
	# 				var = lesser_poland_ownership
	# 				value = 0.75
	# 				compare = greater_than_or_equals
	# 			}
	# 		}
	# 	}
	# 	cost = 100
	# 	ai_will_do = {
	# 		base = 1500
	# 	}
	# 	complete_effect = {
	# 		clr_country_flag = unified_voivodeship
	# 		set_country_flag = unified_regional
	# 		regional_unification_effect = yes
	# 	}
	# }
	declare_yourself_a_region = {
		visible = {
			#date > "2031.12.01"
		}
		available = {
			custom_trigger_tooltip = {
				tooltip = "Is a Podlachia or Capital District unifier"
				or = {
					is_podlasie_unifier = yes
					is_capital_district = yes
				}
			}
		}
		cost = 100
		ai_will_do = {
			base = 1500
		}
		complete_effect = {
			clr_country_flag = unified_voivodeship
			set_country_flag = unified_regional
			regional_unification_effect = yes
		}
	}
}

superregional_conquest_category = {
	attack_other_region = {
		visible = {
			date > "2033.12.01"
			ROOT = {
				has_war = no
				not = {
					has_country_flag = getting_ready_to_war
				}
			}
		}
		target_array = root.neighbors
		target_trigger = {
			FROM = {
				has_country_flag = unified_regional
				not = {
					has_war = yes
				}
			}
		}
		cost = 100
		days_remove = 14
		days_re_enable = 14
		ai_will_do = {
			base = 1000
			modifier = {
				date < "2035.12.01"
				factor = 0
				or = {
					#capital region
					and = {
						is_kuyavia_mazovia_unifier = yes
						from = {
							NOT = {
								is_capital_district = yes
							}
						}
					}
					and = {
						is_capital_district = yes
						from = {
							NOT = {
								is_kuyavia_mazovia_unifier = yes
							}
						}
					}
					#galicia
					and = {
						is_red_ruthenia_unifier = yes
						from = {
							NOT = {
								is_lesser_poland_region_unifier = yes
							}
						}
					}
					and = {
						is_lesser_poland_region_unifier = yes
						from = {
							NOT = {
								is_red_ruthenia_unifier = yes
							}
						}
					}
					#śląąąąąąąąąąąąą
					and = {
						is_greater_lower_silesia_unifier = yes
						from = {
							NOT = {
								is_upper_silesian_unifier = yes
							}
						}
					}
					and = {
						is_upper_silesian_unifier = yes
						from = {
							NOT = {
								is_greater_lower_silesia_unifier = yes
							}
						}
					}
					#central
					and = {
						is_old_polish_basin_unifier  = yes
						from = {
							NOT = {
								is_lodz_jura_unifier = yes
							}
						}
					}
					and = {
						is_lodz_jura_unifier = yes
						from = {
							NOT = {
								is_old_polish_basin_unifier = yes
							}
						}
					}
					#north west
					and = {
						is_greater_poland_unifier  = yes
						from = {
							NOT = {
								is_pomerelia_unifier  = yes
							}
						}
					}
					and = {
						is_pomerelia_unifier = yes
						from = {
							NOT = {
								is_greater_poland_unifier = yes
							}
						}
					}
					#north east
					and = {
						is_warmia_and_masuria = yes
						from = {
							NOT = {
								is_podlasie_unifier = yes
							}
						}
					}
					and = {
						is_podlasie_unifier = yes
						from = {
							NOT = {
								is_warmia_and_masuria = yes
							}
						}
					}
				}
			}
		}
		war_with_target_on_complete = yes
		complete_effect = {
			set_country_flag = getting_ready_to_war
			log = "[GetDateText]: [ROOT.GetName] is getting ready to war..."
		}
		remove_effect = {
			declare_war_on = {
				target = FROM
				type = annex_everything
			}
			clr_country_flag = getting_ready_to_war
		}
	}
	# war_for_silesia = {
	# 	visible = {
	# 		or = {
	# 			is_legnica = yes
	# 			is_lower_silesia = yes
	# 			is_zaglebie_and_jura = yes
	# 			is_upper_silesia = yes
	# 		}
	# 		date > "2034.01.01"
	# 	}
	# 	available = {
	# 		has_war = no
	# 	}
	# 	cost = 100
	# 	fire_only_once = yes
	# 	days_remove = 14
	# 	remove_effect = {
	# 		every_country = {
	# 			limit = {
	# 				exists = yes
	# 				or = {
	# 					is_legnica = yes
	# 					is_lower_silesia = yes
	# 					is_opolskie = yes
	# 					is_upper_silesia = yes
	# 				}
	# 				not = {
	# 					original_tag = ROOT
	# 				}
	# 			}
	# 			root = {
	# 				declare_war_on = {
	# 					target = prev
	# 					type = annex_everything
	# 				}
	# 			}
	# 		}
	# 	}
	# }
	# war_for_galicia = {
	# 	visible = {
	# 		or = {
	# 			is_tatry_region = yes
	# 			is_lesser_poland = yes
	# 			is_zaglebie_and_jura = yes
	# 			is_subcarpathia = yes
	# 			is_holy_cross = yes
	# 			is_lubelskie = yes
	# 			is_radom_province = yes
	# 		}
	# 		date > "2032.01.01"
	# 	}
	# 	available = {
	# 		has_war = no
	# 	}
	# 	cost = 100
	# 	fire_only_once = yes
	# 	days_remove = 14
	# 	remove_effect = {
	# 		every_country = {
	# 			limit = {
	# 				exists = yes
	# 				or = {
	# 					is_tatry_region = yes
	# 					is_lesser_poland = yes
	# 					is_zaglebie_and_jura = yes
	# 					is_subcarpathia = yes
	# 					is_holy_cross = yes
	# 					is_lubelskie = yes
	# 					is_radom_province = yes
	# 				}
	# 				not = {
	# 					original_tag = ROOT
	# 				}
	# 			}
	# 			root = {
	# 				declare_war_on = {
	# 					target = prev
	# 					type = annex_everything
	# 				}
	# 			}
	# 		}
	# 	}
	# }
	attack_lesser_vois = {
		visible = {
			ROOT = {
				not = {
					has_country_flag = getting_ready_to_war
				}
			}
			has_country_flag = unified_regional
		}
		available = {
			date > "2036.09.01"
		}
		target_array = root.neighbors
		target_trigger = {
			FROM = {
				is_poland_unifier = yes
				has_country_flag = unified_voivodeship
				NOT = {
					has_country_flag = unified_regional
					has_country_flag = unified_supregional
				}
			}
		}
		ai_will_do = {
			base = 1000
			modifier = {
				date < "2035.12.01"
				factor = 0
				or = {
					#capital region
					and = {
						is_kuyavia_mazovia_unifier = yes
						from = {
							NOT = {
								is_capital_district = yes
							}
						}
					}
					and = {
						is_capital_district = yes
						from = {
							NOT = {
								is_kuyavia_mazovia_unifier = yes
							}
						}
					}
					#galicia
					and = {
						is_red_ruthenia_unifier = yes
						from = {
							NOT = {
								is_lesser_poland_region_unifier = yes
							}
						}
					}
					and = {
						is_lesser_poland_region_unifier = yes
						from = {
							NOT = {
								is_red_ruthenia_unifier = yes
							}
						}
					}
					#śląąąąąąąąąąąąą
					and = {
						is_greater_lower_silesia_unifier = yes
						from = {
							NOT = {
								is_upper_silesian_unifier = yes
							}
						}
					}
					and = {
						is_upper_silesian_unifier = yes
						from = {
							NOT = {
								is_greater_lower_silesia_unifier = yes
							}
						}
					}
					#central
					and = {
						is_old_polish_basin_unifier  = yes
						from = {
							NOT = {
								is_lodz_jura_unifier = yes
							}
						}
					}
					and = {
						is_lodz_jura_unifier = yes
						from = {
							NOT = {
								is_old_polish_basin_unifier = yes
							}
						}
					}
					#north west
					and = {
						is_greater_poland_unifier  = yes
						from = {
							NOT = {
								is_pomerelia_unifier  = yes
							}
						}
					}
					and = {
						is_pomerelia_unifier = yes
						from = {
							NOT = {
								is_greater_poland_unifier = yes
							}
						}
					}
					#north east
					and = {
						is_warmia_and_masuria = yes
						from = {
							NOT = {
								is_podlasie_unifier = yes
							}
						}
					}
					and = {
						is_podlasie_unifier = yes
						from = {
							NOT = {
								is_warmia_and_masuria = yes
							}
						}
					}
				}
			}
		}
		days_remove = 14
		complete_effect = {
			set_country_flag = getting_ready_to_war
			log = "[GetDateText]: [ROOT.GetName] is getting ready to war..."
		}
		remove_effect = {
			declare_war_on = {
				target = FROM
				type = annex_everything
			}
			clr_country_flag = getting_ready_to_war
		}
	}
	declare_yourself_a_superregion = {
		visible = {
			date > "2033.12.01"
		}
		available = {
			custom_trigger_tooltip = {
				tooltip = "Has at least 75% of 6 voivodeships"
			}
			count_triggers = {
				amount = 6
				check_variable = {
					var = lodz_ownership
					value = 0.75
					compare = greater_than_or_equals
				}
				check_variable = {
					var = holy_cross_ownership
					value = 0.75
					compare = greater_than_or_equals
				}
				check_variable = {
					var = zaglebie_and_jura_ownership
					value = 0.75
					compare = greater_than_or_equals
				}
				check_variable = {
					var = upper_silesia_ownership
					value = 0.75
					compare = greater_than_or_equals
				}
				check_variable = {
					var = tatry_region_ownership
					value = 0.75
					compare = greater_than_or_equals
				}
				check_variable = {
					var = opolskie_ownership
					value = 0.75
					compare = greater_than_or_equals
				}
				check_variable = {
					var = lower_silesia_ownership
					value = 0.75
					compare = greater_than_or_equals
				}
				check_variable = {
					var = lubuskie_ownership
					value = 0.75
					compare = greater_than_or_equals
				}
				check_variable = {
					var = west_pomerania_ownership
					value = 0.75
					compare = greater_than_or_equals
				}
				check_variable = {
					var = northern_greater_poland_ownership
					value = 0.75
					compare = greater_than_or_equals
				}
				check_variable = {
					var = kuyavia_ownership
					value = 0.75
					compare = greater_than_or_equals
				}
				check_variable = {
					var = pomerelia_ownership
					value = 0.75
					compare = greater_than_or_equals
				}
				check_variable = {
					var = warmia_and_masuria_ownership
					value = 0.75
					compare = greater_than_or_equals
				}
				check_variable = {
					var = podlasie_ownership
					value = 0.75
					compare = greater_than_or_equals
				}
				check_variable = {
					var = podlasie_ownership
					value = 0.75
					compare = greater_than_or_equals
				}
				check_variable = {
					var = mazovia_ownership
					value = 0.75
					compare = greater_than_or_equals
				}
				check_variable = {
					var = capital_district_ownership
					value = 0.75
					compare = greater_than_or_equals
				}
				check_variable = {
					var = radom_province_ownership
					value = 0.75
					compare = greater_than_or_equals
				}
				check_variable = {
					var = lubelskie_ownership
					value = 0.75
					compare = greater_than_or_equals
				}
				check_variable = {
					var = subcarpathia_ownership
					value = 0.75
					compare = greater_than_or_equals
				}
				check_variable = {
					var = lesser_poland_ownership
					value = 0.75
					compare = greater_than_or_equals
				}
			}
		}
		cost = 100
		ai_will_do = {
			base = 1500
		}
		complete_effect = {
			clr_country_flag = unified_regional
			set_country_flag = unified_supregional
			regional_unification_effect = yes
		}
	}
}

final_conquest_category = {
	attack_other_superregion = {
		visible = {
			date > "2036.01.01"
			ROOT = {
				has_war = no
			}
		}
		target_array = root.neighbors
		target_trigger = {
			FROM = {
				has_country_flag = unified_supregional
				not = {
					or = {
						has_country_flag = getting_ready_to_war
					}
				}
			}
		}
		war_with_target_on_complete = yes
		ai_will_do = {
			base = 1000
		}
		cost = 100
		days_remove = 14
		#cancel_if_not_visible = yes
		cancel_effect = {
			clr_country_flag = getting_ready_to_war
		}
		complete_effect = {
			set_country_flag = getting_ready_to_war
			log = "[GetDateText]: [ROOT.GetName] is getting ready to war..."
		}
		remove_effect = {
			declare_war_on = {
				target = FROM
				type = annex_everything
			}
			clr_country_flag = getting_ready_to_war
		}
	}
	the_sacred_war = {
		visible = {
			or = {
				is_poland_unifier = yes
			}
			date > "2038.01.01"
		}
		available = {
			has_war = no
		}
		cost = 100
		fire_only_once = yes
		days_remove = 14
		ai_will_do = {
			base = 1000
		}
		remove_effect = {
			every_country = {
				limit = {
					exists = yes
					is_poland_unifier = yes
					not = {
						original_tag = ROOT
					}
				}
				root = {
					declare_war_on = {
						target = prev
						type = annex_everything
					}
				}
			}
		}
	}
}