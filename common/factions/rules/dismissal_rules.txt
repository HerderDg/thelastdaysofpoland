
dismissal_rule_nation_eurozone = {
	type = dismissal_rules
	visible = { NOT = { is_eurozone = yes } }
	ai_will_do = {
		base = 0
	}

	dismiss_member_trigger = {
		is_eurozone = yes
	}
}

dismissal_rule_nation_szkop = {
	type = dismissal_rules
	visible = { NOT = { core_is_german = yes } }
	ai_will_do = {
		base = 0
	}

	dismiss_member_trigger = {
		core_is_german = yes
	}
}

dismissal_rule_nation_kacap = {
	type = dismissal_rules
	visible = { NOT = { core_is_kacap = yes } }
	ai_will_do = {
		base = 0
	}

	dismiss_member_trigger = {
		core_is_kacap = yes
	}
}

dismissal_rule_nation_niepolak = {
	type = dismissal_rules
	visible = { NOT = { core_is_niepolak = yes } }
	ai_will_do = {
		base = 0
	}

	dismiss_member_trigger = {
		core_is_niepolak = yes
	}
}

dismissal_rule_nation_polak = {
	type = dismissal_rules
	visible = { core_is_niepolak = yes }
	ai_will_do = {
		base = 0
	}

	dismiss_member_trigger = {
		core_is_niepolak = no
	}
}

##   ##  ### ###  ##  #  #  ## 
#  # #  #  #   #  #  # ## # #   
#### #     #   #  #  # # ##  #  
#  # #  #  #   #  #  # #  #   # 
#  #  ##   #  ###  ##  #  # ##  


dismissal_rule_world_tension = {
	type = dismissal_rules
	ai_will_do = {
		base = 0
	}
	dismiss_member_trigger = {
		has_added_tension_amount > 0.1
	}
}

dismissal_rule_offensive_war = {
	type = dismissal_rules
	ai_will_do = {
		base = 0
	}
	dismiss_member_trigger = {
		has_offensive_war_without_friend = FROM
	}
}


### ###  ###  ##  #    ##   ##  #   # 
 #  #  # #   #  # #   #  # #     # #  
 #  #  # ##  #  # #   #  # # ##   #   
 #  #  # #   #  # #   #  # #  #   #   
### ###  ###  ##  ###  ##   ##    #   

dismissal_rule_ideology_radical = {
	type = dismissal_rules
	visible = { 
		NOT = { has_government = neonationalism } 
		NOT = { has_government = reactionary } 
		NOT = { has_government = postpolonism } 
		NOT = { has_government = marxism_cranism } 
		NOT = { has_government = squattism } 
	}
	ai_will_do = {
		base = 0
	}

	dismiss_member_trigger = {
		has_government = neonationalism 
		has_government = reactionary 
		has_government = postpolonism 
		has_government = marxism_cranism 
		has_government = squattism 
	}
}

dismissal_rule_ideology_democratic = {
	type = dismissal_rules
	visible = { 
		NOT = { has_government = socialdemocrature } 
		NOT = { has_government = euroliberalism } 
		NOT = { has_government = paternal_conservatism } 
		NOT = { has_government = freemarketism } 
		NOT = { has_government = corporativism } 
	}
	ai_will_do = {
		base = 0
	}

	dismiss_member_trigger = {
		has_government = socialdemocrature 
		has_government = euroliberalism 
		has_government = paternal_conservatism 
		has_government = freemarketism 
		has_government = corporativism 
	}
}

dismissal_rule_ideology_capitalist = {
	type = dismissal_rules
	visible = { 
		NOT = { has_government = euroliberalism } 
		NOT = { has_government = freemarketism } 
		NOT = { has_government = corporativism } 
	}
	ai_will_do = {
		base = 0
	}

	dismiss_member_trigger = {
		has_government = euroliberalism 
		has_government = freemarketism 
		has_government = corporativism 
	}
}

dismissal_rule_ideology_socialist = {
	type = dismissal_rules
	visible = { 
		NOT = { has_government = marxism_cranism } 
		NOT = { has_government = squattism } 
		NOT = { has_government = socialdemocrature } 
	}
	ai_will_do = {
		base = 0
	}

	dismiss_member_trigger = {
		has_government = marxism_cranism 
		has_government = squattism 
		has_government = socialdemocrature 
	}
}

dismissal_rule_ideology_not_socialist = {
	type = dismissal_rules
	visible = { 
		OR = { 
			has_government = marxism_cranism 
			has_government = squattism 
			has_government = socialdemocrature 
		}
	}
	ai_will_do = {
		base = 0
	}

	dismiss_member_trigger = {
		NOT = { has_government = marxism_cranism }
		NOT = { has_government = squattism }
		NOT = { has_government = socialdemocrature }
		NOT = { is_subject_of = var:faction_members }
	}
}

dismissal_rule_ideology_marxism_cranism = {
	type = dismissal_rules
	visible = { NOT = { has_government = marxism_cranism } }
	ai_will_do = {
		base = 0
	}

	dismiss_member_trigger = {
		has_government = marxism_cranism
	}
}

dismissal_rule_ideology_squattism = {
	type = dismissal_rules
	visible = { NOT = { has_government = squattism } }
	ai_will_do = {
		base = 0
	}

	dismiss_member_trigger = {
		has_government = squattism
	}
}

dismissal_rule_ideology_socialdemocrature = {
	type = dismissal_rules
	visible = { NOT = { has_government = socialdemocrature } }
	ai_will_do = {
		base = 0
	}

	dismiss_member_trigger = {
		has_government = socialdemocrature
	}
}

dismissal_rule_ideology_euroliberalism = {
	type = dismissal_rules
	visible = { NOT = { has_government = euroliberalism } }
	ai_will_do = {
		base = 0
	}

	dismiss_member_trigger = {
		has_government = euroliberalism
	}
}

dismissal_rule_ideology_paternal_conservatism = {
	type = dismissal_rules
	visible = { NOT = { has_government = paternal_conservatism } }
	ai_will_do = {
		base = 0
	}

	dismiss_member_trigger = {
		has_government = paternal_conservatism
	}
}

dismissal_rule_ideology_freemarketism = {
	type = dismissal_rules
	visible = { NOT = { has_government = freemarketism } }
	ai_will_do = {
		base = 0
	}

	dismiss_member_trigger = {
		has_government = freemarketism
	}
}

dismissal_rule_ideology_corporativism = {
	type = dismissal_rules
	visible = { NOT = { has_government = corporativism } }
	ai_will_do = {
		base = 0
	}

	dismiss_member_trigger = {
		has_government = corporativism
	}
}

dismissal_rule_ideology_autocracy = {
	type = dismissal_rules
	visible = { NOT = { has_government = autocracy } }
	ai_will_do = {
		base = 0
	}

	dismiss_member_trigger = {
		has_government = autocracy
	}
}

dismissal_rule_ideology_neonationalism = {
	type = dismissal_rules
	visible = { NOT = { has_government = neonationalism } }
	ai_will_do = {
		base = 0
	}

	dismiss_member_trigger = {
		has_government = neonationalism
	}
}

dismissal_rule_ideology_reactionary = {
	type = dismissal_rules
	visible = { NOT = { has_government = reactionary } }
	ai_will_do = {
		base = 0
	}

	dismiss_member_trigger = {
		has_government = reactionary
	}
}

dismissal_rule_ideology_postpolonism = {
	type = dismissal_rules
	visible = { NOT = { has_government = postpolonism } }
	ai_will_do = {
		base = 0
	}

	dismiss_member_trigger = {
		has_government = postpolonism
	}
}
