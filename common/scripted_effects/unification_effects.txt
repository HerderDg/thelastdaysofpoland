regional_unification_effect = {
	add_research_slot = 1
	if = {
		limit = { original_tag = ZHP }
		set_cosmetic_tag = ZHP_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.4 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = SAP 
				 check_variable = {
					var = sapek_sanity
					value = 50
					compare = greater_than_or_equals
				} 
		}			
		set_cosmetic_tag = SAP_sane_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.5 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = SAP 
				 check_variable = {
					var = sapek_sanity
					value = 50
					compare = less_than
				} 
		}
		set_cosmetic_tag = SAP_insane_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.5 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = WML }
		set_cosmetic_tag = WML_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.1 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = BBW }
		set_cosmetic_tag = BBW_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.2 }
			}
		}
		
	}



	else_if = {
		limit = { 
				 original_tag = ZYD
				 has_government = squattism
			}
		set_cosmetic_tag = ZYD_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.3 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = ZYD
				 has_government = marxism_cranism }
		set_cosmetic_tag = ZYD_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.37 }
			}
		}
		
	}


	else_if = {
		limit = { original_tag = KRS
				  has_government = corporativism }
		set_cosmetic_tag = KRS_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.6 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = KRS
				  has_government = postpolonism }
		set_cosmetic_tag = KRS_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.35 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = KRS
				  has_government = autocracy }
		set_cosmetic_tag = KRS_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.36 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = CAL }
		set_cosmetic_tag = CAL_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.31 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = FEL }
		set_cosmetic_tag = FEL_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.33 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = GNI }
		set_cosmetic_tag = GNI_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.32 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = SUS }
		set_cosmetic_tag = SUS_regional
										
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.34 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = CRN }
		set_cosmetic_tag = CRN_regional
							
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.7 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = PUL }
		set_cosmetic_tag = PUL_regional
				
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.8 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = KSN }
		set_cosmetic_tag = KSN_regional
				
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.9 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = SWD }
		set_cosmetic_tag = SWD_regional
				
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.10 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = ZUK }
		hidden_effect = {
			set_capital = {
				state = 363
				remember_old_capital = no
			}
		}
		
		set_cosmetic_tag = ZUK_regional
				
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.11 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = BIL }
		set_cosmetic_tag = BIL_regional
				
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.12 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = BPD }
		set_cosmetic_tag = BPD_regional
					
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.13 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = SAT }
		set_cosmetic_tag = SAT_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.106 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = RAD }
		set_cosmetic_tag = RAD_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.107 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = GAR }
		set_cosmetic_tag = GAR_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.108 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = SKA }
		set_cosmetic_tag = SKA_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.109 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = DGR }
		set_cosmetic_tag = DGR_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.14 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = SOS }
		set_cosmetic_tag = SOS_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.15 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = JAW }
		set_cosmetic_tag = JAW_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.16 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = ZAW
					has_government = paternal_conservatism }
		set_cosmetic_tag = ZAW_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.17 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = CCH }
		set_cosmetic_tag = CCH_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.18 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = BRZ }
		set_cosmetic_tag = BRZ_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.19 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = JWK }
		set_cosmetic_tag = JWK_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.20 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = KRK }
		set_cosmetic_tag = KRK_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.136 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = PRK }
		set_cosmetic_tag = PRK_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.62 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = NHT }
		set_cosmetic_tag = NHT_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.137 }
			}
		}
		
	}	


	else_if = {
		limit = { original_tag = WDW }
		set_cosmetic_tag = WDW_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.138 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = TRN }
		set_cosmetic_tag = TRN_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.139 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = OSW }
		set_cosmetic_tag = OSW_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.140 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = UMN }
		set_cosmetic_tag = UMN_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.141 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = HOR }
		set_cosmetic_tag = HOR_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.142 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = KRA }
		set_cosmetic_tag = KRA_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.143 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = BRK }
		set_cosmetic_tag = BRK_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.144 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = WND }
		set_cosmetic_tag = WND_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.145 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = HAB }
		set_cosmetic_tag = HAB_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.146 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = AGH }
		set_cosmetic_tag = AGH_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.147 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = UJG }
		set_cosmetic_tag = UJG_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.148 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = CIE }
		set_cosmetic_tag = CIE_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.38 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = BSK
				  has_government = autocracy }
		set_cosmetic_tag = BSK_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.39 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = GUR }
		set_cosmetic_tag = GUR_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.40 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = MSL }
		set_cosmetic_tag = MSL_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.41 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = SCZ }
		set_cosmetic_tag = SCZ_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.42 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = LEM }
		set_cosmetic_tag = LEM_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.43 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = BSK
				  has_government = freemarketism }
		set_cosmetic_tag = BSK_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.44 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = ASG }
		set_cosmetic_tag = ASG_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.21 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = WOD }
		set_cosmetic_tag = WOD_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.22 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = JSW }
		set_cosmetic_tag = JSW_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.23 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = GLI }
		set_cosmetic_tag = GLI_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.24 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = CHO }
		set_cosmetic_tag = CHO_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.25 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = RAS }
		set_cosmetic_tag = RAS_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.26 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = FER }
		set_cosmetic_tag = FER_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.27 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = RUD }
		set_cosmetic_tag = RUD_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.28 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = KAT }
		set_cosmetic_tag = KAT_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.29 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = TRC }
		set_cosmetic_tag = TRC_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.30 }
			}
		}
		
	}
	
	
	else_if = {
		limit = { original_tag = MNI }
		set_cosmetic_tag = MNI_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.63 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = OPO }
		set_cosmetic_tag = OPO_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.64 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = LCH }
		set_cosmetic_tag = LCH_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.65 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = WND }
		set_cosmetic_tag = WND_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.66 }
			}
		}
		
	}



	else_if = {
		limit = { 
				 original_tag = LEH	}
		set_cosmetic_tag = LEH_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.67 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = NYS }
		set_cosmetic_tag = NYS_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.68 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = POZ }
		set_cosmetic_tag = POZ_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.50 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = SYJ }
		set_cosmetic_tag = SYJ_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.51 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = NAC }
		set_cosmetic_tag = NAC_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.52 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = SLU }
		set_cosmetic_tag = SLU_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.61 }
			}
		}
		
	}
	
	
	
	else_if = {
		limit = { original_tag = PIL }
		set_cosmetic_tag = PIL_regional
										
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.53 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = INO }
		set_cosmetic_tag = INO_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.79 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = TOR
				  has_government = corporativism }
		set_cosmetic_tag = TOR_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.80 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = GRU }
		set_cosmetic_tag = GRU_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.81 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = KUJ }
		set_cosmetic_tag = KUJ_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.82 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = BYD }
		set_cosmetic_tag = BYD_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.83 }
			}
		}
		
	}	
	
	
	else_if = {
		limit = { original_tag = TOR
				  has_government = neonationalism }
		set_cosmetic_tag = TOR_regional

		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.151 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = TOR
				  has_government = reactionary }
		set_cosmetic_tag = TOR_regional

		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.149 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = TOR
				  has_government = freemarketism }
		set_cosmetic_tag = TOR_regional

		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.150 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = WRO }
		set_cosmetic_tag = WRO_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.46 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = CZE }
		set_cosmetic_tag = CZE_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.47 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = EKS }
		set_cosmetic_tag = EKS_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.45 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = KGH }
		set_cosmetic_tag = KGH_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.48 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = RYB }
		set_cosmetic_tag = RYB_regional
		hidden_effect = {
			every_country = {
				limit = { RYB = { is_ai = yes } }
				news_event = { id = unificationreg.49 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = ANA }
		set_cosmetic_tag = ANA_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.57 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = ZUL }
		set_cosmetic_tag = ZUL_regional
										
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.59 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = MNL }
		set_cosmetic_tag = MNL_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.58 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = CYG }
		set_cosmetic_tag = CYG_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.60 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = MPK }
		set_cosmetic_tag = MPK_regional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.78 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = ZAG }
		set_cosmetic_tag = ZAG_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.69 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = WIN }
		set_cosmetic_tag = WIN_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.70 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = KAM }
		set_cosmetic_tag = KAM_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.71 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = MOT }
		set_cosmetic_tag = MOT_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.72 }
			}
		}
		
	}

	else_if = {
		limit = {
			original_tag = JAS
			has_government = postpolonism
		}
		set_cosmetic_tag = JAS_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.73 }
			}
		}
	}

	else_if = {
		limit = { 
			original_tag = JAS
			has_government = autocracy
		}
		set_cosmetic_tag = JAS_regional_poreba
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.731 }
			}
		}
	}

	else_if = {
		limit = { 
			original_tag = JAS
			has_government = neonationalism
		}
		set_cosmetic_tag = JAS_regional_sendecki
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.732 }
			}
		}
	}



	else_if = {
		limit = { original_tag = EKW }
		set_cosmetic_tag = EKW_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.54 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = STE }
		set_cosmetic_tag = STE_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.74 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = SWI }
		set_cosmetic_tag = SWI_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.75 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = JOM }
		set_cosmetic_tag = JOM_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.76 }
			}
		}
		
	}	

	else_if = {
		limit = { original_tag = USP }
		set_cosmetic_tag = USP_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.77 }
			}
		}
		
	}	

	else_if = {
		limit = { original_tag = OWS }
		set_cosmetic_tag = OWS_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.84 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = BOL }
		set_cosmetic_tag = BOL_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.85 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = GDA }
		set_cosmetic_tag = GDA_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.86 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = GAY }
		set_cosmetic_tag = GAY_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.87 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = KSZ }
		set_cosmetic_tag = KSZ_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.88 }
			}
		}
		
	}	

	else_if = {
		limit = { original_tag = HEL }
		set_cosmetic_tag = HEL_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.89 }
			}
		}
		
	}	
	
	else_if = {
		limit = { original_tag = CZL }
		set_cosmetic_tag = CZL_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.90 }
			}
		}
		
	}	

	else_if = {
		limit = { original_tag = LOT }
		set_cosmetic_tag = LOT_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.91 }
			}
		}
		
	}	

	else_if = {
		limit = { original_tag = KOR }
		set_cosmetic_tag = KOR_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.92 }
			}
		}
		
	}		
	
	else_if = {
		limit = { original_tag = MAL }
		set_cosmetic_tag = MAL_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.93 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = ROS }
		set_cosmetic_tag = ROS_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.56 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = TEU }
		set_cosmetic_tag = TEU_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.94 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = PRU }
		set_cosmetic_tag = PRU_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.95 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = PZW }
		set_cosmetic_tag = PZW_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.96 }
			}
		}
		
	}	

	else_if = {
		limit = { original_tag = WRM }
		set_cosmetic_tag = WRM_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.97 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = SAN }
		set_cosmetic_tag = SAN_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.98 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = TAT }
		set_cosmetic_tag = TAT_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.99 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = BOM }
		set_cosmetic_tag = BOM_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.100 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = KON }
		set_cosmetic_tag = KON_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.101 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = BEL }
		set_cosmetic_tag = BEL_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.102 }
			}
		}
		
	}	

	else_if = {
		limit = { original_tag = LMZ }
		set_cosmetic_tag = LMZ_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.103 }
			}
		}
		
	}	

	else_if = {
		limit = { original_tag = SDL }
		set_cosmetic_tag = SDL_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				#news_event = { id = unificationreg.104 } outdated
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = AGR }
		set_cosmetic_tag = AGR_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.105 }
			}
		}
		
	}		

	else_if = {
		limit = { original_tag = ORL }
		set_cosmetic_tag = ORL_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.110 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = SAS }
		set_cosmetic_tag = SAS_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.55 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = POL }
		set_cosmetic_tag = POL_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.111 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = KEL }
		set_cosmetic_tag = KEL_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.121 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = NRA }
		set_cosmetic_tag = NRA_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.122 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = NSZ }
		set_cosmetic_tag = NSZ_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.123 }
			}
		}
		
	}	


	else_if = {
		limit = { original_tag = SND }
		set_cosmetic_tag = SND_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.124 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = KIE }
		set_cosmetic_tag = KIE_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.125 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = STO }
		set_cosmetic_tag = STO_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.126 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = STA }
		set_cosmetic_tag = STA_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.127 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = RZE }
		set_cosmetic_tag = RZE_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.128 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = CHR }
		set_cosmetic_tag = CHR_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.129 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = UPA }
		set_cosmetic_tag = UPA_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.130 }
			}
		}
		
	}	


	else_if = {
		limit = { original_tag = KOZ }
		set_cosmetic_tag = KOZ_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.131 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = BSC }
		set_cosmetic_tag = BSC_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.132 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = KRO }
		set_cosmetic_tag = KRO_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.133 }
			}
		}
		
	}


	else_if = {
		limit = { original_tag = TAR }
		set_cosmetic_tag = TAR_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.134 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = LGP }
		set_cosmetic_tag = LGP_regional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationreg.135 }
			}
		}
		
	}
}

superregional_unification_effect = {
	add_research_slot = 1
	if = {
		limit = { original_tag = ZHP }
		set_cosmetic_tag = ZHP_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.4 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = SAP 
				 check_variable = {
					var = sapek_sanity
					value = 50
					compare = greater_than_or_equals
				} 
		}			
		set_cosmetic_tag = SAP_sane_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.5 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = SAP 
				 check_variable = {
					var = sapek_sanity
					value = 50
					compare = less_than
				} 
		}
		set_cosmetic_tag = SAP_insane_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.5 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = WML }
		set_cosmetic_tag = WML_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.1 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = BBW }
		set_cosmetic_tag = BBW_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.2 }
			}
		}
		
	}

	else_if = {
		limit = { 
				 original_tag = ZYD
				 has_government = squattism
			}
		set_cosmetic_tag = ZYD_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.3 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = ZYD
				 has_government = marxism_cranism }
		set_cosmetic_tag = ZYD_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.37 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = KRS
				  has_government = corporativism }
		set_cosmetic_tag = KRS_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.6 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = KRS
				  has_government = postpolonism }
		set_cosmetic_tag = KRS_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.35 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = KRS
				  has_government = autocracy }
		set_cosmetic_tag = KRS_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.36 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = CAL }
		set_cosmetic_tag = CAL_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.31 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = FEL }
		set_cosmetic_tag = FEL_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.33 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = GNI }
		set_cosmetic_tag = GNI_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.32 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = SUS }
		set_cosmetic_tag = SUS_supregional
										
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.34 }
			}
		}
		
	}

	else_if = {
		limit = { 
			original_tag = JAS
			has_government = postpolonism
		}
		set_cosmetic_tag = JAS_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.73 }
			}
		}
	}

	else_if = {
		limit = {
			original_tag = JAS
			has_government = autocracy
		}
		set_cosmetic_tag = JAS_supregional_poreba 
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.731 }
			}
		}
	}

	else_if = {
		limit = {
			original_tag = JAS
			has_government = neonationalism
		}
		set_cosmetic_tag = JAS_supregional_sendecki 
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.732 }
			}
		}
	}

	else_if = {
		limit = { original_tag = EKW }
		set_cosmetic_tag = EKW_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.54 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = STE }
		set_cosmetic_tag = STE_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.74 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = SWI }
		set_cosmetic_tag = SWI_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.75 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = JOM }
		set_cosmetic_tag = JOM_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.76 }
			}
		}
		
	}	

	else_if = {
		limit = { original_tag = USP }
		set_cosmetic_tag = USP_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.77 }
			}
		}
		
	}	

	else_if = {
		limit = { original_tag = OWS }
		set_cosmetic_tag = OWS_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.84 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = BOL }
		set_cosmetic_tag = BOL_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.85 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = GDA }
		set_cosmetic_tag = GDA_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.86 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = GAY }
		set_cosmetic_tag = GAY_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.87 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = KSZ }
		set_cosmetic_tag = KSZ_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.88 }
			}
		}
		
	}	

	else_if = {
		limit = { original_tag = HEL }
		set_cosmetic_tag = HEL_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.89 }
			}
		}
		
	}	
	
	else_if = {
		limit = { original_tag = CZL }
		set_cosmetic_tag = CZL_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.90 }
			}
		}
		
	}	

	else_if = {
		limit = { original_tag = LOT }
		set_cosmetic_tag = LOT_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.91 }
			}
		}
		
	}	

	else_if = {
		limit = { original_tag = KOR }
		set_cosmetic_tag = KOR_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.92 }
			}
		}
		
	}	

	else_if = {
		limit = { original_tag = POZ }
		set_cosmetic_tag = POZ_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.50 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = SYJ }
		set_cosmetic_tag = SYJ_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.51 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = NAC }
		set_cosmetic_tag = NAC_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.52 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = SLU }
		set_cosmetic_tag = SLU_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.61 }
			}
		}
		
	}
			
	else_if = {
		limit = { original_tag = PIL }
		set_cosmetic_tag = PIL_supregional
										
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.53 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = INO }
		set_cosmetic_tag = INO_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.79 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = TOR
				  has_government = corporativism }
		set_cosmetic_tag = TOR_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.80 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = GRU }
		set_cosmetic_tag = GRU_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.81 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = KUJ }
		set_cosmetic_tag = KUJ_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.82 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = BYD }
		set_cosmetic_tag = BYD_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.83 }
			}
		}
		
	}
		
	else_if = {
		limit = { original_tag = TOR
				  has_government = neonationalism }
		set_cosmetic_tag = TOR_supregional

		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.151 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = TOR
				  has_government = reactionary }
		set_cosmetic_tag = TOR_supregional

		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.149 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = TOR
				  has_government = freemarketism }
		set_cosmetic_tag = TOR_supregional

		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.150 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = CRN }
		set_cosmetic_tag = CRN_supregional
							
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.7 }
			}
		}	
	}

	else_if = {
		limit = { original_tag = PUL }
		set_cosmetic_tag = PUL_supregional
				
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.8 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = KSN }
		set_cosmetic_tag = KSN_supregional
				
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.9 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = SWD }
		set_cosmetic_tag = SWD_supregional
				
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.10 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = ZUK }
		hidden_effect = {
			set_capital = {
				state = 363
				remember_old_capital = no
			}
		}
		
		set_cosmetic_tag = ZUK_supregional
				
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.11 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = BIL }
		set_cosmetic_tag = BIL_supregional
				
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.12 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = BPD }
		set_cosmetic_tag = BPD_supregional
					
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.13 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = SAT }
		set_cosmetic_tag = SAT_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.106 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = RAD }
		set_cosmetic_tag = RAD_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.107 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = GAR }
		set_cosmetic_tag = GAR_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.108 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = SKA }
		set_cosmetic_tag = SKA_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.109 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = DGR }
		set_cosmetic_tag = DGR_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.14 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = SOS }
		set_cosmetic_tag = SOS_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.15 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = JAW }
		set_cosmetic_tag = JAW_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.16 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = ZAW
					has_government = paternal_conservatism }
		set_cosmetic_tag = ZAW_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.17 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = CCH }
		set_cosmetic_tag = CCH_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.18 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = BRZ }
		set_cosmetic_tag = BRZ_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.19 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = JWK }
		set_cosmetic_tag = JWK_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.20 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = KRK }
		set_cosmetic_tag = KRK_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.136 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = PRK }
		set_cosmetic_tag = PRK_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.62 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = NHT }
		set_cosmetic_tag = NHT_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.137 }
			}
		}
		
	}	

	else_if = {
		limit = { original_tag = WDW }
		set_cosmetic_tag = WDW_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.138 }
			}
		}
	}

	else_if = {
		limit = { original_tag = TRN }
		set_cosmetic_tag = TRN_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.139 }
			}
		}
	}

	else_if = {
		limit = { original_tag = OSW }
		set_cosmetic_tag = OSW_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.140 }
			}
		}
	}

	else_if = {
		limit = { original_tag = UMN }
		set_cosmetic_tag = UMN_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.141 }
			}
		}
	}
	
	else_if = {
		limit = { original_tag = HOR }
		set_cosmetic_tag = HOR_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.142 }
			}
		}
	}
	
	else_if = {
		limit = { original_tag = KRA }
		set_cosmetic_tag = KRA_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.143 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = BRK }
		set_cosmetic_tag = BRK_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.144 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = WND }
		set_cosmetic_tag = WND_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.145 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = HAB }
		set_cosmetic_tag = HAB_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.146 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = AGH }
		set_cosmetic_tag = AGH_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.147 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = UJG }
		set_cosmetic_tag = UJG_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.148 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = CIE }
		set_cosmetic_tag = CIE_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.38 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = BSK
				  has_government = autocracy }
		set_cosmetic_tag = BSK_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.39 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = GUR }
		set_cosmetic_tag = GUR_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.40 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = MSL }
		set_cosmetic_tag = MSL_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.41 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = SCZ }
		set_cosmetic_tag = SCZ_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.42 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = LEM }
		set_cosmetic_tag = LEM_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.43 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = BSK
				  has_government = freemarketism }
		set_cosmetic_tag = BSK_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.44 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = KEL }
		set_cosmetic_tag = KEL_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.121 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = NRA }
		set_cosmetic_tag = NRA_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.122 }
			}
		}
	}

	else_if = {
		limit = { original_tag = NSZ }
		set_cosmetic_tag = NSZ_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.123 }
			}
		}
		
	}	

	else_if = {
		limit = { original_tag = SND }
		set_cosmetic_tag = SND_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.124 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = KIE }
		set_cosmetic_tag = KIE_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.125 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = STO }
		set_cosmetic_tag = STO_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.126 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = STA }
		set_cosmetic_tag = STA_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.127 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = RZE }
		set_cosmetic_tag = RZE_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.128 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = CHR }
		set_cosmetic_tag = CHR_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.129 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = UPA }
		set_cosmetic_tag = UPA_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.130 }
			}
		}
		
	}	

	else_if = {
		limit = { original_tag = KOZ }
		set_cosmetic_tag = KOZ_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.131 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = BSC }
		set_cosmetic_tag = BSC_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.132 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = KRO }
		set_cosmetic_tag = KRO_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.133 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = TAR }
		set_cosmetic_tag = TAR_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.134 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = LGP }
		set_cosmetic_tag = LGP_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.135 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = SAN }
		set_cosmetic_tag = SAN_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.98 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = TAT }
		set_cosmetic_tag = TAT_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.99 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = BOM }
		set_cosmetic_tag = BOM_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.100 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = KON }
		set_cosmetic_tag = KON_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.101 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = BEL }
		set_cosmetic_tag = BEL_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.102 }
			}
		}
		
	}	

	else_if = {
		limit = { original_tag = LMZ }
		set_cosmetic_tag = LMZ_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.103 }
			}
		}
		
	}	

	else_if = {
		limit = { original_tag = SDL }
		set_cosmetic_tag = SDL_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.104 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = AGR }
		set_cosmetic_tag = AGR_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.105 }
			}
		}
		
	}		

	else_if = {
		limit = { original_tag = ORL }
		set_cosmetic_tag = ORL_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.110 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = SAS }
		set_cosmetic_tag = SAS_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.55 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = POL }
		set_cosmetic_tag = POL_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.111 }
			}
		}
		
	}	

	else_if = {
		limit = { original_tag = MAL }
		set_cosmetic_tag = MAL_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.93 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = ROS }
		set_cosmetic_tag = ROS_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.56 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = TEU }
		set_cosmetic_tag = TEU_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.94 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = PRU }
		set_cosmetic_tag = PRU_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.95 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = PZW }
		set_cosmetic_tag = PZW_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.96 }
			}
		}
		
	}	

	else_if = {
		limit = { original_tag = WRM }
		set_cosmetic_tag = WRM_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.97 }
			}
		}
		
	}			

	else_if = {
		limit = { original_tag = MAF }
		set_cosmetic_tag = MAF_voivodeship

		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.112 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = WOW }
		set_cosmetic_tag = WOW_voivodeship

		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.113 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = ZYR }
		set_cosmetic_tag = ZYR_voivodeship

		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.114 }
			}
		}
		
	}	


	else_if = {
		limit = { original_tag = DUP }
		set_cosmetic_tag = DUP_voivodeship

		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.115 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = BIE }
		set_cosmetic_tag = BIE_voivodeship

		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.116 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = PLC }
		set_cosmetic_tag = PLC_voivodeship

		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.117 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = ZIE }
		set_cosmetic_tag = ZIE_voivodeship

		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.118 }
			}
		}
		
	}


	else_if = {
		limit = { original_tag = TVP }
		set_cosmetic_tag = TVP_voivodeship

		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.119 }
			}
		}
		
	}


	else_if = {
		limit = { original_tag = TRZ }
		set_cosmetic_tag = TRZ_voivodeship

		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.120 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = ASG }
		set_cosmetic_tag = ASG_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.21 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = WOD }
		set_cosmetic_tag = WOD_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.22 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = JSW }
		set_cosmetic_tag = JSW_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.23 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = GLI }
		set_cosmetic_tag = GLI_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.24 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = CHO }
		set_cosmetic_tag = CHO_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.25 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = RAS }
		set_cosmetic_tag = RAS_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.26 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = FER }
		set_cosmetic_tag = FER_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.27 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = RUD }
		set_cosmetic_tag = RUD_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.28 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = KAT }
		set_cosmetic_tag = KAT_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.29 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = TRC }
		set_cosmetic_tag = TRC_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.30 }
			}
		}
		
	}
	
	
	else_if = {
		limit = { original_tag = MNI }
		set_cosmetic_tag = MNI_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.63 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = OPO }
		set_cosmetic_tag = OPO_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.64 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = LCH }
		set_cosmetic_tag = LCH_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.65 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = WND }
		set_cosmetic_tag = WND_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.66 }
			}
		}
		
	}



	else_if = {
		limit = { 
				 original_tag = LEH	}
		set_cosmetic_tag = LEH_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.67 }
			}
		}
		
	}

	else_if = {
		limit = { original_tag = NYS }
		set_cosmetic_tag = NYS_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.68 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = WRO }
		set_cosmetic_tag = WRO_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.46 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = CZE }
		set_cosmetic_tag = CZE_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.47 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = EKS }
		set_cosmetic_tag = EKS_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.45 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = KGH }
		set_cosmetic_tag = KGH_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.48 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = RYB }
		set_cosmetic_tag = RYB_supregional
		hidden_effect = {
			every_country = {
				limit = { RYB = { is_ai = yes } }
				news_event = { id = unificationsupreg.49 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = ANA }
		set_cosmetic_tag = ANA_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.57 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = ZUL }
		set_cosmetic_tag = ZUL_supregional
										
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.59 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = MNL }
		set_cosmetic_tag = MNL_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.58 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = CYG }
		set_cosmetic_tag = CYG_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.60 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = MPK }
		set_cosmetic_tag = MPK_supregional
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.78 }
			}
		}
		
	}
	
	else_if = {
		limit = { original_tag = ZAG }
		set_cosmetic_tag = ZAG_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.69 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = WIN }
		set_cosmetic_tag = WIN_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.70 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = KAM }
		set_cosmetic_tag = KAM_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.71 }
			}
		}
		
	}



	else_if = {
		limit = { original_tag = MOT }
		set_cosmetic_tag = MOT_supregional
		
		hidden_effect = {
			every_country = {
				limit = { NOT = { is_ai = yes } }
				news_event = { id = unificationsupreg.72 }
			}
		}
		
	}
}