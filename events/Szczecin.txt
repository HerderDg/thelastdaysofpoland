add_namespace = szczecin



country_event = { #charter of freedoms#
	id = szczecin.1
	title = szczecin.1.t
	desc = szczecin.1.desc
	picture = GFX_event_szczecin.1

	fire_only_once = yes
	is_triggered_only = yes

	immediate = { log = "[GetDateText]: [ROOT.GetName] fires szczecin.1" }

	option = {
		name = szczecin.1.a
		swap_ideas = {
			remove_idea = partial_refundation
			add_idea = private_healthcare
		}
		swap_ideas = {
			remove_idea = civil_industry_subsidies
			add_idea = no_subsidies
		}
		swap_ideas = {
			remove_idea = random_enlistment
			add_idea = volunteer_only
		}
		swap_ideas = {
			remove_idea = economy_regulated_capitalism
			add_idea = economy_laissez_faire
		}
		swap_ideas = {
			remove_idea = taxes_medium
			add_idea = taxes_minimal
		}
		swap_ideas = {
			remove_idea = trade_regulated
			add_idea = trade_free
		}
	}
}

