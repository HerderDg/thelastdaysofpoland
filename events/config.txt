add_namespace = config

country_event = {
	id = config.1
	title = config.1.t
	desc = config.1.desc

	fire_only_once = yes
	is_triggered_only = yes

	trigger = {
		is_ai = no
	}

	immediate = { log = "[GetDateText]: [ROOT.GetName] fires config.1" }

	option = {
		name = config.1.a
		custom_effect_tooltip = "Levels of goverment will be unlocked by taking enough of former Polish territory"
		set_global_flag = ffa_unification
	}

	option = {
		name = config.1.b
		custom_effect_tooltip = "Levels of goverment will be unlocked by taking certain parts of former Polish territory"
		set_global_flag = story_unification
	}
}