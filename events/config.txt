add_namespace = config

country_event = {
	id = config.1
	title = config.1.t
	desc = config.1.desc

	is_triggered_only = yes
	fire_only_once = yes


	immediate = { log = "[GetDateText]: [ROOT.GetName] fires config.1" }

	option = {
		name = config.1.a
		custom_effect_tooltip = config_ffa_tt
		set_global_flag = ffa_unification
		
		if = { 
			limit = { has_country_flag = curse_of_piu }
			country_event = {
				id = config.2
				}
			}
	}

	option = {
		name = config.1.b
		custom_effect_tooltip = config_story_tt
		set_global_flag = story_unification
		
		if = { 
			limit = { has_country_flag = curse_of_piu }
			country_event = {
				id = config.2
				}
			}
	}
}


country_event = {
	id = config.2
	title = config.2.t
	desc = config.2.desc

	is_triggered_only = yes
	fire_only_once = yes


	immediate = { log = "[GetDateText]: [ROOT.GetName] fires config.2" }

	option = {
		name = config.2.a
	}

}

country_event = {
	id = config.3
	title = config.3.t
	desc = config.3.desc
	hidden = yes
	is_triggered_only = yes

	hidden = yes
	immediate = { log = "STO LAT PIU"
        set_global_flag = Super_Event_Visible
        set_global_flag = Piurodziny_super_visible
		hidden_effect = {
			if ={
				limit = {
					NOT = {
						has_global_flag = superevents_muted
					}
				}
			sound_effect = "Piurodziny_super"
			play_song = "cisza60"
			}
		}
	}

	option = {
		name = config.3.a
		every_country = {
			limit = { is_ai = no }
		country_event = {
			id = config.3
			days = 365
		}
		}
	}

}
