add_namespace = trzasek

country_event = {
	id = trzasek.1
	title = trzasek.1.t
	desc = trzasek.1.desc

	fire_only_once = yes
		trigger = {
		date > 2028.08.01
		country_exists = TVN
		TVP = {
		    exists = no }
		}

	immediate = { log = "[GetDateText]: [ROOT.GetName] fires trzasek.1" }

	option = {
		name = trzasek.1.a 
		annex_country = {
            target = TVN
            transfer_troops = no
        }
    }
}